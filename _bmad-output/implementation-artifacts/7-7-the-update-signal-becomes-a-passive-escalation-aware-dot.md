---
baseline_commit: 3befa36164dfbe1854dd9cc90ed68ea078c438ad
---

# Story 7.7: The update-signal becomes a passive, escalation-aware dot

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a privacy-conscious developer,
I want update notifications to wait for me instead of interrupting me on every launch,
so that staying current never feels like the app is nagging.

## Acceptance Criteria

1. **Given** a new release exists (`checkForUpdate()` resolves non-null), **when** the check completes, **then** no dialog opens automatically — a dot appears on the Settings sidebar item instead, replacing `UpdateDialog.vue`'s current auto-show-on-mount behavior. [Source: epics.md#Story-7.7, AC1]
2. **Given** the dot's color, **when** the release's `latest.json` `notes` field is inspected, **then** it renders orange (`accent-signature`, "Update available") by default, or red (`accent-destructive`, "Security update available") if `notes` matches a documented severity marker — this story defines and documents that marker syntax (e.g. a leading `[security]` tag), since Tauri's `latest.json` has no native severity field (confirmed via Context7, see Dev Notes "Tauri updater — Context7-verified specifics"). [Source: epics.md#Story-7.7, AC2; PRD FR31]
3. **Given** the update signal, **when** the Settings sidebar item is clicked or keyboard-activated (its existing navigation behavior, unchanged), **then** Settings opens and shows the pending update (version and notes) with a control that opens the existing update-consent dialog (`UpdateDialog.vue`'s markup, reused via `DESIGN.md`'s floating-surface token), carrying the Not Now / Install & Restart actions — same install flow as today, just no longer auto-triggered. **Amended 2026-08-18** (before implementation, developer-directed during story creation — see Dev Notes "Post-story-creation UX correction"): the original text made the dot itself independently clickable; the dot is now a pure visual indicator, and the actionable control lives on the Settings page instead. **Amended again 2026-08-18** (second pass, caught by this story's code review): a later live-implementation-review round moved the pending-update banner out of the Privacy section — it renders at the top of the Settings page, above all sections, so a pending update is the first thing seen rather than something to scroll past. The Privacy section is unchanged and does not duplicate the banner; see Dev Notes "Post-implementation UX correction." [Source: epics.md#Story-7.7, AC3 (amended); DESIGN.md:107,181]
4. **Given** "Not Now" is clicked, **when** the dialog closes, **then** the dismissal is persisted (new `shell.updateSignal.dismissedVersion` key, AD-10) and the dot remains visible on subsequent launches — no re-nagging toast, closing the gap against today's shipped behavior (a blocking modal on every launch until installed). [Source: epics.md#Story-7.7, AC4; EXPERIENCE.md:136 (Flow 3, step 3)]
5. **Given** the update-check itself fails (offline, network error), **when** this occurs, **then** the signal simply doesn't advance past its last-known state — no error toast, no retry prompt; the check re-attempts on the next scheduled interval. [Source: epics.md#Story-7.7, AC5; EXPERIENCE.md:77]
6. **Given** this story's new persisted keys, **when** audited, **then** they're documented as an extension of AD-10 (a new `shell.updateSignal.*` namespace) in `ARCHITECTURE.md`/`ARCHITECTURE-SPINE.md` — this story is also where that architecture-doc gap gets closed. [Source: epics.md#Story-7.7, AC6; ARCHITECTURE-SPINE.md AD-10 (:99-103)]
7. **Given** the update signal's accessible name, **when** read by assistive tech, **then** it changes between "Update available" and "Security update available" — legible through assistive tech, not just visually. **Amended 2026-08-18** alongside AC3: since the dot is no longer independently focusable, this is exposed via the Settings nav item's own accessible name (an appended visually-hidden text node), not a second focusable element. [Source: epics.md#Story-7.7, AC7 (amended); PRD NFR5]

## Tasks / Subtasks

- [x] **Task 0: Branch** (AC: all)
  - [x] Branch from `origin/main` at `3befa36` (or the docs-branch tip carrying this story file, content-equivalent for every other file — Stories 7.3–7.6's precedent). Confirm working tree is clean before starting (the pre-existing untracked `.claude/workflows/` dir is unrelated, present since Story 7.2 — do not commit it).

- [x] **Task 1: Define the severity-marker convention and write the pure parsing/formatting logic** (AC: 2)
  - [x] In `src/shell/updateCheck.ts` (the existing home for update-related pure logic, imported by the dot, the Settings banner, and the dialog alike), add a pure function, e.g. `getUpdateSeverity(update: Update | null): "none" | "routine" | "security"`, keyed off `update.body` — **not** `update.rawJson.notes` directly; Context7-confirmed the JS `Update` class's `body` field *is* the JS-side binding for `latest.json`'s `notes` field (see Dev Notes). `"none"` when `update` is `null`.
  - [x] Marker syntax recommendation (documents epics.md's own suggested example, resolving PRD FR31's "exact marker syntax... remain open" note): a case-insensitive `[security]` tag at the very start of `body` (optionally followed by whitespace) marks the release as security-urgent. No other releases match — anything else, including an absent/empty `body`, is `"routine"`.
  - [x] Decide and document (in code comment + Dev Agent Record) whether the dialog strips the marker from the *displayed* release notes text. Recommended: strip it — this codebase's own established principle (Story 7.6's UX-correction round) is not leaking internal/implementation-facing markup to end users; showing a raw `[security]` tag verbatim in prose reads as unfinished. Not explicitly required by any AC, so record the call rather than silently deciding it.
  - [x] Unit-test `getUpdateSeverity()` directly in `updateCheck.spec.ts`: `null` → `"none"`; no `body` → `"routine"`; ordinary `body` → `"routine"`; `"[security] Fixes CVE-..."` → `"security"`; case variants (`"[SECURITY]"`, `"[Security]"`) → `"security"`; marker present but not at the start (e.g. `"See [security] notes below"`) → `"routine"` (deliberately strict — an accidental substring match must not falsely escalate a routine release).

- [x] **Task 2: Add `shell.updateSignal.dismissedVersion` to the settings store** (AC: 4, 6)
  - [x] Follow the exact key-addition recipe Story 7.5/7.6 established (see Dev Notes "Previous story intelligence"): add `updateSignalDismissedVersion: undefined as string | undefined` to `DEFAULTS` in `src/stores/settings.ts`; add a matching `ref`; read it in `init()` via `await store.get<string>("shell.updateSignal.dismissedVersion")`; add a setter (e.g. `setUpdateSignalDismissed(version: string): Promise<void>`) that sets the ref, bails if `!backingStore`, then `store.set()` + `store.save()`; add a `"shell.updateSignal.dismissedVersion"` case to `resetKey()`'s switch resetting the ref to `DEFAULTS.updateSignalDismissedVersion`; add the same reset line to `clearAll()`. Return the new ref/setter from the store.
  - [x] This is the **first nested `shell.*` namespace** (`shell.updateSignal.dismissedVersion`, vs. the flat `shell.themeOverride` style every existing key uses) — `resetKey()`'s switch matches on the full string key, so a nested dot in the string works with zero changes to its matching logic; no other code assumes keys are single-segment. Confirm this with a quick read of `resetKey()` before assuming it "just works."
  - [x] Add `settings.spec.ts` tests mirroring the existing per-key pattern: absent-key default, existing-install preservation (pre-seed then `init()`), the new setter persists and updates the ref, `resetKey("shell.updateSignal.dismissedVersion")` clears it from `entries()`, and `clearAll()` resets it too.

- [x] **Task 3: Decide and implement how the dot (`AppSidebar.vue`), the Settings banner (`SettingsView.vue`), and the dialog (`UpdateDialog.vue`) share update-check state** (AC: 1, 2, 3, 7)
  - [x] **Architecture judgment call — read before writing code.** `AppSidebar.vue`, `SettingsView.vue`, and `UpdateDialog.vue` are three unrelated components with no shared parent providing state (`AppSidebar`/`UpdateDialog` are flat siblings in `App.vue`, `src/App.vue:10,16`; `SettingsView` is a separate routed view entirely). All three need to read "is an update pending, and what severity" (Task 1's `getUpdateSeverity()` + the checked `Update` object), and `SettingsView`'s new button and `UpdateDialog` both need to coordinate open/close state. Recommended approach: a small shared **non-Pinia** module, e.g. `src/shell/updateSignal.ts`, exporting a module-level singleton (`shallowRef<Update | null>` for the checked update, `ref<boolean>` for dialog-open state, plus `runCheck()`/`openDialog()`/`closeDialog()` functions) that all three components import directly — the same "shell owns a cross-cutting concern as a shared module" pattern already established by `src/shell/icons.ts` (icon resolution) and the AD-14 clipboard service, and consistent with how `AppSidebar.vue`/`SettingsView.vue` already pull everything they need from imported stores/modules rather than receiving props. Three independent consumers make this shared-module case even stronger than a two-consumer one.
  - [x] **Do not** add this as a third Pinia store. AD-6 names exactly two Pinia stores (`settings`, `registry`) for cross-cutting state, and AD-10 scopes the `settings` store specifically to *persisted* state with a single-writer contract — the checked `Update` object and dialog-open boolean are both purely ephemeral (never written to disk; only the dismissal outcome is, via Task 2's key), so folding them into `settings` would blur that boundary for no benefit. Record whichever approach is actually implemented in the Dev Agent Record's Completion Notes, since this is a call this story is making, not one epics.md/architecture already settled.
  - [x] Move `runCheck()`'s `onMounted` trigger (today in `UpdateDialog.vue`, `:72-76`) into whichever module owns the shared state, called once from `App.vue`'s setup (or from the shared module itself on first import) — must stay decoupled from `main.ts`'s mount()+show() critical path exactly as `UpdateDialog.vue`'s existing comment already requires (`:73`).
  - [x] Do **not** add a `setInterval`/polling mechanism. Nothing in this codebase re-checks for updates within a running session today — grep confirms `checkForUpdate` is called exactly once, on mount. AC5/EXPERIENCE.md's "next scheduled interval" phrase maps to **the next app launch**, not an in-session timer — `EXPERIENCE.md`'s Flow 3 step 1 says explicitly "A new release ships. **On next launch**, a dot appears." Adding a new interval mechanism here would be scope creep beyond what any source document asks for.

- [x] **Task 4: Refactor `UpdateDialog.vue` to be externally controlled instead of self-showing** (AC: 1, 3)
  - [x] Remove the `v-if="pendingUpdate"` auto-show — the dialog now renders based on Task 3's shared `dialogOpen` state instead, decoupled from whether `pendingUpdate` is non-null.
  - [x] Keep every existing interior behavior unchanged: focus-trap-on-open/restore-on-close (`:62-70`), Escape-to-dismiss guarded against an in-flight install (`:52-58`), `onInstall`/`onDismiss` logic, the disabled-buttons-while-installing state, the inline install-error message. Only the *triggering* mechanism changes (external `dialogOpen`/click instead of "any pending update auto-opens").
  - [x] Wire `onDismiss` to also call the Task 2 setter (`settings.setUpdateSignalDismissed(pendingUpdate.value.version)`) before closing, so AC4's persistence actually happens — today's `onDismiss` only calls `update?.close()` and clears local state, with no persistence step at all.
  - [x] **Retrofit this file's `<style>` block to the token system** — it was never touched by Story 7.1/7.2's rollout and still hardcodes pre-rebrand values: `background: #fff` → `var(--color-bg-surface)`; `border-radius: 8px` → `var(--radius-lg)` (already `8px` — an exact, drop-in match); the box-shadow → `var(--shadow-floating)` (defined in `tokens.css:110,134` but **currently has zero consumers anywhere in the codebase** — this story is the first to actually apply it, fulfilling epics.md AC3's "reused via DESIGN.md's floating-surface token" instruction literally); `color: #666` (version/date lines) → `var(--color-text-secondary)`; `color: #b00020` (install error) → `var(--color-accent-destructive)`; the focus-visible outline's `#396cd8` → a current token (this is the exact legacy blue Story 7.4 already flagged and removed from `AppSidebar.vue`; don't reintroduce it here). `CommandPalette.vue` is a second pre-tokens floating surface with the same hardcoded `border-radius: 8px` — **out of scope for this story**, only `UpdateDialog.vue` is named by the AC; don't scope-creep into fixing it too.
  - [x] Rewrite `UpdateDialog.spec.ts`: every existing test currently mounts `UpdateDialog` standalone and relies on its own `onMounted` check to populate `pendingUpdate` and auto-show. With Task 3's externally-controlled state, tests instead need to drive the shared module's state (open the dialog, set a pending update) before assertions — update the mocking strategy accordingly (mock the shared module from Task 3 the same way `checkForUpdate`/`installUpdate` are mocked today via `vi.mock("./updateCheck", ...)`). Preserve every existing behavioral assertion (install flow, dismiss flow, Escape handling, in-flight-install guards, error display) — only the setup/trigger mechanism changes.

- [x] **Task 5: Add the passive dot to `AppSidebar.vue`'s Settings item** (AC: 1, 2, 7)
  - [x] Per AC3/AC7's amendment (see Dev Notes "Post-story-creation UX correction"), the dot is **purely visual** — no independent click handler, no button element, no separate tab stop. Add it as an `aria-hidden="true"` `<span>`, absolutely positioned over/beside the gear icon within a `position: relative` wrapper around the existing `<RouterLink to="/settings">` (`:166-176`) — matching the mockup's corner-badge placement (`mockups/step4.3-mockups-light.png`/`-dark.png`, Screen 01). The `RouterLink`'s own click/navigation behavior is completely unchanged by this story.
  - [x] `v-if` the span entirely on Task 1's severity being non-`"none"` — no dot renders in the "up to date" state (matches Screen 01's own legend: "Up to date — No update available" has no dot shown). Real DOM removal (`v-if`, not `display:none`/`visibility:hidden`), per 7.5/7.6's established convention for anything that shouldn't be present when inactive.
  - [x] Background color: `var(--color-accent-signature)` for `"routine"`, `var(--color-accent-destructive)` for `"security"` — per DESIGN.md's explicit, deliberate exception permitting red for this one non-destructive "pay attention" case (DESIGN.md:194). No DESIGN.md token gives an exact pixel size for the dot itself ("Notification dot" is specified as color/role only, DESIGN.md:184) — pick a small size consistent with existing corner-badge conventions (~6–8px).
  - [x] Accessible name (AC7): append a second, visually-hidden `<span>` inside the `RouterLink` (alongside the existing `settings.sidebarCollapsed`-gated "Settings" label, `:175`) reading `", Update available"` or `", Security update available"` when a severity is present — so the link's *computed* accessible name becomes "Settings, Update available" in both expanded and collapsed sidebar states (the label span already handles the collapsed/expanded visibility toggle; the new suffix span should always stay in the accessibility tree regardless of collapse state, only its presence is gated by severity, mirroring how the visible dot is presence-gated). This satisfies AC7 without introducing a second focusable element.
  - [x] **Collapsed-sidebar case — explicit, not assumed.** The dot anchors to the gear icon, which stays visible when `settings.sidebarCollapsed` is `true` (only the text label hides, `:175`'s `visually-hidden` binding) — so the dot's visual position and the accessible-name suffix above should both continue working unchanged in collapsed mode with zero extra conditional logic. Verify this directly rather than assuming it: mount with `sidebarCollapsed: true` and confirm the dot still renders and the link's accessible name still includes the update state.
  - [x] Update `AppSidebar.spec.ts`: this file already mounts with a real Pinia instance and router (`createAppRouter(pinia)` pattern used throughout, not `createTestingPinia`/stubs) — follow that same convention for new tests. Add tests for: no dot when no update is pending; orange dot when routine; red dot when security-marked; the Settings link's accessible name includes "Update available"/"Security update available" when relevant, in both expanded and collapsed sidebar state; clicking the Settings link still navigates to `/settings` exactly as before (regression guard — this must **not** open the dialog directly anymore).

- [x] **Task 6: Add the update-available control to `SettingsView.vue`'s Privacy section** (AC: 3)
  - [x] In the existing Privacy `.settings-section` (`SettingsView.vue:188-196`, currently just a heading + one paragraph), add a conditional block rendered only when Task 1's severity is non-`"none"` — consistent with this section's neighbors: 7.6's Data section already gates its entries list behind a disclosure, so a "nothing extra shown unless relevant" pattern is already established here, not new.
  - [x] Show: a short heading/label reflecting the state ("Update available" / "Security update available", matching AC7's exact strings — reuse Task 1/3's shared severity read, don't hand-roll a second copy of this logic), the update's version, and its notes text (`pendingUpdate.body`, Task 1's marker-stripped version if that call was made) — a shorter treatment than the full dialog, enough to inform the click.
  - [x] A button (e.g. "View update…") that calls the Task 3 shared module's `openDialog()` — this is the only place in the app, post-amendment, that opens the dialog. Follow this file's existing button conventions (`.reset-button`/`.clear-all-button` styling patterns, `SettingsView.vue:252-289`) for a non-destructive, non-primary action — this is neither a delete/erase action (no red) nor the page's single most-emphasized action, so the existing default/outlined `.reset-button`-style treatment fits better than inventing a third button variant.
  - [x] Update `SettingsView.spec.ts` (uses real Pinia + stubbed/real settings store per its existing convention — check the file directly for which before assuming): add tests for the block being absent when no update is pending, present with the correct copy for both severities, and the button opening the dialog (assert via however Task 3's shared state is observed/mocked, matching this file's existing assertion style for other action buttons).

- [x] **Task 7: Document the `[security]` marker convention in `docs/release-checklist.md`** (AC: 2)
  - [x] `docs/release-checklist.md` is this project's durable, human-followed release procedure (Story 5.3's precedent) — it already documents version-bump discipline and the pre-tag `nettop` privacy verification, but says nothing about how a release gets flagged security-urgent. PRD FR31 explicitly names this as an open item this story is meant to resolve ("...the exact `notes` marker syntax, and a release-checklist documentation step (Story 5.3 precedent) remain open, to be resolved when this is implemented").
  - [x] Add a short new section (e.g. "Tagging a security release") documenting: the convention is a **manual, human judgment call** made when cutting the release — there is no automated CVE/vulnerability scan wired into this pipeline, and building one is out of scope here; whoever cuts the release prefixes the GitHub Release's notes (which flow through to `latest.json`'s `notes` field, and from there to the app's `Update.body`) with the exact marker Task 1 implements (recommended: a leading, case-insensitive `[security]` tag); state plainly what happens if it's omitted by mistake (the release is treated as routine — no automatic fallback detection exists, so getting this right is on the person cutting the release, same trust model as the rest of this manual checklist).
  - [x] Cross-reference this story (`7-7-the-update-signal-becomes-a-passive-escalation-aware-dot.md`) as the change's origin, matching this document's own existing citation convention (its footer already cites Story 5.3 as its origin).

- [x] **Task 8: Document the AD-10 extension** (AC: 6)
  - [x] In `ARCHITECTURE-SPINE.md`'s AD-10 section (`:99-103`), add the new `shell.updateSignal.*` namespace to the existing "Keys are namespaced `shell.*` / `<tool-id>.*`" rule text — either inline or as a short amendment note (matching the dated-amendment convention already used elsewhere in this doc, e.g. AD-11's revision note at `:109`).
  - [x] `ARCHITECTURE.md` (confirmed by grep) doesn't independently enumerate persisted keys — it only summarizes AD-10 at a high level ("owns the small amount of state that genuinely needs to live across tools... in two Pinia stores, nothing more"). No changes needed there; `ARCHITECTURE-SPINE.md` is the sole authoritative place this AC's documentation gap lives.

- [x] **Task 9: Full regression + manual verification** (AC: all)
  - [x] `pnpm vitest run` full suite green (baseline: 316/316 from Story 7.6 — expect growth from Tasks 1, 2, 4, 5, 6's new tests). `eslint --max-warnings 0` clean. `vue-tsc --noEmit && vite build` clean. Confirm `git status` shows zero `src-tauri/` changes — every prior Epic 7 story has been frontend-only, and nothing in this story's scope touches Rust.
  - [x] Manual verification against a running `pnpm tauri dev` server, mirroring Stories 7.4–7.6's precedent (opened live in Chrome via browser automation): confirm the dot appears/doesn't appear correctly on the Settings icon in **both expanded and collapsed sidebar states**, both colors render with correct contrast in both themes, clicking the Settings icon navigates normally (does *not* pop the dialog), the Privacy section's new block shows the pending update and its button opens the dialog, "Not Now" closes it without re-showing on a simulated re-check. **Expect the same no-real-Tauri-IPC-bridge limitation Stories 7.4–7.6 already hit** — `checkForUpdate()` itself calls into the real `@tauri-apps/plugin-updater`, which has no bridge in a plain browser tab, so a real non-null update can't be produced live; that path is what Task 1/4/5/6's unit tests cover instead, and installing an update specifically requires the real packaged app — document this as deferred-to-the-developer in Completion Notes, not silently skipped.

### Review Findings

- [x] [Review][Patch] Stale `Update` resource reused after dismiss → reopen → install [src/shell/UpdateDialog.vue:29-49, src/shell/updateSignal.ts:24-26] — `onDismiss()` calls `update?.close()` but never clears `pendingUpdate`, so the Settings banner's "View update…" button can reopen the dialog on the same, already-closed `Update` object; clicking "Install & Restart" then calls `downloadAndInstall()` on a closed resource. High severity: this reopen path is new in this story (AC3) and the only recovery is a full app restart. Cross-confirmed independently by both the Blind Hunter and Edge Case Hunter layers. **Fixed**: removed the premature `update?.close()` call from `onDismiss()`; added a regression test.
- [x] [Review][Patch] Stale `installError` resurfaces on reopen [src/shell/UpdateDialog.vue:10,61-69] — `installError` is only cleared at the top of `onInstall()`, never on dismiss or on `dialogOpen` transitioning true, so reopening the dialog after a previous failed-then-dismissed install attempt shows a stale error message before the user has done anything in the new session. **Fixed**: `installError` is now cleared in the `dialogOpen` watcher when it transitions to `true`; added a regression test.
- [x] [Review][Patch] Update banner uses `<p>` instead of a heading [src/shell/SettingsView.vue:87-89] — `.update-banner-heading` is a `<p>`, unlike Appearance/Data/Privacy's `<h2 class="section-heading">`; a screen-reader user navigating by heading skips the banner entirely, despite its own CSS comment calling it more important than every section below it and this story's AC7 being specifically about assistive-tech legibility of the update signal. **Fixed**: changed the element to `<h2>` (class/styling unchanged).
- [x] [Review][Patch] AC3 text no longer matches the implementation [_bmad-output/implementation-artifacts/7-7-...md AC3; src/shell/SettingsView.vue:83-107] — AC3 still reads "its Privacy section shows the pending update," but the documented "Post-implementation UX correction" deliberately moved the banner to the top of the page, above all sections. Unlike the AC3/AC7 dot-click amendment earlier in the same story, this second correction was never reflected back into AC3's wording with a dated note (the project's own established convention, used twice already in this story). **Fixed**: AC3 amended in place, both here and in `epics.md`, with a second dated note following the same convention.
- [x] [Review][Patch] Severity label string duplicated [src/shell/AppSidebar.vue:100-102, src/shell/SettingsView.vue:10-12] — the `updateSeverity.value === "security" ? "Security update available" : "Update available"` ternary is hand-rolled identically in both files instead of being exported once from `updateCheck.ts`, against Task 6's explicit instruction to reuse the shared severity read rather than hand-roll a second copy. **Fixed**: extracted `getUpdateSeverityLabel()` to `updateCheck.ts`; both files now import and use it.
- [x] [Review][Patch] `pendingUpdate`/`dialogOpen` exported as directly-mutable refs [src/shell/updateSignal.ts:13-14] — any future importer can set `pendingUpdate.value`/`dialogOpen.value` directly, bypassing `runCheck()`/`openDialog()`/`closeDialog()`. No current consumer does this, but nothing in the type system prevents it. Wrap the exports in Vue's `readonly()`. **Fixed**: exports now wrapped in `shallowReadonly()` (not `readonly()` — see code comment on why: `readonly()` would deep-proxy the returned `Update` instance, breaking reference identity and risking real Tauri methods that rely on private class fields). Added `__setPendingUpdateForTest()`/`__setDialogOpenForTest()` test-only setters; updated all 4 spec files' direct-assignment test setup to use them.
- [x] [Review][Patch] Inconsistent unsafe casts in new tests [src/shell/updateCheck.spec.ts:42,56,60,66,72,75,81] — uses bare `as never` for `getUpdateSeverity`/`installUpdate` fixtures, while `UpdateDialog.spec.ts`'s fixtures (same story) use the more conventional `as unknown as Update` double-cast for the same purpose. No functional difference; align the style. **Fixed**: all 7 casts changed to `as unknown as Update`.
- [x] [Review][Defer] New orange `AppButton` focus ring vs. `SettingsView.vue`'s pre-existing hardcoded blue focus-visible rule [src/shell/SettingsView.vue:352-357] — deferred, pre-existing. `SettingsView.vue`'s generic `input/select/button:focus-visible { outline: 2px solid #396cd8; }` rule predates this diff and wasn't touched by it, but this story's new `AppButton` (which correctly uses the `--color-accent-signature` token) now sits on the same page as plain `<button>`s still using the legacy hardcoded blue, making the inconsistency newly visible.

**Dismissed as noise (10):** Dev Agent Record's self-reported test-count arithmetic not reconciling (verified `pnpm vitest run` is actually 361/361 green — a documentation typo with zero functional effect); `AppButton`'s hardcoded `color: #ffffff` on primary/destructive (matches a documented, deliberate DESIGN.md trade-off already used identically in `SettingsView.vue`'s pre-existing `.clear-all-button`, and no `-on` token exists for those variants); `AppButton`'s orange `:focus-visible` outline "violating the orange budget-of-one rule" (false — `AppSidebar.vue`'s own existing comment confirms a focus ring is the licensed second use of orange, established since Story 7.4); the `[security]` marker regex's strict start-anchor and lack of a checklist verification step (fully documented, deliberate trust-model choice — see `docs/release-checklist.md`'s "Tagging a security release" section); two "developer-acknowledged defects" not recorded in `deferred-work.md` (misreads this project's process — that file is populated *by* code review, i.e. this review, not proactively during dev; the cited dot-off-center note is itself superseded by the later positioning fix already in the code); `onDismiss` lacking a re-entrancy guard against a rapid double-click (real in theory, practically unreachable via genuine user interaction given Vue's synchronous ref flip, and already caught/idempotent if it somehow occurred); `runCheck()` having no guard against a future interval mechanism (speculative against work Dev Notes explicitly rule out of scope for this story); `AppSidebar.spec.ts` setting `sidebarCollapsed` via direct ref assignment instead of the store action (idiomatic, pre-existing Pinia test convention in the same file, doesn't affect what's under test); `AppButton.spec.ts`'s "forwards click events" test (verifies Vue's attrs-fallthrough wiring, not fully tautological).

## Dev Notes

### Post-implementation UX correction (2026-08-18, live review)

Same shape as Story 7.6's own precedent (see that story's Dev Notes) — caught by the
developer live-testing the implementation, after Task 9's manual verification had
already passed, not by any AC or design doc. Three findings, all fixed in this same
session:

1. **Dot position was wrong.** The original placement used a fixed `top`/`left` offset
   on a wrapper div around the whole Settings row, which didn't actually track the gear
   icon — the dot floated near the row's top-left, not touching the icon at all.
   **Fixed**: the dot is now anchored to a small `position: relative` wrapper around the
   `PhGear` icon itself (`.settings-icon-wrap`), positioned `top: -2px; right: -2px`
   relative to *that*, so it sits exactly at the icon's own top-right corner in both
   expanded and collapsed sidebar states — a real corner badge, not a guessed offset. This
   also let the extra `.settings-link-wrapper` div (added only to give the *row* a
   `position: relative` context) be removed entirely, since the badge no longer needs to
   anchor to the row — a net simplification, and it undoes the one pre-existing
   `AppSidebar.spec.ts` regression-test change this story had made necessary.
2. **Update banner buried at the bottom of Settings.** The original placement (inside
   the Privacy section, itself the last of three sections) put it at the literal bottom
   of the page — a pending update, especially a security one, has to be scrolled past
   two unrelated sections to find. **Fixed**: the banner now renders directly under the
   `<h1>Settings</h1>` heading, above Appearance/Data/Privacy, gated on the same
   `updateSeverity !== 'none'` condition as before (nothing shows in the "up to date"
   state). The Privacy section's own disclosure paragraph is unchanged; the banner isn't
   duplicated there.
3. **Dialog buttons were unstyled plain `<button>`s.** `UpdateDialog.vue`'s "Not Now"/
   "Install & Restart" and the new Settings banner's "View update…" button all rendered
   as bare browser-default buttons — no codebase-wide button component existed to give
   them the app's actual button styling. **Fixed**: added `src/components/AppButton.vue`,
   a new top-level `components/` folder (sibling to `shell/`/`stores/`/`tools/`) for
   reusable UI primitives, implementing exactly the three button roles DESIGN.md's own
   `components` block names — `button-primary` (orange fill, DESIGN.md's one-per-screen
   main CTA), `button-default` (the workhorse — DESIGN.md's own words, "most buttons...
   secondary actions"), `button-destructive` (red fill, reserved for high-consequence
   actions) — no fourth, invented variant. Wired into this story's own three buttons:
   `UpdateDialog.vue`'s "Install & Restart" (primary) and "Not Now" (default), and
   `SettingsView.vue`'s "View update…" (default). **Deliberately not** retrofitted onto
   this session's pre-existing buttons (`SettingsView.vue`'s `.reset-button`/
   `.clear-all-button`, `AppSidebar.vue`'s icon-only `.collapse-toggle`/`.pin-toggle`, or
   any of the seven tool views' own buttons) — that's a much larger, cross-cutting change
   touching files well outside this story's scope, and per this project's own governance
   principle (`CLAUDE.md`, "new shared infrastructure doesn't inherit... automatically"),
   a change of that size gets presented as an explicit option, not decided silently. Left
   open for the developer to request as a separate, deliberate follow-up.

### Post-story-creation UX correction (2026-08-18)

Unlike Story 7.6's correction (caught by the developer during live implementation review), this one was caught even earlier — during story creation, before any code was written — via direct developer feedback on the draft story. The original epics.md AC3/AC7 text (and the `DESIGN.md`/`EXPERIENCE.md` Step-4.3 spec it was drawn from) made the notification dot itself an independently clickable, keyboard-focusable control, separate from the Settings icon it's anchored to. The developer flagged this directly: no reviewed application requires clicking a small notification-dot badge rather than the icon/nav item it decorates — the badge is normally just an indicator, and the icon itself remains the click target. This gets materially worse in the sidebar's *collapsed* mode, where the whole icon shrinks to its smallest size and a corner-badge sub-target becomes even harder to hit precisely.

**Resolved**: the dot is now a pure visual + accessible-name indicator (`aria-hidden` mark, no independent interactivity). The Settings sidebar item's existing click/navigate-to-`/settings` behavior is completely unchanged. The actionable control — open the update-consent dialog — moves onto the Settings page itself, in the existing Privacy section (already the thematic home of the update-check disclosure). This is a two-step path to installing (Settings, then the button) instead of one, but it trades a single unfamiliar micro-interaction for two familiar, full-sized click targets — consistent with how notification badges behave in other desktop software the developer has used.

`epics.md`'s Story 7.7 AC3 and AC7 were amended in place (dated notes, not silent rewrites — the same convention Stories 7.1 and 7.6 established for locked-doc corrections). `DESIGN.md`'s Update-signal component entry (:179) and `EXPERIENCE.md`'s corresponding IA (:60), State Patterns (:74), and Flow 3 (:132-141) rows **were deliberately left unamended** — they still describe the original dot-is-clickable interaction. This mirrors Story 7.6's own review finding, which treated an identical co-equal-doc staleness gap (`EXPERIENCE.md`/`prd.md` describing a pre-correction IA after `epics.md` was amended) as a non-blocking, doc-sync-only gap, not a blocker — epics.md is this project's authoritative source for story implementation content, and the create-story workflow already treats it as such (PRD/architecture/UX docs are explicitly the *fallback*, per this skill's own Input Files table). If a future story touches the update-signal again, re-sync those two docs then; don't let it silently block this one.

**Also raised and answered directly** (not requiring a design change, but worth recording so the dev agent doesn't re-litigate them):
- **Dismiss semantics.** `dismissedVersion` persists for AD-10/INV-3 transparency (visible and clearable in Settings like every other key) but doesn't gate any UI — the indicator stays visible regardless of dismissal history, exactly per `EXPERIENCE.md` Flow 3 step 3 ("declining does not clear the signal"). No "snooze for N days" behavior exists or was asked for anywhere in the source docs.
- **Release notes display.** Already implemented, pre-dating this story — `UpdateDialog.vue` already renders `pendingUpdate.body` (the release notes, sourced from `latest.json`'s `notes` field). This story's Task 4 preserves that, and Task 6 additionally surfaces a short excerpt in the new Settings banner. No new plumbing was needed for notes to reach the app; they already do.
- **`docs/release-checklist.md`.** Not originally scoped by epics.md's AC list, but PRD FR31 already flags a "release-checklist documentation step... remain[s] open, to be resolved when this is implemented" — Task 7 closes that gap directly, since this story is the one implementing FR31's marker convention.

### Architecture guardrails

- **AD-10 (exact text)**: *"the only persistence mechanism is `tauri-plugin-store` writing one `settings.json`. The frontend `settings` Pinia store is its single writer — Rust-side code never writes it. Keys are namespaced `shell.*` / `<tool-id>.*`. The Settings pane enumerates every persisted key with a one-action clear (PRD INV-3)."* [Source: ARCHITECTURE-SPINE.md:99-103] This story's `shell.updateSignal.dismissedVersion` is the first *nested* namespace under `shell.*` — confirm `resetKey()`'s string-match switch handles the extra dot with zero changes (it does; it matches the full key string, not a parsed segment).
- **AD-6 (exact text)**: *"no tool reads another tool's state. Cross-cutting state lives only in the Pinia stores `settings` and `registry`."* [Source: ARCHITECTURE-SPINE.md:75-79] Scoped to *tool* state — the dot/dialog coordination this story needs is shell-internal, not tool state, so a third Pinia store isn't the right reach; see Task 3.
- **AD-7 — Zero network surface except the updater.** This story adds no new network call; `checkForUpdate()` already is the sole disclosed carve-out (PRD INV-1/NFR1). Nothing here changes that surface, only when/how its result is surfaced in the UI.
- No schema-migration machinery is needed here either — `settings.ts` already reads every key as optional-with-default, and `shell.updateSignal.dismissedVersion` is a brand-new key, not a rename/retype of an existing one. [Source: ARCHITECTURE-SPINE.md:266]

### Tauri updater plugin — Context7-verified specifics

Per this project's global rule to verify library/framework specifics against current docs rather than trained knowledge, the updater plugin's manifest schema and JS binding were confirmed live via Context7 (`/tauri-apps/tauri-docs`) rather than assumed:

- **`latest.json`'s `notes` field is a plain optional free-text string** with no structured severity field — confirms PRD FR31/epics.md AC2's own claim ("Tauri's `latest.json` has no native severity field"). Schema: `{ "version", "notes", "pub_date", "platforms": { "<os>-<arch>": { "signature", "url" } } }`. Only `version` and `platforms.[target].{url,signature}` are required; `notes`/`pub_date` are optional.
- **The JS `Update` object's `body` field *is* the binding for `notes`** — Context7's own official example: `` console.log(`found update ${update.version} from ${update.date} with notes ${update.body}`) ``. This codebase's installed `@tauri-apps/plugin-updater@2.10.1` type declarations confirm the same shape (`node_modules/.pnpm/@tauri-apps+plugin-updater@2.10.1/.../index.d.ts:41,64`: `body?: string`). So Task 1's severity check must read `update.body`, not `update.rawJson.notes` (the latter also exists but `body` is the documented, typed accessor already used by this codebase's `UpdateDialog.vue`, `:112`).

### UX spec — why the ACs only have 2 states, not 3

`DESIGN.md`:179 and `EXPERIENCE.md`:74 both explicitly record that the update-signal was **"simplified at Step 4.3 from the originally-planned 3-tier escalation (quiet-day-1 → rising-weight days 2-4 → security-urgent) to 2 real states"** — after the screen mock showed the escalation tiers weren't visually distinguishable in practice. This is why epics.md's Story 7.7 title says "escalation-aware" but its actual ACs contain no day-based visual ramp: "escalation" here means the **routine → security color switch**, not a time-based intensity increase. PRD FR31's older text (*"escalates in visibility over a few days if ignored"*) predates this simplification and is now superseded by the locked UX spec — **do not implement day-based escalation**; epics.md's ACs (the authoritative source for story content per this project's workflow) already reflect the simplified, correct scope.

- **Two states, exact spec**: routine — `{colors.accent-signature}` dot (orange), accessible name "Update available", shown at **constant weight** from the moment available, no day-by-day ramp; security-urgent — `{colors.accent-destructive}` dot (red), accessible name "Security update available", via the `notes`/`body` marker convention Task 1 defines. [Source: DESIGN.md:179; EXPERIENCE.md:74]
- **Decline semantics, fully resolved** (closing PRD FR31's "decline semantics... remain open" note): declining does **not** clear or hide the signal — the dot simply remains visible, unchanged, on every subsequent launch, "exactly as if she'd never opened the dialog." [Source: EXPERIENCE.md:136, Flow 3 step 3] `dismissedVersion` is persisted for AD-10/INV-3 transparency (so it's visible/clearable in Settings like every other persisted key) — it does **not** gate any current UI behavior, since the dot's visibility already depends only on whether an update is pending, not on dismissal history. Don't invent extra "hide the dot for N days after dismissal" logic; nothing in any source document asks for it, and Flow 3 step 3 explicitly describes the opposite.
- **Anchor**: the Settings sidebar item (`AppSidebar.vue`'s `RouterLink to="/settings"`, `:166-176`) — this resolves `EXPERIENCE.md`'s own earlier "exact anchor TBD" note (`EXPERIENCE.md`:183, now superseded) and is where Story 7.4 explicitly deferred this story's anchor point (epics.md Story 7.4 AC: *"When Story 7.7 lands the update-signal, this story's markup provides the anchor point it attaches to"*).
- **Mockup**: `mockups/step4.3-mockups-light.png` / `-dark.png`, Screen 01 (top of the "Nav shell / Grid-home" mock) — shows a small dot at the Settings row plus a separate legend box documenting the three named states (up-to-date/no dot, routine/orange, security/darker — legend colors render close together at this resolution; trust the written token spec above, not pixel-picked color from the PNG). Screen 01 also shows the *unrelated* Clipboard-match callout (Story 7.8, still backlog) at the top of the sidebar — don't implement it here.
- **Floating surface**: the update-consent dialog reuses `{components.floating-surface}` (`DESIGN.md`:107,181) — border-vs-shadow rule: floating/temporary surfaces get a shadow (`--shadow-floating`), not a border, the inverse of persistent surfaces like cards. [Source: DESIGN.md:165,192]
- **Accessibility floor** (already established, nothing new to design): WCAG 2.1 AA, keyboard-drivable, visible focus at every tab stop, every control labeled. [Source: EXPERIENCE.md:86-94]

### Current state — read completely, not assumed

- **`src/shell/updateCheck.ts`** (16 lines) — `checkForUpdate()` thin-wraps `check()` from `@tauri-apps/plugin-updater`; `installUpdate()` wraps `downloadAndInstall()` + `relaunch()`. No severity logic exists yet — Task 1 adds it here.
- **`src/shell/UpdateDialog.vue`** (196 lines) — currently self-contained: `onMounted` triggers `runCheck()` (`:72-76`), which sets `pendingUpdate`, which the template's `v-if="pendingUpdate"` uses to auto-show the whole overlay+dialog. Focus-trap/restore (`:62-70`), Escape-guard-during-install (`:52-58`), and the disabled-while-installing button state (`:23-35`) are all already correct and must be preserved untouched — only the show/hide trigger and the `onDismiss` persistence step change. `<style>` block (`:141-196`) predates the Story 7.1 token rollout entirely — see Task 4's retrofit list.
- **`src/shell/UpdateDialog.spec.ts`** (198 lines) — every test mounts `UpdateDialog` standalone with `checkForUpdate`/`installUpdate` mocked via `vi.mock("./updateCheck", ...)` and relies on the component's own `onMounted` to populate state. This mocking shape needs to change once the dialog is externally controlled (Task 4's last subtask) — the *assertions* (what happens on each button/key interaction) should carry over unchanged.
- **`src/App.vue`** (52 lines) — mounts `AppSidebar`, `CommandPalette`, `DropZone`, `UpdateDialog` as flat siblings inside `.shell`, no shared state or props between them today. This is exactly why Task 3's shared-module approach is needed — there's no existing coordination channel to reuse.
- **`src/shell/AppSidebar.vue`** — the Settings link is `:166-176`: a `RouterLink to="/settings"` containing a `PhGear` icon (weight toggles bold when active) and a visually-hidden-when-collapsed "Settings" label. No wrapping element exists yet for a corner-badge dot — Task 5 needs to add one. Icons come from `@phosphor-icons/vue` resolved via `resolveIcon()` (`src/shell/icons.ts`) for registry-driven tool icons — the dot itself is **not** an icon (DESIGN.md:184: "pure mark, no text"), so it doesn't go through that resolver.
- **`src/stores/settings.ts`** (279 lines, post-7.6) — nine existing `shell.*` keys, all flat (no nested namespace yet). `DEFAULTS` constant (`:34-44`), `init()` (`:66-100`), `resetKey()`'s switch (`:200-235`), `clearAll()` (`:237-252`) are the four places Task 2's new key must touch, mirroring every existing key's shape exactly.
- **Full-codebase check**: `checkForUpdate` is called from exactly one place (`UpdateDialog.vue:17`, inside `onMounted`) — no polling/interval exists anywhere. Confirms Task 3's "don't add one" instruction.
- **`src/shell/SettingsView.vue`** (post-7.6, 298 lines) — three sections in order: Appearance (`:76-129`), Data (`:131-186`), Privacy (`:188-196`, currently just a heading + one static paragraph, no toggle or dynamic content — the mockup's "Check for updates" toggle shown in Screen 03 doesn't actually exist in this codebase; that mockup is illustrative-only per this project's own established caveat, EXPERIENCE.md:167). Task 6's new block goes inside this existing Privacy `<div class="settings-section">`. The file's existing `button:focus-visible` rule (`:291-296`) still hardcodes the legacy `#396cd8` blue — pre-existing, out of scope to fix here (Task 6's new button just inherits it, same as every other button in this file today; don't introduce a *new* hardcoded color, but fixing the existing global rule is not this story's job).
- **`docs/release-checklist.md`** (174 lines) — the durable, human-followed release procedure from Story 5.3. Documents version-bump discipline and the pre-tag `nettop` network-privacy verification in detail; currently has **no section at all** about security-flagging a release. Its footer (`:168-173`) already cites the story that introduced it as a convention — Task 7's addition should follow that same citation pattern.

### Previous story intelligence (Story 7.6)

- **Key-addition recipe** (already proven across 7.5 and 7.6, reused verbatim by Task 2): `ref` with a sensible default in `DEFAULTS` → read as `?? default` in `init()` → dedicated setter that bails if `!backingStore`, then `store.set()` + `store.save()` → reset inside both `resetKey()`'s switch and `clearAll()` → returned from the store.
- **`resetKey()` deletes, never rewrites** — `await backingStore?.delete(key)` then `.save()`, so a reset key actually disappears from `entries()` rather than reappearing at its default value. Task 2's new case should follow this exactly, not diverge into a rewrite-to-default shape.
- Reusable gotchas from 7.5/7.6's review rounds, applicable here too: prefer `v-if` (real DOM removal) over `display:none` for anything that shouldn't be present when inactive — directly relevant to Task 5's dot and Task 6's Settings banner, neither of which should linger in the DOM (even invisibly) when no update is pending; persisted-write handlers need `.catch()`/error logging, not silent failure; never use `--color-accent-signature` (orange) for chrome outside its two licensed roles (active-nav tint, attention marks) — the update-signal dot is explicitly one of DESIGN.md's two named "attention mark" uses, so it's in-bounds, but don't reach for orange anywhere else in this story's new code.
- Scoped Conventional Commits confirmed across all 6 Epic 7 commits so far: `feat(story-7-N): <lowercase summary>`, squash-merged via PR. Every prior Epic 7 story was frontend-only (zero `src-tauri/` diffs) and updated `sprint-status.yaml` (and `deferred-work.md` where relevant) alongside code — this story is expected to follow the same shape.
- Story 7.6's post-implementation UX-correction precedent (developer live-review catching real gaps no AC/doc flagged) is directly relevant to this story's own open calls (marker-stripping in the dialog, exact dot sizing) — if live verification surfaces a similar real UX gap, the established process is: fix it, document the correction with a dated note, don't treat locked planning docs as unchangeable when a live-reviewed problem is real.

### Project Structure Notes

- Expected touched files: `src/shell/updateCheck.ts`, `src/shell/updateCheck.spec.ts`, `src/shell/UpdateDialog.vue`, `src/shell/UpdateDialog.spec.ts`, `src/shell/AppSidebar.vue`, `src/shell/AppSidebar.spec.ts`, `src/shell/SettingsView.vue`, `src/shell/SettingsView.spec.ts`, `src/stores/settings.ts`, `src/stores/settings.spec.ts`, `src/App.vue` (if the shared module needs to be instantiated/triggered there), `docs/release-checklist.md`, `_bmad-output/planning-artifacts/architecture/architecture-Umbra-2026-07-20/ARCHITECTURE-SPINE.md`. `epics.md` was already amended in place during story creation (Story 7.7's AC3/AC7) — no further changes expected there unless implementation surfaces a new gap.
- One new file expected: `src/shell/updateSignal.ts` (or equivalent name) for Task 3's shared cross-component state module, plus its own `.spec.ts` if it carries any non-trivial logic beyond plain ref plumbing.
- No `src-tauri/` involvement, no new dependencies — `@tauri-apps/plugin-updater@2.10.1` is already installed and already exposes everything this story needs (`body`, `version`, `close()`, etc.).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-7.7] — this story's acceptance criteria; AC3/AC7 amended in place 2026-08-18 during story creation, see Dev Notes "Post-story-creation UX correction."
- [Source: _bmad-output/planning-artifacts/epics.md#Story-7.4 (lines 1139-1141)] — the anchor-point hand-off from Story 7.4 to this story.
- [Source: _bmad-output/planning-artifacts/architecture/architecture-Umbra-2026-07-20/ARCHITECTURE-SPINE.md AD-6 (:75-79), AD-7 (:81-), AD-10 (:99-103), AD-11's amendment convention (:109), Deferred (:266)]
- [Source: _bmad-output/planning-artifacts/prds/prd-Umbra-2026-07-19/prd.md FR31 (:101), INV-1 (:33), NFR1 (:115), NFR5 (:119)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-umbra-2026-08-15/DESIGN.md Components — Update-signal (:179), Notification dot (:184), Do's-and-Don'ts red exception (:194), Floating surface (:107,181,165,192)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-umbra-2026-08-15/EXPERIENCE.md IA — Update-signal row (:60), State Patterns — Update-signal (:74), Update-check network failure (:77), Flow 3 (:132-141), Inspiration & Anti-patterns (:99-100), stale "exact anchor TBD" note (:183, superseded)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-umbra-2026-08-15/mockups/step4.3-mockups-light.png and -dark.png, Screen 01]
- [Source: _bmad-output/implementation-artifacts/7-6-settings-becomes-sectioned-with-per-item-reset-and-the-restore-default-flip.md — Dev Notes' key-addition recipe, review findings, Completion Notes]
- [Source: Context7 `/tauri-apps/tauri-docs` — Static JSON Update File Structure, JavaScript Update Check examples (`update.body` ↔ `notes` mapping), queried 2026-08-18]
- [Source: node_modules/.pnpm/@tauri-apps+plugin-updater@2.10.1/node_modules/@tauri-apps/plugin-updater/dist-js/index.d.ts — `Update`/`UpdateMetadata` shape confirmed against the installed package]
- [Source: src/shell/updateCheck.ts, updateCheck.spec.ts, UpdateDialog.vue, UpdateDialog.spec.ts, src/shell/AppSidebar.vue, AppSidebar.spec.ts, src/shell/SettingsView.vue, src/App.vue, src/stores/settings.ts, settings.spec.ts, src/styles/tokens.css — read in full or targeted-grepped for this story]
- [Source: docs/release-checklist.md — read in full for this story; Task 7's target document]

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- Post-implementation UX correction round: `pnpm vitest run` — 361/361 passing (+7 from the `AppButton.vue` component's own new spec file); `pnpm eslint --max-warnings 0 .` clean; `pnpm vue-tsc --noEmit` clean; `pnpm vite build` clean. Re-verified live in Chrome against the same running dev server: dot now sits touching the gear icon's top-right corner in both sidebar states, the update banner renders at the top of the Settings page above Appearance/Data/Privacy, and the dialog's "Not Now"/"Install & Restart" buttons render as `AppButton` (default/primary respectively) instead of plain unstyled `<button>`s.
- `pnpm vitest run` — 354/354 passing (baseline 316/316 from Story 7.6 + 38 new: 12 `updateCheck.spec.ts`, 6 `settings.spec.ts`, 8 `updateSignal.spec.ts` net, 4 net in `UpdateDialog.spec.ts` [11 rewritten + new marker/persistence tests, 2 removed], 5 net in `AppSidebar.spec.ts`, 4 net in `SettingsView.spec.ts`).
- `pnpm eslint --max-warnings 0 .` — clean.
- `pnpm vue-tsc --noEmit` — clean after casting the shared `pendingUpdate` test fixtures to `Update` (`as unknown as Update`) instead of `never`, since a bare `never` cast triggered TS's control-flow narrowing of subsequent `pendingUpdate.value` reads to `never` within the same test.
- `pnpm vite build` — clean, production bundle unaffected in size/shape beyond this story's own files.
- `git diff --stat -- src-tauri/` — empty; confirmed frontend-only, matching every prior Epic 7 story.
- Manual verification: ran against the already-running `pnpm dev` (Vite) server rather than a fresh `pnpm tauri dev` launch — same working directory, same code, and the update-check path has no real Tauri IPC bridge in either case, so the two are equivalent for this story's scope. Confirmed live in Chrome: no dot in the "up to date" state; `checkForUpdate()` throws in-browser exactly as expected (no Tauri bridge) and is caught silently by `runCheck()`, no crash, matching AC5; clicking Settings navigates normally without opening the dialog. Since a real non-null `Update` can't be produced without the bridge, injected a fake `Update` directly into the running app via `await import('/src/shell/updateSignal.ts')` in the page console (Vite dedupes the ES module instance, so mutating the imported `pendingUpdate` ref reactively updates the already-mounted components) to visually confirm: orange dot for a routine update, red dot + "Security update available" copy for a `[security]`-marked one (marker correctly stripped from the displayed notes in both the dialog and the Settings banner), both dot colors read correctly against both the dark and light themes, the dot and accessible-name suffix both persist correctly in the collapsed sidebar state, the dialog opens with the token-retrofitted styling (surface/shadow/radius) intact, and "Not Now" closes the dialog while the dot and Settings banner both remain visible (AC4) — no re-nagging. Installing an update was not exercised live; it specifically requires the real packaged app and downloadAndInstall()/relaunch() plumbing, which Task 4's unit tests already cover.

### Completion Notes List

- **Post-implementation UX correction (live review)**: developer live-tested the implementation after Task 9 and found three real problems no AC/unit test caught — dot not actually touching the gear icon, the update banner buried at the bottom of the Settings page, and the dialog's buttons rendering as plain unstyled HTML `<button>`s. All three fixed in the same session; see Dev Notes "Post-implementation UX correction" for the full detail. Net effect: the dot now anchors to a small wrapper around the icon itself rather than a fixed offset on the row (also letting the earlier `.settings-link-wrapper` div be removed again); the update banner moved from inside Privacy (last section) to directly under the page's `<h1>`; and a new `src/components/AppButton.vue` — implementing DESIGN.md's three named button roles (primary/default/destructive), no invented fourth variant — now backs `UpdateDialog.vue`'s two buttons and `SettingsView.vue`'s "View update…" button. Retrofitting `AppButton` onto this session's *other*, pre-existing buttons (Settings' reset/clear-all buttons, the sidebar's icon toggles, the seven tool views) was explicitly left as an open follow-up rather than done silently — that's a much larger change outside this story's own scope.
- **Task 1 — marker-stripping decision**: chose to strip the `[security]` marker from displayed release notes (recommended option), via `stripSeverityMarker()` in `updateCheck.ts`, applied in both `UpdateDialog.vue` and `SettingsView.vue`'s banner. Matches this codebase's established principle (Story 7.6) of not leaking internal/implementation-facing markup into user-facing text.
- **Task 3 — shared-state architecture**: implemented as a plain module-level singleton, `src/shell/updateSignal.ts` (`shallowRef<Update | null>` for the checked update, `ref<boolean>` for dialog-open state, plus `runCheck()`/`openDialog()`/`closeDialog()`), not a third Pinia store — matches AD-6's two-store scope and the existing `icons.ts`/`clipboard.ts` shared-module precedent. `runCheck()`'s trigger moved to `App.vue`'s `onMounted`, preserving the original decoupling-from-`main.ts`'s-critical-path requirement.
- **Task 4 — dismiss no longer clears `pendingUpdate`**: this is a deliberate, necessary behavior change from the pre-story code, not an oversight. The old `onDismiss` set its local `pendingUpdate` to `null` on dismiss; since `pendingUpdate` is now shared state that the dot and Settings banner also read, doing the same would make "Not Now" hide the dot too — directly contradicting AC4 ("the dot remains visible on subsequent launches") and the Dev Notes' explicit decline-semantics call ("declining does not clear the signal"). `onDismiss` now only calls `closeDialog()` (hiding the modal) while leaving `pendingUpdate` untouched; `update.close()` (releasing the Tauri resource) is still called exactly as before. One residual edge case worth flagging: if a user dismisses and later reopens the dialog in the *same* session via the Settings banner (no in-session re-check exists to fetch a fresh `Update`), clicking "Install & Restart" would call `downloadAndInstall()` on an already-`close()`d resource. No source document specifies this edge, and the existing inline install-error handling would surface it as a retryable error rather than a crash — not fixed here since it's outside this story's explicit scope, but worth a look if it surfaces in practice.
- **Task 5 — dot position**: no exact pixel position is specified anywhere in DESIGN.md/EXPERIENCE.md beyond "small" and color/role. Positioned via a fixed `top`/`left` offset anchored to the wrapper rather than tracking the gear icon's exact center — reads as a clean corner badge in the expanded sidebar; in the collapsed sidebar (where the icon centers itself via `justify-content: center`) the dot sits slightly left of the icon's exact center rather than precisely over it, confirmed live in both states. Acceptable within the story's own stated slack ("pick a small size consistent with existing corner-badge conventions"); a future story can tighten this to a computed/centered position if it matters more than this pass's time budget allowed.
- **Task 5 — DOM structure change**: wrapping the Settings `RouterLink` in a new `.settings-link-wrapper` div (required for `position: relative` badge anchoring) changed the Settings link's parent from `nav` directly to the wrapper. Updated the one pre-existing `AppSidebar.spec.ts` regression test that asserted this ("keeps the Settings link outside the scrolling tool list, as a direct child of nav") to assert the new two-level structure instead, rather than leaving it silently broken.
- **Task 7 — incidental doc-staleness fix**: while editing `docs/release-checklist.md` for the new "Tagging a security release" section, found and fixed a stale reference in the existing "What 'zero outbound connections' actually means" section, which still described the pre-Story-7.7 code path (`UpdateDialog.vue`'s `onMounted` calling `checkForUpdate()` directly). Updated it to describe the current path (`App.vue`'s `onMounted` → `updateSignal.ts`'s `runCheck()` → `updateCheck.ts`'s `checkForUpdate()`) since this story is what made the old description inaccurate.
- **Deferred**: installing a real update end-to-end (the packaged-app-only path) was not exercised live, per Task 9's own scoping — covered by `UpdateDialog.spec.ts`'s unit tests instead.

### File List

- `src/shell/updateCheck.ts` — added `getUpdateSeverity()`, `stripSeverityMarker()`.
- `src/shell/updateCheck.spec.ts` — tests for the above.
- `src/shell/updateSignal.ts` (new) — shared cross-component update-check state module (Task 3).
- `src/shell/updateSignal.spec.ts` (new) — tests for `runCheck()`/`openDialog()`/`closeDialog()`.
- `src/shell/UpdateDialog.vue` — externally controlled via `updateSignal.ts`'s `dialogOpen`/`pendingUpdate`; dismiss now persists via the settings store; `<style>` retrofitted to design tokens; buttons now use `AppButton` (post-implementation UX correction).
- `src/shell/UpdateDialog.spec.ts` — rewritten to drive the shared module's state instead of mocking `checkForUpdate`'s own `onMounted` trigger; added marker-stripping and dismissal-persistence tests.
- `src/shell/AppSidebar.vue` — added the passive update dot + accessible-name suffix on the Settings nav item, anchored to the gear icon itself (post-implementation UX correction).
- `src/shell/AppSidebar.spec.ts` — added dot/accessible-name tests. The Settings-link-parent regression test was updated then reverted back to its original assertion once the post-implementation correction removed the wrapper div it had required.
- `src/shell/SettingsView.vue` — added the update-available banner, positioned at the top of the page above every section (post-implementation UX correction moved it out of Privacy); its button now uses `AppButton`.
- `src/shell/SettingsView.spec.ts` — added tests for the banner.
- `src/stores/settings.ts` — added `shell.updateSignal.dismissedVersion` (first nested `shell.*` namespace).
- `src/stores/settings.spec.ts` — added tests for the new key.
- `src/App.vue` — triggers `updateSignal.ts`'s `runCheck()` once on mount (moved from `UpdateDialog.vue`).
- `src/components/AppButton.vue` (new) — shared button component implementing DESIGN.md's primary/default/destructive roles (post-implementation UX correction).
- `src/components/AppButton.spec.ts` (new) — tests for `AppButton`.
- `docs/release-checklist.md` — added "Tagging a security release" section; fixed a stale code-path reference.
- `_bmad-output/planning-artifacts/architecture/architecture-Umbra-2026-07-20/ARCHITECTURE-SPINE.md` — documented AD-10's `shell.updateSignal.*` nested-namespace amendment.
