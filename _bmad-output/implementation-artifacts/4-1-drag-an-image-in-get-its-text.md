---
baseline_commit: 3ba7886
---

# Story 4.1: Drag an image in, get its text

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a privacy-conscious developer,
I want to drop an image into the Bucket and get its text extracted entirely on-device,
so that screenshots of errors and documents become copyable text without touching any cloud OCR.

## Acceptance Criteria

1. **Given** the Bucket tool is active, **when** I drop a PNG, JPEG, or WebP image, **then** its text is extracted by the local ONNX OCR engine and displayed (FR23), **and** the file arrives as a path via the shell drop service — `src-tauri` reads the bytes and hands them to core (AD-14, AD-15).
2. **Given** the OCR implementation, **when** inspected, **then** `umbra-core` defines the OCR trait (image bytes in → recognized text + confidence out) and `oar-ocr` is the adapter behind it — callers, commands, and UI depend on the trait only (AD-8), **and** the PaddleOCR ONNX models are bundled as app resources with `oar-ocr`'s auto-download not enabled (AD-7), **and** the exact model files are chosen and documented in this story, closing the spine's deferred item.
3. **Given** a cold app launch, **when** launch completes, **then** the OCR engine has not initialized — it initializes lazily behind a thread-safe once-cell on first use, and cold launch stays under 2 seconds (AD-4, AD-16, NFR2).
4. **Given** OCR inference is running, **when** the UI is used, **then** inference executes async on the blocking thread pool and the UI stays responsive (AD-4).

**Explicitly out of scope for this story** (do not build ahead of these — later stories own them, per epics.md's own story split):
- Editable result text / one-click copy — FR24, Story 4.2's AC.
- Clipboard-paste (⌘V) input — Story 4.2's AC.
- Explicit "no text found" / corrupt-image messaging beyond a plain structured `ToolError` — FR26, Story 4.3's AC.
- The formal NFR1 network-monitor tour proving zero network activity — Story 4.3's AC (this story still must not enable any auto-download path; that's AC2 above, not the same thing as the release-blocking proof).
- Racing-first-use-initialization test coverage — Story 4.3's AC (this story must still *build* the once-cell guard correctly; 4.3 adds the concurrency test for it).

## Tasks / Subtasks

- [ ] **Task 0: Branch from the correct base — read this before creating a branch (AC: all)**
  - [ ] This story's `baseline_commit` (`3ba7886`) is `main`'s tip as of story creation — `docs/epic-3-retrospective` merged to `main` (#35, squash) before this story was committed, so no stacking is needed. `3ba7886` contains the AD-16 amendment to `ARCHITECTURE-SPINE.md` that Task 7 below depends on directly (`registry.getLatestWinsRunner(toolId)` as the named pattern for the Bucket tool).
  - [ ] Branch `feat/story-4-1-drag-an-image-in-get-its-text` from `main`. Confirm the AD-16 amendment (`ARCHITECTURE-SPINE.md`'s "Amended 2026-08-04, Epic 3 retrospective" section) is present before proceeding.
  - [ ] This is Epic 4's first story: confirm `sprint-status.yaml`'s `epic-4` entry moved from `backlog` to `in-progress` (done automatically by `bmad-create-story`, already reflected in this story's own creation).

- [ ] **Task 1: Re-verify `oar-ocr`'s actual current API before writing any code (AC: 2) — do not trust this story's research as gospel**
  - [ ] The architecture spine pins `oar-ocr` at "0.8.x (confirmed 2026-07-20)" with an explicit warning: *"Pre-1.0: re-verify exact API at Epic 4 start regardless, since minors can still break."* That warning fired for real: `crates.io` shows `oar-ocr` **0.9.0**, published 2026-08-03 — one day before this story was drafted. This story's Dev Notes below describe the 0.9.0 API as researched via `docs.rs`/GitHub (summarized through an intermediary tool, not read verbatim) — treat it as a strong starting hypothesis, not verified fact. Add `oar-ocr = "0.9"` to `crates/umbra-core/Cargo.toml`, run `cargo doc --no-deps -p oar-ocr --open` (or read the vendored source under `~/.cargo/registry/src/.../oar-ocr-0.9.0/`) and confirm directly, the same way Epic 2/3's retros credit this project's dependency-verification discipline for catching stale/wrong secondhand docs twice already (`croner`, `oar-ocr`'s own earlier 0.2.x-vs-0.8.x mix-up in the spine's own history).
  - [ ] Specifically confirm, against the real source, before building the adapter: (a) the exact builder/constructor signature (`OAROCRBuilder::new(det_path, rec_path, dict_path)` per this story's research), (b) the exact prediction method name and signature — does `predict()` take `&self` or `&mut self`? This determines whether the once-cell in Task 5 can hold `OAROCR` directly behind a bare lock-free read, or needs a `Mutex`/interior-mutability wrapper for concurrent calls, (c) the exact result shape — this story's research found `text_regions` with a `.text_with_confidence()` accessor returning `(String, f32)` per region; confirm this exists as described, (d) whether `predict()` takes a decoded image type (e.g. `image::DynamicImage`) or raw bytes — AD-8's contract is "image bytes in," so if `oar-ocr` wants a decoded image, the adapter in `umbra-core::ocr` must decode bytes itself (likely via the `image` crate, which will then also need adding to `crates/umbra-core/Cargo.toml`).
  - [ ] If the `image` crate is needed for bytes→image decoding: WebP decoding ships in its **default** feature set (`default-formats` pulls in `webp` among 14 other formats, confirmed via `docs.rs`'s feature-flag listing) — plain `image = "0.25"` with default features on already covers PNG/JPEG/WebP (FR23's three required formats). Do not add `default-features = false` to trim it — that's a real pattern seen elsewhere in the Rust ecosystem for library crates minimizing their own dependents' build size, but it would silently drop WebP support here, and this app is the end consumer, not a library other crates depend on.
  - [ ] Confirm the `auto-download` Cargo feature is **not enabled by default** (this story's research: `cargo add oar-ocr --features cuda,auto-download` is shown as the opt-in example, implying default-off) and is not pulled in transitively by any feature this story does enable. Do **not** add `auto-download` to the `oar-ocr` dependency line in `Cargo.toml` — AC2/AD-7 requires it stay off. This is a separate concern from the `ort` crate's own default of downloading the ONNX Runtime *binary* at build time (see AD-11 in Dev Notes) — do not conflate the two "downloads."

- [ ] **Task 2: Choose and document the exact OCR model files (AC: 2) — closes the spine's Deferred item**
  - [ ] This story's research points to `oar-ocr`'s own quick-start example using PP-OCRv6 **tiny** detection + recognition models, published by PaddlePaddle on Hugging Face: `PaddlePaddle/PP-OCRv6_tiny_det_onnx` and (by naming convention, verify it exists) `PaddlePaddle/PP-OCRv6_tiny_rec_onnx`, plus a character dictionary file. License: Apache-2.0 (confirmed on the det model's Hugging Face card) — compatible with bundling into an All-Rights-Reserved app per the project's dependency-hygiene convention (permissive licenses are fine). The PP-OCRv6 tiny tier is multi-language (English included); FR25 only requires the *app* to support English in v1, it does not forbid an underlying multi-lingual-capable model — no AD-13 conflict.
  - [ ] Download and verify both model files' SHA-256 (Hugging Face model cards publish these) before bundling — do not trust an unverified download for a dependency this security-sensitive (privacy tool, bundled ML weights).
  - [ ] Verify FR24's "~3s typical extraction on Apple Silicon" bar is realistic for the tiny tier during Task 9's manual verification. If tiny proves inadequate on real screenshots, the medium tier (`PP-OCRv6_medium_det_onnx`/`_rec_onnx`, larger/slower/more accurate) is the documented fallback — record whichever tier you actually ship and why in this story's Completion Notes; don't leave the choice implicit.
  - [ ] Update `ARCHITECTURE-SPINE.md`: bump the Stack table's `oar-ocr` row from "0.8.x" to the version actually pinned in `Cargo.lock`, and replace the Deferred section's `"Exact OCR ONNX model files. Chosen and documented at Epic 4 Story 4.1."` bullet with the actual filenames/source/license chosen — this is how prior stories (1.8, 1.9) closed their own spine Deferred items; follow that precedent, don't leave the bullet dangling after this story claims to close it.

- [ ] **Task 3: Define the OCR trait in `umbra-core` (AC: 2, AD-1, AD-8)**
  - [ ] New module `crates/umbra-core/src/ocr.rs` — named after the *capability* (OCR), not the *tool* (Bucket), matching the spine's own Structural Seed and leaving room for a future structured-extraction feature (FR29/Epic 6) to reuse or extend the same trait shape, per the spine's own Deferred note on this exact question.
  - [ ] Trait shape, per AD-8: image bytes in, `{ text: String, confidence: Option<f32> }`-style output out (exact field names your call — this is new code, no prior precedent to match), honest empty/failure states — no `unwrap`/`expect`, errors as `ToolError` per AD-3's contract (reuse `crates/umbra-core/src/error.rs`'s `ToolError`, don't invent a parallel error type).
  - [ ] The `oar-ocr` adapter (behind the trait, per AD-8 — commands/UI never import `oar-ocr` directly) concatenates all detected `text_regions` into one string (reading order as the model returns them — no reordering logic needed for v1) and aggregates a confidence value (e.g. mean across regions; `None`/no value when zero regions are found, which is the "no text" signal Story 4.3 will build explicit UX around — this story just needs the trait to surface that state truthfully, not to render it specially).
  - [ ] Wire the two bundled model paths + dictionary path into the adapter's construction — the adapter needs *resolved absolute paths* at runtime (see Task 4), so its constructor should take paths as arguments rather than hardcoding them, keeping `umbra-core` filesystem-path-agnostic per AD-2/AD-15 (core still never *touches* the filesystem itself — decoding image bytes already in memory is not a filesystem access).

- [ ] **Task 4: Bundle the models as Tauri resources and resolve their path at runtime (AC: 2)**
  - [ ] Add the two `.onnx` files and the dictionary file under a new `src-tauri/resources/models/` directory (matches the spine's own Structural Seed: `resources/models/`).
  - [ ] Add `"resources": ["resources/models/"]` under `bundle` in `src-tauri/tauri.conf.json` — this copies the whole directory into `$RESOURCE/resources/models/` in both dev and packaged builds.
  - [ ] In the new command (Task 6), resolve the actual path with `handle.path().resolve("resources/models/<file>", tauri::path::BaseDirectory::Resource)?` (`handle: tauri::AppHandle`, or the command's own `app: tauri::AppHandle` parameter — Tauri injects this automatically for any command that declares it). This is a genuinely new pattern for this codebase: every command so far takes plain data arguments only, none has needed `AppHandle`. **No new capability/permission entry is needed** — this happens entirely in Rust-side command code, not via a webview-callable API, so `src-tauri/capabilities/default.json` is unaffected.

- [ ] **Task 5: Lazy, thread-safe, once-only initialization (AC: 3, AD-4, AD-16)**
  - [ ] Use `std::sync::OnceLock`, **not** `std::cell::OnceCell` — the OCR engine will be read from multiple concurrent async command invocations running on different blocking-thread-pool threads (AD-4), so it needs a `Sync` primitive. `std::cell::OnceCell` is not `Sync` and would not compile in that position; `std::sync::OnceLock` is stable since Rust 1.70, well under this project's 1.85 MSRV, so no new dependency is needed for this.
  - [ ] `OnceLock::get_or_try_init` (which would let the init closure return a `Result`) is **still unstable** as of this story (tracked under `once_cell_try`, requires a nightly feature flag) — do not reach for it. The straightforward stable pattern is `static OCR_ENGINE: OnceLock<Result<YourAdapterType, ToolError>> = OnceLock::new();` with `get_or_init(|| { /* build the adapter, resolving model paths */ })` — the `Result` (success or failure) is cached permanently for the process lifetime once computed, matching AD-16's stated v1 scope of "no progress events and no cancellation" (and, implicitly here, no retry-after-failure — acceptable since a first-use init failure almost certainly means a packaging/bundling defect, not a transient condition).
  - [ ] Confirm via Task 9's manual check that the engine genuinely does not construct at launch — e.g. add a temporary debug log inside the init closure during manual testing (removed before commit) and confirm it only fires on the first drop, never during `pnpm tauri dev` startup.

- [ ] **Task 6: The Tauri command (AC: 1, 4, AD-3, AD-4)**
  - [ ] New file `src-tauri/src/commands/bucket.rs` (tool prefix `bucket`, per AD-3's `<tool>_<verb>` naming and this tool's registry id — see Task 7's naming note). Command: `bucket_extract_text(path: String, app: tauri::AppHandle) -> Result<OcrOutcome, ToolError>` (name the output type to match whatever Task 3's trait actually returns).
  - [ ] Body: `tauri::async_runtime::spawn_blocking` (AD-4, same pattern as every other file-consuming command — `hash_compute_file` in `src-tauri/src/commands/hash.rs` is the closest direct precedent), reads the file via `crate::fs_helper::read_file_bytes` (existing shared helper, AD-15 — don't write a new file-read function), resolves the once-cell engine (Task 5), calls the trait method, maps errors to `ToolError` (reuse `map_join_error`'s pattern from `hash.rs`, but the internal error code should be `bucket-*`-prefixed, not copy-pasted `hash-*`).
  - [ ] Add an image-size guard before decode, mirroring `base64.rs`/`hash.rs`'s existing `MAX_INPUT_BYTES` (100 MB) CWE-400 convention — checked via `std::fs::metadata` before the file is read into memory, same ordering as `hash_compute_file`'s `check_file_size`. Reuse the exact same 100 MB constant unless you have a concrete reason (there's no NFR mandating a tighter image-specific cap) to pick a different one — if you do, say why in Completion Notes.
  - [ ] A non-image file dropped on the Bucket is **not** filtered by the shell (`src/shell/dropZone.ts`'s `routeDrop` doesn't inspect file type — confirmed by reading it directly; `acceptedMimeTypes` on registry entries is pre-existing dead configuration, already flagged in `deferred-work.md`'s Story 2.5 entry, not something this story should wire up). Let the Rust-side image decode step reject it naturally with a structured `ToolError` (e.g. `bucket-unsupported-format` or whatever the decode library's own error maps to) — this is acceptable per NFR4 ("errors always shown in-tool," not "the shell must pre-filter every possible bad input") and matches every other tool's error-surfacing pattern in this codebase.
  - [ ] Register `bucket_extract_text` in `src-tauri/src/lib.rs`'s `tauri::generate_handler![...]` list and its `use commands::bucket::bucket_extract_text;` import, alongside the existing six tool command modules.

- [ ] **Task 7: Register the Bucket tool and build its view (AC: 1)**
  - [ ] **The tool id is `"bucket"`, not `"ocr"`.** The PRD's own glossary defines "The Bucket" as the drop-zone tool *family* — OCR now (Epic 4), PDF/image operations later (Epic 6, Stories 6.1/6.2, which explicitly extend "the Bucket," not a new tool). Registering it as `"ocr"` here would force a rename (and a route/registry churn) when Epic 6 lands. Add one entry to `src/stores/registry.ts`'s `TOOLS` array: `{ id: "bucket", name: "Bucket", aliases: ["bucket", "ocr", "screenshot"], route: "/tools/bucket", icon: "<pick one — no existing convention to match beyond short glyphs like the others>", component: () => import("../tools/bucket/BucketView.vue"), drop: { acceptedMimeTypes: [], handler: "bucket_extract_text" } }` — `acceptedMimeTypes: []` matches every other drop-capable entry's existing (unused) convention, not a regression specific to this story.
  - [ ] New `src/tools/bucket/BucketView.vue`. Per AD-16's amendment (`ARCHITECTURE-SPINE.md`, "Amended 2026-08-04, Epic 3 retrospective"), **use `registry.getLatestWinsRunner("bucket")`, not a local `createLatestWinsRunner()`** — `HashView.vue` is the cited reference implementation for this exact shape (a tool with one write-surface reachable both from `DropZone.vue`'s dispatcher and, later, Story 4.2's clipboard-paste). Wiring the shared runner correctly now, even though this story only has one write-trigger (drop), is what lets Story 4.2 add paste without a structural rework — read `HashView.vue` in full before writing this file; it is the pattern to follow, not just a reference to skim.
  - [ ] View content for *this* story only: a drop-hint paragraph (matching `HashView.vue`'s `.drop-hint` convention: "Drop a PNG, JPEG, or WebP image anywhere in the window to extract its text."), a read-only display of the extracted text (a `<pre>` or a disabled `<textarea>` — not editable, not copyable yet; that's explicitly Story 4.2's AC, don't build it early only to rework it), and a `role="alert"` error region matching every other tool view's existing convention (`p[role="alert"] { color: #b00020; }`).
  - [ ] Watch `registry.dropResult` filtered to `toolId === "bucket"`, exactly as `HashView.vue` does for `"hash"` — clear it as a one-shot signal on consumption.

- [ ] **Task 8: CI's `ort-sys` binary caching (AD-11) — verify before assuming a gap**
  - [ ] `ARCHITECTURE-SPINE.md`'s AD-11 states `ort-sys` ONNX Runtime binaries "are cached in CI from Epic 4 onward" — `.github/workflows/ci.yml` currently has no `ort`/`onnx`-specific caching step (confirmed by reading it directly); the existing `Swatinem/rust-cache@v2` step caches `~/.cargo` and `target/` generically, which *may* already cover whatever `ort`'s build script downloads into `OUT_DIR` incidentally. Don't assume either way — after Task 1 adds the dependency, run CI (or inspect a local `cargo build` with `CARGO_LOG=cargo::core::compiler::fingerprint=info` or simply check what lands under `target/.../build/ort-sys-*/out/`) and confirm whether a second CI run is actually faster. If `rust-cache` already covers it, say so in Completion Notes and make no CI change (don't add redundant caching); if not, add an explicit cache step keyed on the `ort`/`ort-sys` version before this story's PR.

- [ ] **Task 9: Manual `pnpm tauri dev` verification (every story since 1.7 requires this — this one especially, given how much is new)**
  - [ ] Cold-launch the app and confirm it opens in well under 2 seconds — the OCR engine must not be constructed at launch (Task 5's log-and-remove check).
  - [ ] Drop a real screenshot (an error dialog or similar, matching the PRD's own demo scenario) containing English text onto the Bucket view; confirm extracted text displays and roughly matches what's actually in the image.
  - [ ] Time the first extraction (cold engine init + inference) and a second extraction (warm) separately — note both in Completion Notes; FR24's "~3s typical" bar is about steady-state extraction, not first-use init cost.
  - [ ] Drop a non-image file (e.g. a `.txt`) and confirm a clear inline error, not a crash or silent hang (NFR4).
  - [ ] Drop an oversized file (construct one past the Task 6 size guard, e.g. via a sparse file — same technique as `hash.rs`'s existing size-limit test) and confirm the guard rejects it before attempting to decode.
  - [ ] While the app runs, spot-check (e.g. Activity Monitor's Network tab, or `nettop`) that dropping the image triggers no outbound connection — a lightweight sanity pass, not the formal NFR1 tour (that's Story 4.3's release-blocking scope), but worth doing now while the auto-download risk is fresh instead of discovering a leak two stories later.

- [ ] **Task 10: Tests**
  - [ ] `crates/umbra-core/src/ocr.rs`: unit tests against the trait/adapter using a small real test-fixture image with known text (commit a tiny PNG fixture, e.g. under `crates/umbra-core/tests/fixtures/` or inline as `include_bytes!` — check whether a `tests/fixtures` convention already exists in this crate before inventing a new one) — assert extracted text contains the expected substring (OCR output on a real model is not byte-exact reproducible the way a hash is, so exact-string assertions will be flaky; assert `contains()` on the key expected words instead).
  - [ ] `src-tauri/src/commands/bucket.rs`: integration tests mirroring `hash.rs`'s `#[tokio::test]` pattern — a real temp-file round trip, the oversized-file rejection, and a not-an-image rejection.
  - [ ] `src/tools/bucket/BucketView.spec.ts`: mirror `HashView.spec.ts`'s structure — drop-result success path, drop-result error path, confirm `registry.getLatestWinsRunner("bucket")` is what's actually used (not a local runner).
  - [ ] Full verification pass: `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `pnpm lint`, `pnpm test`, `pnpm build`, `vue-tsc --noEmit`.

- [ ] **Task 11: Commit and open a PR**
  - [ ] Conventional Commit(s), `feat` type scoped to `bucket` (new tool + new core capability — the first Epic 4 commit).
  - [ ] PR against `main`.

## Dev Notes

- **This is Epic 4's first story and structurally the most novel one in the project so far** — first bundled ML dependency, first Tauri resource-bundling usage, first lazy-singleton pattern, first command needing `AppHandle`, first new Cargo dependency added mid-epic rather than at scaffold time. Budget more verification time than a typical story; don't pattern-match its size expectations against Epic 2/3 stories.
- **Version drift already happened once, live, during this story's own creation.** The spine pinned `oar-ocr` 0.8.x on 2026-07-20 with an explicit "re-verify at Epic 4 start" warning; by 2026-08-03 (one day before this story was drafted) the crate was at 0.9.0. Task 1 is not optional boilerplate — treat every API detail in this Dev Notes section sourced from `oar-ocr`'s docs as a hypothesis to confirm against the real crate, not a fact to build against blindly. [Source: `ARCHITECTURE-SPINE.md` Stack table, `oar-ocr` row]
- **Two unrelated "downloads" are both named `oar-ocr`/`ort` — don't conflate them:**
  1. `ort`'s own default behavior downloads the ONNX Runtime *shared library binary* at **build time** (`cargo build`), cached by CI per AD-11 (Task 8). This is not a v1-runtime network call and doesn't violate INV-1/AD-7.
  2. `oar-ocr`'s `auto-download` Cargo feature — opt-in, not default — would fetch OCR *model weights* from a hub at **runtime** if enabled. This is exactly the AD-7 risk; Task 1 confirms it stays off.
- **AD-8 (the architectural point of this story):** the OCR trait lives in `umbra-core`, `oar-ocr` sits behind it as the v1 adapter, and nothing outside `ocr.rs` imports `oar-ocr` directly. The spine's own Deferred section flags that a future structured-extraction feature (FR29, Epic 6) might extend or fork this same trait shape — naming the module after the capability (`ocr.rs`), not the tool (`bucket`), keeps that door open without deciding it now. [Source: `ARCHITECTURE-SPINE.md` AD-8, Deferred section]
- **AD-16's amendment is this story's load-bearing precedent, and it's not on `main` yet — see Task 0.** The amendment (dated 2026-08-04, from the Epic 3 retrospective) names `registry.getLatestWinsRunner(toolId)` as the required pattern specifically for "Epic 4's Bucket tool (drag-drop and clipboard-paste both dispatching to the same registry-declared handler, per Story 4.2's AC)" and cites `HashView.vue` as the reference implementation. This closed what was Epic 2's retrospective action item #1, left open through all of Epic 3 because neither cron section needed it. [Source: `ARCHITECTURE-SPINE.md` AD-16 amendment, `_bmad-output/implementation-artifacts/epic-3-retro-2026-08-04.md` Action Items]
- **AD-4/AD-16 (performance):** OCR inference is exactly the kind of >100ms CPU work AD-4 exists for — it must run on `spawn_blocking`, not the async executor's default thread. The once-cell (Task 5) is the same pattern AD-16 already names for "the OCR session" specifically, so this story isn't inventing a new architectural concept, just implementing one the spine already anticipated.
- **AD-14/AD-15 (drop path):** nothing new here — this story is the fourth drop-capable tool (after Base64, Hash) and follows the exact same `DropZone.vue` → registry-declared handler → `fs_helper::read_file_bytes` path already proven three times.
- **NFR4/FR26 boundary:** this story must not crash or hang on bad input (any image decode failure becomes a `ToolError`), but the *explicit, friendly* "no text found" / "corrupt image" messaging that FR26 describes is Story 4.3's AC, not this one's. Don't gold-plate error UX here; a correct, structured `ToolError` surfaced through the existing `role="alert"` pattern is sufficient for this story.
- **CI's cross-platform gate (AD-11) now actually matters for the first time.** Every prior story's Rust code was pure-logic and OS-agnostic by construction; a real ONNX Runtime binary dependency is the first thing in this codebase that could plausibly behave differently per platform. `cargo check`/clippy/`cargo test` still run on all three OS runners as required checks — if `oar-ocr`/`ort` has a platform-specific build quirk, this is where it would first surface.

### Project Structure Notes

- New files: `crates/umbra-core/src/ocr.rs`, `src-tauri/src/commands/bucket.rs`, `src-tauri/resources/models/` (two `.onnx` files + one dict file), `src/tools/bucket/BucketView.vue`, `src/tools/bucket/BucketView.spec.ts`.
- Modified files: `crates/umbra-core/Cargo.toml` (add `oar-ocr`, likely `image`), `Cargo.lock`, `src-tauri/src/lib.rs` (register the new command), `src-tauri/tauri.conf.json` (add `bundle.resources`), `src/stores/registry.ts` (add the `bucket` entry), `ARCHITECTURE-SPINE.md` (Stack table version bump + close the model-files Deferred item, per Task 2).
- No conflict with any existing tool's structure — this is purely additive, following the exact `crates/umbra-core/src/<domain>.rs` + `src-tauri/src/commands/<tool>.rs` + `src/tools/<tool-id>/` three-layer pattern every prior tool already established, with `ocr` (capability-named core module) and `bucket` (tool-id-named command/registry/view) deliberately not matching 1:1 — see Dev Notes' AD-8 note for why.
- `.github/workflows/ci.yml` may need a small addition (Task 8) — verify before assuming.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` — Story 4.1: Drag an image in, get its text (lines 691-716); Epic 4 overview (lines 687-689); FR23-FR26, AD-8; FR Coverage Map]
- [Source: `_bmad-output/planning-artifacts/prds/prd-Umbra-2026-07-19/prd.md` — F7 (FR23-FR26), NFR1, NFR2, §10 Glossary ("The Bucket")]
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-Umbra-2026-07-20/ARCHITECTURE-SPINE.md` — AD-1/AD-2 (core purity), AD-3 (command naming/ToolError), AD-4 (async/lazy init), AD-7 (network surface), AD-8 (OCR trait), AD-14/AD-15 (drop/file I/O), AD-16 and its 2026-08-04 amendment (latest-wins runner scoping), AD-11 (CI, `ort-sys` caching "from Epic 4 onward"), Stack table (`oar-ocr` 0.8.x pin, superseded — see Task 1), Deferred section ("Exact OCR ONNX model files" and "AD-8's OCR trait shape vs. a structured second AI feature")]
- [Source: `_bmad-output/implementation-artifacts/epic-3-retro-2026-08-04.md` — Epic 4 Preview section (structural differences called out: first ML dependency, first multi-write-trigger tool, first `OnceCell` pattern); Action Items (AD-16 documentation as critical prep, now closed)]
- [Source: `_bmad-output/implementation-artifacts/sprint-status.yaml` — epic-2 action item #1, resolved 2026-08-04 citing the AD-16 amendment above]
- [Source: `_bmad-output/implementation-artifacts/deferred-work.md` — Story 2.5 entry on `acceptedMimeTypes: []` being pre-existing dead configuration, cited in Task 7]
- [Source: `_bmad-output/implementation-artifacts/3-3-the-phrase-corpus-as-an-automated-acceptance-gate.md` — most recent prior story, read for continuity even though it's a different epic/no direct technical overlap; confirms the project's "verify dependencies against ground truth, not secondhand docs" discipline this story's Task 1 applies to `oar-ocr`]
- Live-read this session: `src/stores/registry.ts`, `src/shell/invoke.ts`, `src/shell/DropZone.vue`, `src/shell/dropZone.ts`, `src/tools/hash/HashView.vue`, `src-tauri/src/commands/hash.rs`, `src-tauri/src/fs_helper.rs`, `crates/umbra-core/src/error.rs`, `src-tauri/src/lib.rs`, `src-tauri/tauri.conf.json`, `src-tauri/capabilities/default.json`, `.github/workflows/ci.yml`, `crates/umbra-core/Cargo.toml`, `src-tauri/Cargo.toml` — confirmed no existing OCR/`oar-ocr`/ONNX code anywhere in the repo (repo-wide grep, no real hits).
- Researched this session (`crates.io` API, `docs.rs`, GitHub, Hugging Face — summarized through an intermediary fetch tool, re-verify per Task 1): `oar-ocr` 0.9.0 (published 2026-08-03) API shape, `auto-download` feature default-off behavior, PP-OCRv6 tiny det/rec ONNX models on Hugging Face (`PaddlePaddle/PP-OCRv6_tiny_det_onnx`), Apache-2.0 license, `std::sync::OnceLock::get_or_try_init`'s unstable status (tracking issue `rust-lang/rust#109737`), Tauri v2's `bundle.resources` config + `AppHandle::path().resolve(..., BaseDirectory::Resource)` runtime resolution API (via the official `tauri-docs` source).

## Change Log

- 2026-08-04: Story drafted via `bmad-create-story`, auto-discovered from `sprint-status.yaml`'s backlog as the first `backlog`-status story in file order (first story of Epic 4, transitioning `epic-4` from `backlog` to `in-progress`). No same-epic previous story exists, so intelligence was instead drawn from the Epic 3 retrospective's explicit "Epic 4 Preview" section (which named this story's exact structural risks: first ML dependency, first multi-write-trigger tool, first lazy-singleton pattern) and its critical-prep action item (AD-16's runner-scoping amendment), confirmed already closed but confirmed **not yet merged to `main`** — flagged as a branch-stacking requirement (Task 0), the same shape as Story 3.2's stacking need. Live-verified the current absence of any OCR-related code in the repo, and re-researched `oar-ocr`'s exact API/version against the architecture spine's own explicit "pre-1.0, re-verify at Epic 4 start" instruction — found the crate had moved from the spine's verified 0.8.x to 0.9.0 in the 24 hours before this story was drafted, confirming that warning was not hypothetical. Chose PP-OCRv6 tiny det/rec ONNX models (Apache-2.0, Hugging Face) as the documented model choice, closing the spine's "exact model files" Deferred item conditionally on Task 2's final verification. Identified and resolved a subtle correctness risk before it could reach implementation: `OnceLock::get_or_try_init` (the natural-looking choice for a fallible lazy init) is still unstable in Rust, so Task 5 directs the stable `OnceLock<Result<...>>` + `get_or_init` pattern instead. Verified via Context7 (official `tauri-docs` source) the exact Tauri v2 resource-bundling and runtime path-resolution API, since no prior story in this codebase has needed it.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
