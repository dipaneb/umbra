---
baseline_commit: b151314
---

# Story 8.2: Reimagine Base64 Encode/Decode

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As the developer,
I want to reconsider Base64 Encode/Decode's feature set through open discovery before redesigning its UI,
so that the redesign reflects a deliberately chosen scope, not a visual reskin of whatever shipped first.

## Acceptance Criteria

**This story ships in two gated tasks (epics.md's own shared Epic 8 shape). Task 1's ACs below are real and testable now. Task 2 (redesign) has no ACs yet — writing them before Task 1's decision record exists would be fiction, per epics.md's explicit instruction — they are added to this story file as a follow-up edit (Task 2a) once Task 1 completes, exactly as Story 8.1 did.**

1. **Given** open scope discovery is run for the Base64 tool (`bmad-party-mode` — the developer has chosen the full roundtable for this story; a second, narrower `bmad-forge-idea` pass may follow), framed explicitly as reconsidering the tool's scope from first principles, **when** discovery concludes, **then** a written decision record exists stating what is kept, cut, and added relative to today's shipped implementation (text ↔ Base64 with standard + URL-safe alphabets and auto-detected alphabet on decode; file → Base64 via window drop; Base64 → saved file via the save dialog; whitespace- and line-wrap-tolerant decode; distinct `base64-not-utf8` vs `base64-invalid` errors carrying a byte-offset position; the 100 MB paste cap and the 10 MB dropped-file cap), with rationale for each call — the existing implementation is reference only, not a decision to preserve by default.
2. **Given** the decision record, **when** it is produced, **then** it states which of FR10–FR12 remain accurate, which are revised, and which are newly added — Epic 8's own preamble makes this revision each story's own output, not predicted in advance.
3. **Given** any idea considered and cut during discovery, **when** the decision record lands, **then** it is captured as a public backlog candidate (FR35) — filed as an individual, max-context GitHub issue per this project's idea-capture convention, not folded into a PRD rewrite and not silently dropped (the same disclosure discipline Story 6.3's own AC established for its FR29 decision).
4. **Given** the chosen scope, **when** the decision record completes, **then** it states which parts of the existing `crates/umbra-core/src/base64.rs` functional core survive as-is (AD-1: core owns every transformation) versus which need new pure functions, **and** whether the `src-tauri/src/commands/base64.rs` wrapper layer (the file-path commands and their size caps) changes — Task 2 builds directly on this split.
5. **Given** Task 1 has not yet produced its decision record, **when** this story starts, **then** Task 2 (redesign, and its own Given/When/Then acceptance criteria) has not begun — no implementation starts before the decision record exists.

## Acceptance Criteria — Task 2 (Redesign)

Written 2026-08-29 via `bmad-party-mode` (same room as Task 1), scoped strictly to
`8-2-base64-decision-record.md`. Numbering continues from Task 1's AC1–5. The room resolved the
decision record's open items first (recorded in "Open items resolved by Task 2a" below); the
CSP question (open item 4) and the exact final `base64-*` code list (open item 5) remain 2b
implementation-discovery items and are called out as such in AC11 / AC13.

6. **Container — single enriched view.** **Given** the redesigned Base64 tool, **when** it
   renders with empty input, **then** exactly three controls are visible — the input panel, an
   Encode ⇄ Decode segmented switch, and the (empty) output panel — with **no tab bar**, no
   `AppTabs` import, and no Encode/Decode/Paste action-button row; **and** all previously
   hardcoded styling (`.drop-hint`'s `#666`/`#ccc`, `p[role="alert"]`'s `#b00020`, every bare
   `<button>`) is replaced with `--color-*` / `--font-code-*` / `--radius-*` / `--spacing-*`
   tokens and `AppButton`, with `src/styles/base.css`'s existing bare-`<textarea>` treatment
   left in place (not re-styled).
   *Amended 2026-08-29 (code review, DN1): the "exactly three controls" clause holds for the
   Decode direction but not for Encode, where the URL-safe checkbox, the output-header wrap
   `<select>` (slice 5), the disabled Copy icon, and the "Build a data URI" disclosure (slice
   4) are also visible with empty input. These are accepted as low-noise standing affordances
   for the Encode direction; the clause is now read as "the Decode direction shows three; the
   Encode direction adds the alphabet, wrap, copy and data-URI-builder affordances." The "no
   tab bar / no `AppTabs` import / no Encode/Decode/Paste action-button row" clauses are met
   as written. The "and `AppButton`" clause is also relaxed: the tool ships no `AppButton` —
   its remaining `<button>`s are icon / text-link affordances (`.copy-button`, `.disclosure`,
   `.offer-action`) for which `AppButton` would be the wrong component, following
   `JsonTree.vue`'s bare copy-button precedent.*

7. **Live conversion.** **Given** the tool with a chosen direction, **when** the user types or
   pastes into the input, **then** the output updates automatically (debounced) with no button
   press; **and** switching the Encode ⇄ Decode segmented control re-runs the conversion on the
   current input immediately; **and** the existing single `createLatestWinsRunner()` still
   backs every conversion (encode, decode, data-URI decode, `sniff`) so a slow earlier call can
   never overwrite a newer result (AD-16 — no new runner scope).

8. **Paste removed, Copy downsized.** **Given** the redesigned tool, **when** the user inspects
   the controls, **then** there is no "Paste" button (⌘V into the input works as normal);
   **and** "Copy" is a single icon button on the output panel (24 px hit area, icon ≥ 16 px per
   `JsonTree.vue`'s copy-button floor), disabled when the output is empty, reusing the
   signature-accent "copied" feedback pattern (no separate success colour).

9. **One contextual slot, priority-ordered, never stacked.** **Given** the output panel,
   **when** zero or more of {a conversion error, a detection result, a data-URI offer} apply,
   **then** at most one is shown, directly under the output, in the fixed priority
   **error > detection > data-URI offer**; **and** it renders as a caption-weight line (not a
   tinted card); **and** when a higher-priority item resolves, the next one down (if any)
   appears without layout jump.
   *Amended 2026-08-29 (slice-3 render review, `/design` Option D): when a decode has no
   textual output, the slot may **replace** the output `<textarea>` with a reading of the
   payload — a JWT segment split, or an inline `<img>` preview — headed by a caption-weight
   "reading strip". Still at most one thing; still no tinted card; error still wins.*

10. **Blob detection — honest by construction (AD-9).** **Given** a successful decode, **when**
    the decoded bytes / input structure match a known shape, **then** the contextual slot shows
    exactly one identification from the fixed ordered set — **not-Base64 (rendered as the
    error) → JWT → PNG / PDF / gzip / zip (magic bytes) → valid UTF-8 text → unknown** — first
    match wins; **and** a JWT identification offers a **"Read as JWT"** confirm and is never
    applied silently (the split-by-segment reading only takes effect on confirm); **and** an
    "unknown" result states "decoded N bytes — unrecognized" and asserts nothing more; **and**
    no identification is ever phrased as certain fact (`sniff` returns candidates carrying
    ambiguity, per the decision record's AD-1 split). Regression: a plain-ASCII input that is
    incidentally valid Base64 must land on "valid UTF-8 text", not a false binary guess.
    *Amended 2026-08-29 (slice-3 render review, `/design` Option D): the JWT split-by-segment
    reading is shown **by default**, headed by a "Reading as JWT" strip with a persistent
    **"Show raw"** that collapses it to the bare "Looks like a JWT" line. The AD-9 honesty bar
    is met by the present-tense "reading" framing + the always-available dismissal + the
    ability to answer "unknown" — not by gating the view behind a click. `sniff`'s JWT test
    stays strict (3 segments, both header+payload are JSON objects, header has `alg`), so a
    false positive is near-impossible; and a PNG whose `<img>` fails to render falls back to
    the text note, never a broken-image icon.*
    *Amended 2026-08-30 (code review, P5): the PNG / PDF / gzip / zip magic-byte arms still
    outrank a plain-text reading, but only for payloads that are **not** also valid UTF-8. A
    real file of those types carries non-text bytes past its header, so it still classifies
    correctly; a plain-text note that merely opens with the literal characters `%PDF` (or the
    zip/gzip lead bytes) now lands on "valid UTF-8 text" rather than being hidden behind a
    false "looks like a file" reading — the same incidental-collision principle this AC's own
    `dGVzdA==` regression already states. `sniff_jwt` additionally requires the signature (3rd)
    segment to be empty (`alg: "none"`) or itself Base64URL, so `header.payload.<punctuation>`
    is not read as a token.*

11. **Data URI — build.** **Given** the Encode direction, **when** the user opens the data-URI
    builder and picks a MIME type, **then** the tool shows `data:<mime>;base64,<current encoded
    output>` as a copyable string, updating live with the input; **and** when the current
    conversion originated from a dropped file, the MIME type is pre-selected from that file's
    extension; **and** the build is pure view-side string composition (no new command, no
    async).

12. **Data URI — decode + preview.** **Given** input that starts with `data:`, **when** it is
    decoded, **then** `base64_parse_data_uri` (new `spawn_blocking` command, `Result<T,
    ToolError>`) splits the MIME + payload; **and** for an `image/*` MIME the contextual slot
    renders an inline `<img>` preview of the decoded bytes **before** any save; **and** a "Save
    as file" action writes the decoded bytes via the existing `base64_decode_to_file` /
    `fs_helper` path (AD-15), defaulting the filename extension from the MIME; **and** a
    malformed `data:` prefix produces a distinct `base64-data-uri-malformed` `ToolError`, never
    a crash or a silent pass-through. *2b implementation-discovery: confirm whether
    `tauri.conf.json`'s `security.csp` needs an `img-src ... data:` addition for the preview —
    a one-line addition proceeds in 2b; anything larger comes back to the developer as a
    question.* **Resolved 2026-08-29 (slice 3): the CSP already carries `img-src 'self' asset:
    http://asset.localhost data:` — no change needed.** Slice 3's Option D also brings a
    bare-PNG-blob `<img>` preview forward (built from a view-side `data:` URI, no command);
    the `data:`-prefixed-input + `base64_parse_data_uri` path and the `image/*` MIME still
    land in slice 4.

13. **Line-wrap on encode.** **Given** the Encode direction, **when** the user picks a wrap
    width from an inline `none / 64 / 76` selector next to the output, **then** the encoded
    output is line-wrapped at that column (wrapping done in `umbra-core`, not Vue, via a new
    `wrap: Option<LineWrap>` parameter on `encode_bytes` / `encode` / `base64_encode` /
    `base64_encode_file`); **and** the default is `none`; **and** the selector is absent in the
    Decode direction; **and** decode remains tolerant of any wrap width on input (unchanged).

14. **Preserved behaviours.** **Given** the redesign, **when** it ships, **then** all of the
    following still work unchanged: registry-dispatched window drop with the view supplying
    only `url_safe` (AD-14, no document-level listeners); Base64 → file via the save dialog;
    standard-vs-URL-safe alphabet **auto-detection on decode**; the 100 MB input cap
    (`MAX_INPUT_BYTES`) and 10 MB dropped-file cap (`MAX_FILE_BYTES`); **and** every existing
    `crates/umbra-core/src/base64.rs` regression test (all 20) and `Base64View.spec.ts` case
    passes without modification to its assertions.
    *Amended 2026-08-29 (second render review): a dropped file now follows the **direction**.
    The generic AD-14 dispatch is unchanged — no document listeners, the view still supplies
    only args — but the tool's drop handler is `base64_ingest_file` (replacing
    `base64_encode_file`), and the view's provider now supplies `{ url_safe, decode }`. Encode
    direction → encode the bytes (as before). Decode direction → hand the file's UTF-8 text to
    the input box so the live decode/`sniff` pipeline runs, or, for a non-text (binary) file,
    show a transient notice — never silently encode. The 10 MB cap and the encode-a-file path
    are otherwise intact.*
    *Amended 2026-08-29 (code review, DN4): (a) "passes without modification to its assertions"
    could not survive AC15 (the `base64-invalid` → classified-code split) or AC6/AC7/AC8 (the
    Encode/Decode/Paste buttons the old `Base64View.spec.ts` cases drove): core, command and FE
    test assertions were rewritten and the Paste + AC3 alert-text cases removed. Those changes
    are sanctioned as forced by the later ACs. (b) The provider supplies `{ url_safe, decode,
    wrap }`, not `{ url_safe, decode }` — the `wrap` arg is AC13's (slice 5). (c) "Base64 →
    file via the save dialog" — see DN2 below: a "Save as file" action is added to the text
    output panel so this preserved behaviour is reachable for every decode result, not only
    binary/JWT ones.*

15. **i18n — no English-only strings.** **Given** every new user-facing string (detection
    lines, data-URI labels, wrap-selector labels, the new error messages), **when** the app
    runs in French, **then** each renders translated via ordinary `tools.base64.*` /
    `errors.*` keys with `en` + `fr` entries passing `src/locales/locales.spec.ts`; **and** the
    classified codes `base64-invalid-char`, `base64-not-utf8`, `base64-invalid-length`,
    `base64-invalid-padding`, `base64-data-uri-malformed` are added to
    `src/shell/toolError.ts`'s `TRANSLATABLE_CODES` (offsets/counts ride the structured
    `position` field, never baked into the translatable prose); **and** `base64-input-too-large`
    stays **out** of that set (embeds a byte count in prose), matching `json-input-too-large`.
    *2b implementation-discovery: the exact final code list may shift as `map_decode_error` is
    split — the principle (classification codes translated, value-embedding codes not) is
    fixed; the enumeration is confirmed against the real `DecodeError` variants in 2b.*

16. **Architecture compliance for new surface.** **Given** the new functions and commands,
    **when** they are implemented, **then** `parse_data_uri` and `sniff` are pure functions in
    `crates/umbra-core/src/base64.rs` with their own regression tests, touching no filesystem
    and no IPC (AD-1); **and** `base64_parse_data_uri` and `base64_sniff` are `spawn_blocking`
    commands (AD-4) returning `Result<T, ToolError>` (AD-3), named `base64_<verb>`; **and** the
    view renders only from `ToolError` structure (`code`, `position`), never by parsing
    `message`; **and** no other tool's files and no shared `src/shell/` / `src/stores/` file
    beyond `toolError.ts`'s `TRANSLATABLE_CODES` set are modified (AD-6).
    *Amended 2026-08-29 (code review, DN4): four files outside that boundary were touched, all
    mechanically forced and none a behavioural change to another tool: `src/stores/registry.ts`
    (base64's own drop-`handler` string, `base64_encode_file` → `base64_ingest_file`);
    `src/shell/dropZone.spec.ts` (the same rename in a shell dispatch test's fixture);
    `crates/umbra-core/src/jwt.rs` and `src-tauri/src/commands/jwt.rs` (test helpers threading
    the `None` `wrap` arg AC13 added to the shared `encode_bytes`). Accepted as forced by the
    slice-3 handler rename and AC13's signature change.*

### Open items resolved by Task 2a (2026-08-29)

- **"Build a data URI" is not a separate state group** — it is synchronous view-side string
  composition off the existing encoded output; data-URI *decode* rides the one shared
  latest-wins runner alongside `sniff` and live decode. No new runner scope (AC7, AC11).
- **Detection priority is a fixed ordered list, first-match-wins, exactly one shown** —
  not-Base64 → JWT → PNG/PDF/gzip/zip → UTF-8 text → unknown. No confidence scoring (AC10).
- **Line-wrap is an inline `none/64/76` selector**, Encode-direction only, **default `none`**
  (changing the emitted-output default for existing users is a trap) (AC13).
- **Vertical-slice delivery with a developer render-review after the first slice** is written
  into Task 2b's checklist (Winston's ask — the 8.1 reflexive-copy safeguard).

### Still owned by Task 2b (implementation-discovery, not resolvable in the room)

- ~~Whether the `data:` image preview needs a `tauri.conf.json` `security.csp` `img-src data:`
  addition (AC12).~~ **Resolved slice 3/4: the CSP already carries `img-src … data:` — no change.**
- ~~The exact final classified `base64-*` code set, confirmed against the real `base64` crate
  `DecodeError` variants once `map_decode_error` is split (AC15).~~ **Resolved slice 6:
  `base64-invalid-char` (`InvalidByte` / `InvalidLastSymbol`), `base64-invalid-length`
  (`InvalidLength`), `base64-invalid-padding` (`InvalidPadding`), plus `base64-not-utf8` and
  `base64-data-uri-malformed` — matches AC15's enumeration exactly.**

## Tasks / Subtasks

- [x] **Task 0: Branch setup (AC: all)**
  - [x] Confirm `baseline_commit` (`b151314`) is still `origin/main`'s real tip before branching (Story 8.1 was merged, then releases 0.2.0 / 0.2.1 landed on top — `b151314` is that current tip). Verified: `git rev-parse origin/main` == `b151314d016e6a694439fbeb7d52c5a86e81d8ed`.
  - [x] `git checkout -b feat/story-8-2-reimagine-base64-encode-decode` — branched from `13c6942` (current tip = `origin/main` `b151314` + the `docs(story-8-2)` story-creation commit) rather than literally from `origin/main`, so the story file travels with the implementation branch. This matches Story 8.1's effective base (its story-creation commit was the first commit on its own feat branch). Every subsequent commit lands on this branch.

- [x] **Task 1: Discovery — produce the decision record (AC1–4)** — decision record written and confirmed by the developer 2026-08-29; 3 cut-idea GitHub issues filed ([#113](https://github.com/dipaneb/umbra/issues/113), [#114](https://github.com/dipaneb/umbra/issues/114), [#115](https://github.com/dipaneb/umbra/issues/115), `backlog-candidate` label) — AC1–AC4 all satisfied.
  - [x] Run `bmad-party-mode` framed explicitly as: *open scope discovery for Base64 Encode/Decode — the existing implementation is reference only, not a scope to preserve.* (Installed roster — Mary, John, Sally, Winston, Amelia, Paige; `session` mode; party memory on — resumed Story 8.1's history. `bmad-forge-idea` pass not run — not needed, the room reached a clear decision.)
  - [x] Feed the session the current, real state so it starts from fact, not assumption (all re-confirmed by direct read at session start — `base64.rs` 251 lines, `commands/base64.rs` 183 lines, `Base64View.vue` 261 lines — matched Dev Notes exactly):
    - `crates/umbra-core/src/base64.rs`: `encode`/`encode_bytes` (premade `STANDARD`/`URL_SAFE` engines), `decode`/`decode_bytes` (custom `DecodePaddingMode::Indifferent` engines tolerant of unpadded input; whitespace/line-wrap stripped before decode; alphabet auto-detected by presence of `-`/`_`); `decode` layers a UTF-8 check on top, returning a distinct `base64-not-utf8` error with a byte-offset `Position`; `map_decode_error` maps `DecodeError` → `base64-invalid` with a byte offset where one is meaningful; `MAX_INPUT_BYTES` = 100 MB (CWE-400 guard).
    - `src-tauri/src/commands/base64.rs`: async commands `base64_encode`, `base64_decode`, `base64_encode_file`, `base64_decode_to_file` — all `spawn_blocking` (AD-4); `base64_encode_file` enforces a separate 10 MB `MAX_FILE_BYTES` cap via `metadata` *before* the file is read; `fs_helper` owns the actual read/write (AD-15).
    - `src/tools/base64/Base64View.vue`: input textarea + readonly output textarea; standard/url_safe radio `<fieldset>`; Encode / Decode / Paste / Decode-to-file buttons plus a separate Copy button; inline `ToolError` display with `errorLocation` (LineCol or ByteOffset); one `createLatestWinsRunner()` shared across encode/decode/paste; drop wired via `registry.setDropArgsProvider("base64", …)` + a `watch` on `registry.dropResult` (AD-14 — `DropZone.vue` is the dispatcher, the view only supplies `url_safe`); decode-to-file uses `save()` then `base64_decode_to_file`.
    - Styling is entirely pre-Epic-7: hardcoded `#666` / `#ccc` / `#b00020`, bare `<button>`s (not `AppButton`), local scoped CSS — untouched by any Epic 7 story. (`src/styles/base.css`, added by PR #104, already gives a bare `<textarea>` a token border + focus ring for free — don't re-style what's covered.)
    - i18n keys live under `tools.base64.*` in `src/locales/{en,fr}.json`; `src/locales/locales.spec.ts` runs every message through vue-i18n's real compiler.
  - [x] Ground the session in what Epic 7 locked — read `src/styles/tokens.css`, `base.css`, `src/components/AppButton.vue`, `src/components/AppTabs.vue`, `src/App.vue`, `src/shell/icons.ts` and `src/tools/json/JsonView.vue` (the 8.1 redesign reference) to pin exact token values, component anatomy and the main-pane frame before the design canvas.
  - [x] Produce the written decision record satisfying AC1–AC4: kept / changed / added / cut with rationale, container decision (single enriched view — Option A), FR10–FR12 revision, i18n/`TRANSLATABLE_CODES` finding, AD-1 core split. Saved to `_bmad-output/implementation-artifacts/8-2-base64-decision-record.md`. AC3: 3 cut-idea issues filed on `dipaneb/umbra` (#113 hex view, #114 decode-as-gzip, #115 recursive decode), `backlog-candidate` label, each linking back to the decision record.
  - [x] Record the AD-1 functional-core split satisfying AC4: `encode`/`encode_bytes`/`decode`/`decode_bytes`, the decode engines, alphabet detection, the UTF-8 layer and both size guards all survive unchanged; new pure fns `parse_data_uri` and `sniff` plus a `wrap: Option<LineWrap>` param on `encode_bytes`; `src-tauri/src/commands/base64.rs` changes additively (two new `base64_<verb>` commands, a `wrap` arg on two existing ones).
  - [x] Built a 3-artboard comparison design canvas (single view / tabbed / two-pane) matching Umbra's real token system, published as an Artifact, for the developer to choose the container shape. Developer chose Option A (single enriched view) 2026-08-29.

- [x] **Task 2a: Redesign ACs — write real Given/When/Then** — done 2026-08-29 via `bmad-party-mode` (same room). Open items resolved: data-URI build is not a separate state group; detection is a fixed first-match-wins order; line-wrap is an inline Encode-only selector defaulting to `none`. Two items explicitly handed to 2b as implementation-discovery (CSP for `data:` preview; final `base64-*` code list). AC6–AC16 written into the "Acceptance Criteria — Task 2 (Redesign)" section above. **Awaiting developer confirmation of the AC set before 2b starts.**

- [ ] **Task 2b: Redesign — implementation** — deferred until the developer confirms the AC6–AC16 set. Follow the delivery pattern Story 8.1 established (see Dev Notes). **Vertical-slice order, developer render-review after the first slice** (Winston's ask — the 8.1 reflexive-copy safeguard):
  - [x] **Slice 1 — tokenize + restructure to the single enriched view (AC6, AC8, AC14).** Replace hardcoded hex (`.drop-hint` `#666`/`#ccc`; `p[role="alert"]` `#b00020`) with `--color-*`; `--font-code-*` for in/out panels; `--radius-*`/`--spacing-*` for layout; bare `<button>`s → `AppButton`; remove the Paste button, downsize Copy to an output-panel icon. **No `AppTabs`.** → verification pass → **stop for the developer's eyes on the first real render** before slice 2. **Done 2026-08-29** — full verification green (`pnpm lint`, `pnpm test` 588 passed, `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` 229 passed); `pnpm tauri dev` running for the developer's render review. Judgment call resolved against AC6/AC7: Encode/Decode stay as two default-variant `AppButton`s that convert on click for slice 1; the segmented switch + debounced live conversion are slice 2 (AC7's own scope) — no segmented control introduced now. **Awaiting the developer's render review before slice 2.**
  - [x] **Slice 2 — live conversion + segmented direction switch (AC7).** Debounced watch on input; Encode ⇄ Decode segmented control; keep the one shared `createLatestWinsRunner()`. **Done 2026-08-29** — 200ms debounced `watch([input, direction, alphabet])`; a direction/alphabet change re-runs immediately (bypasses the debounce), a text edit waits it out. Encode/Decode `AppButton`s replaced by a two-radio segmented control (native radios, visually collapsed — kept in the tab order + a11y tree). One shared `createLatestWinsRunner()` unchanged (AC7 / AD-16). Folded in the input-label research outcome (developer's Q): static `inputLabel` → direction-aware `inputLabelEncode` "Text to encode" / `inputLabelDecode` "Base64 to decode" (en + fr), matching the mainstream single-field-converter pattern. **Judgment calls (flagged for render review):** the URL-safe alphabet selector now shows only in the Encode direction (decode auto-detects), and the Decode-to-file action only in the Decode direction — both follow from making direction the primary axis; neither is in slice 2's literal checklist line. AC6's "exactly three controls with empty input" is now *closer* but not literally met (the encode-direction alphabet selector remains a fourth control) — treating that as later-slice/explicit-design work. Full verification green (594 FE / 229 + 88 Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`); `pnpm tauri dev` hot-reloaded the change for the render review. **Revised 2026-08-29 after the render review** — developer ran `/design` (canvas `562ce7ef`, 8 artboards: reference-matched switch, 4 auto-looping motion options, alphabet-footprint options, decode-to-file-home options, composite) and chose: **sliding-thumb** motion, **inline URL-safe checkbox** on the switch row (Option A placement + Option B control — single checkbox, "Standard" is the unstated default), **contextual "Save as file"** in place of the stray Decode-to-file button. Implemented: the segmented control is now a grey track (`--color-accent-neutral-chip`, `--radius-lg`) with a white raised card (`--color-bg-surface` + hairline + soft shadow) that slides via `transform 140ms cubic-bezier(.32,.72,0,1)` (`.is-decode` toggles `translateX(100%)`); `prefers-reduced-motion` drops the transition. `→ Encode` / `← Decode` use `PhArrowRight` / `PhArrowLeft`. `<fieldset>`+legend replaced by `role="radiogroup"` + `aria-label` (no visible legend, matches the reference). `alphabet` string ref → `urlSafe` boolean ref; the radio `<fieldset>` → one encode-only `<input type="checkbox">` on the `.switch-row`. Decode-to-file: the standalone `AppButton` is gone (no `AppButton` left in the tool at all); when a live decode fails with **`base64-not-utf8`** the catch sets `notText` instead of `error`, and a caption-weight line offers `Save as file` (plain underlined text action, not the signature accent — JsonView `.position-link` restraint) wired to the **unchanged** `save()` + `base64_decode_to_file` flow (AC14). Full "looks like PNG/JWT/…" detection + the raw-bytes decode path stay slice 3; this interim only handles the one non-text case detectable today. Error + offer both stay above the output for now (where this view has always shown feedback); slice 3's AC9 slot moves the unified, priority-ordered version below it. Locales: removed `alphabetLegend` / `alphabetStandard` / `alphabetUrlSafe` / `decodeToFile`; added `urlSafeAlphabet` / `decodedNotText` / `saveAsFile` (en + fr). Verification green again (596 FE / 229 + 88 Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`); `pnpm tauri dev` hot-reloaded. Render review found the save dialog opened nameless ("Untitled", no extension) — pre-existing (the old Decode-to-file button also called `save()` bare) but conspicuous under the new framing. Fixed in the revision: `save({ defaultPath: "decoded.bin" })` — a plain honest default that needs no type detection. A type-aware name + file-type filter (`decoded.png`, …) rides slice 3's `sniff`; the MIME-derived extension is slice 4 (AC12). **Awaiting the developer's render review of the revision before slice 3.**
  - [x] **Slice 3 — `sniff` + the one contextual slot (AC9, AC10).** Pure `sniff` in `umbra-core::base64` + tests → `base64_sniff` (`spawn_blocking`, `Result<T, ToolError>`) → contextual-slot component with the fixed priority (error > detection > data-URI offer) and the "Read as JWT" confirm. **Done 2026-08-29.** `sniff(input: &str) -> Result<Sniff, ToolError>` — pure, no I/O; first-match-wins JWT → PNG/PDF/gzip/zip (magic bytes) → valid UTF-8 text → unknown. "not-Base64" is the `Err(ToolError)` (renders in the error slot, outranks detection — AC9/AC10). `Sniff` is a serde-tagged enum (`kind`); `Jwt` carries the pretty-printed header + payload so "Read as JWT" only *reveals* what sniff already computed — no third command (AC16 lists only `sniff` / `parse_data_uri`). 8 core regression tests incl. the AC10 regression (plain-ASCII-valid-Base64 → `Text`, never a binary guess) + `alg`-required + `alg:none` empty-sig. `base64_sniff` command (`spawn_blocking`, `Result<_, ToolError>`, AD-3/AD-4) registered; 2 command tests. View: decode direction now calls `base64_sniff` (replaces `base64_decode` there — one call, no second runner race, AC7); `notText` boolean → `sniff` ref; contextual slot is the `<p v-if="error">` / `<template v-else-if="detection">` chain directly under the output, caption-weight, never stacked (AC9). "Read as JWT" → `jwtRevealed` reveals two readonly panels; "Save as file" for binary/unknown reuses the unchanged decode-to-file flow, now with a type-aware `defaultPath` (`decoded.png`/`.pdf`/`.gz`/`.zip`/`.bin`). 6 new FE tests. i18n: `looksLikeJwt` / `readAsJwt` / `looksLikeBinary` (`{type}`/`{size}`) / `decodedUnrecognized` (`{size}`) / `jwtHeader` / `jwtPayload` (en + fr); `decodedNotText` removed. No `toolError.ts` change (classified codes are slice 6). Full verification green (599 FE / 90 core + 237 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, core+tauri clippy). **Revised 2026-08-29 after the render review** — the empty ten-row output `<textarea>` under a faint caption read as broken. `/design` canvas `562ce7ef` page 3 ("Not-text output") offered 4 fixes; developer chose **Option D (auto-preview)**. Now: when a decode isn't text, the output field is *removed* (`showOutputPanel` computed) and the space is taken by a reading of the payload — a JWT's segment split shows **by default** under a "Reading as JWT · Show raw" strip (collapsible to the bare line); a PNG blob renders an inline `<img>` from a **view-side `data:` URI** (no command, no bytes over IPC — CSP already allows `img-src … data:`, resolving the AC12 discovery item), with an `@error` fallback to the text note; pdf/gzip/zip/unknown keep the one caption line. AC9 + AC10 amended in the AC section (recorded, not silent). `sniff` (Rust) unchanged — it already returns everything. `jwtRevealed` → `jwtCollapsed` (default expanded) + `imagePreviewOk` ref. New i18n `readingAsJwt` / `showRaw` / `imagePreviewAlt` (en + fr). 600 FE tests, Rust unchanged (90 + 237), lint / `vue-tsc` / build / `cargo fmt` all green. **Awaiting the developer's render review of the revision before slice 4.**
  - [x] **Slice 4 — data URI build + decode + preview (AC11, AC12).** `parse_data_uri` in core + tests → `base64_parse_data_uri` → builder (sync string compose) + decode path + inline `<img>` preview + Save-as-file reusing `base64_decode_to_file`. Resolve the CSP `img-src data:` question here (one-liner → proceed; larger → ask). **Done 2026-08-29.** CSP question: no change — `tauri.conf.json`'s CSP already carries `img-src … data:` (confirmed slice 3). **Build (AC11):** an Encode-direction collapsed disclosure ("Build a data URI") → a MIME `<select>` (12 curated types) + a readonly `data:<mime>;base64,<output>` textarea + a copy icon; `dataUriString` is a pure `computed` off `output` + `builderMime`, live with the input, no command/async; a dropped file pre-selects the MIME (`base64_ingest_file` now returns `Ingest::Encoded { value, mime }` — extension→MIME via `mime_from_path`). **Decode + preview (AC12):** when the input starts with `data:`, `convert()`'s decode branch calls `base64_parse_data_uri` (NEW `spawn_blocking` command, `Result<DataUri, ToolError>`, `parse_data_uri` pure fn in core — 6 tests: split, empty-mediatype→`text/plain`, media params kept, and 3 malformed cases → `base64-data-uri-malformed`); `image/*` MIME → the existing `.image-preview` renders `<img src="data:<mime>;base64,<payload>">` (view-side); non-image → chains `base64_sniff(payload)` in the *same* latest-wins task so JWT/text/binary detection still works on the inner content. "Save as file" writes the **payload** (not the whole `data:` string) via the unchanged `base64_decode_to_file`, filename ext from the MIME (`mimeToExt`). `base64-data-uri-malformed` added to `toolError.ts`'s `TRANSLATABLE_CODES` + `errors.*` (en + fr) — a fixed canned sentence, safe to translate now rather than waiting for slice 6. i18n: `dataUriImageLine` (`{mime}`) / `dataUriBuilder` / `dataUriMimeLabel` / `dataUriCopied` (en + fr). 5 new FE tests. Full verification green (610 FE / 95 core + 243 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, `cargo clippy --workspace`). **Awaiting the developer's render review before slice 5.**
  - [x] **Slice 5 — line-wrap on encode (AC13).** `wrap: Option<LineWrap>` on `encode_bytes`/`encode`/`base64_encode`/`base64_encode_file` + tests → inline `none/64/76` selector, Encode-direction only, default `none`. **Done 2026-08-29.** `pub enum LineWrap { Col64, Col76 }` (`#[serde(rename_all = "snake_case")]` → `"col64"`/`"col76"`) + a private `wrap_encoded(String, Option<LineWrap>)` helper (`.as_bytes().chunks(cols).join("\n")` — Base64 is ASCII) in `crates/umbra-core/src/base64.rs`; `encode` / `encode_bytes` gain a `wrap: Option<LineWrap>` param (all callers threaded, incl. the `jwt` module's test helpers). Commands: `base64_encode` and `base64_ingest_file` (the slice-3 rename of `base64_encode_file`, per AC13's own list) take `wrap`; the decode paths ignore it. 5 new core tests (no-wrap = single line, wraps at 64 / 76, wrapped round-trips through the whitespace-tolerant `decode` unchanged, `LineWrap` snake_case deserialization) + 1 command test. **View:** `wrapMode` ref (`none` default), `wrapArg()` → `null | "col64" | "col76"`; `convert()`'s encode branch and `dropArgsProvider` pass `wrap`; the `watch` source gains `wrapMode` so a width change re-encodes immediately (same path as `urlSafe`). A `<select>` (`None / 64 / 76`) in the output header, `v-if="direction === 'encode'"` — absent while decoding; decode stays whitespace-tolerant (unchanged). The data-URI builder strips whitespace from the output (`dataUriString`), so a wrapped output still yields a one-line `data:` URI. i18n: `wrapLabel` / `wrapNone` (en + fr; the `64` / `76` option labels are literal digits). 3 new FE tests. Full verification green (614 FE / 96 core + 247 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, `cargo clippy --workspace`). **Awaiting the developer's render review before slice 6.**
  - [x] **Slice 6 — classified error codes + French (AC15).** Split `map_decode_error` into classified `base64-*` codes; add the classification codes to `TRANSLATABLE_CODES`; `en`+`fr` for every new string (`{'{'}` / `{'}'}` escape for literal braces). **Done 2026-08-29.** `map_decode_error` now returns `base64-invalid-char` (`InvalidByte` / `InvalidLastSymbol`, byte offset in `position`), `base64-invalid-length` (`InvalidLength`, no position), `base64-invalid-padding` (`InvalidPadding`, no position) — each with a fixed canned English `message`, no runtime value baked in. `decode`'s UTF-8 failure keeps `base64-not-utf8` but its `message` is now a canned sentence too (the offset stays in `position`, not the raw `FromUtf8Error` text). The **final code list matches AC15 exactly**: `base64-invalid-char`, `base64-not-utf8`, `base64-invalid-length`, `base64-invalid-padding`, `base64-data-uri-malformed` — all five added to `src/shell/toolError.ts`'s `TRANSLATABLE_CODES` (`base64-data-uri-malformed` was already there from slice 4); `base64-input-too-large` stays **out** (embeds a byte count in prose), matching `json-input-too-large`. `errors.base64-invalid-char` / `-invalid-length` / `-invalid-padding` / `-not-utf8` added to en + fr (no literal braces → no escape needed; `locales.spec.ts` compile + parity pass). Rust tests updated (`base64-invalid` → `base64-invalid-char` where a bad char is the cause) + 1 new (`InvalidLength` gets its own code, mid-string `=` is a char error not a length error). FE spec: the 5 `base64-invalid` mocks retargeted — two of them (AC3 / no-position) now *assert* the translated `errors.*` text and that the raw Rust message is NOT shown. Full verification green (614 FE / 96 core + 248 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, `cargo clippy --workspace`). **Task 2b implementation complete — awaiting the developer's final render review, then `bmad-code-review` + commit.**
  - [ ] Per slice: pure Rust fn + regression tests → `base64_<verb>` command (`spawn_blocking`, `Result<T, ToolError>`, AD-3/AD-4) → Vue → full verification pass (`pnpm lint`, `pnpm test`, `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace`) → visual check in `pnpm tauri dev` → commit, **ask before pushing**.
  - [ ] Preserve unchanged (AC14): registry-dispatched window drop (AD-14), Base64 → file save flow, auto-alphabet-detection on decode, all 20 core tests + `Base64View.spec.ts`, the 100 MB / 10 MB caps.
  - [ ] Full verification pass green, then `bmad-code-review` (different LLM).

## Dev Notes

- **This is the second real instance of Epic 8's "Task 1 decision record gates Task 2" shape — and the first one is now proven.** Story 8.1 (`8-1-reimagine-the-json-formatter-viewer.md`) executed the full pattern end to end: `bmad-party-mode` discovery → `8-1-json-decision-record.md` → Task 2a AC-writing in the same room → per-slice implementation with a developer design-review pass → `bmad-code-review` → done. **Use Story 8.1's story file and decision record as the working template**, not Story 6.3 (still `backlog`, never executed).
- **Current implementation, read in full this drafting session — the discovery session's factual starting point, NOT this story's scope:**
  - `crates/umbra-core/src/base64.rs` (251 lines): see Task 1's "feed the session" bullet for the full breakdown. 20 regression tests already cover round-trips, `+`/`/` vs `-`/`_` alphabet detection, unpadded input, trailing newline, PEM-style line wrapping, invalid-char byte offset, non-UTF-8 offset-after-valid-prefix, and both size-cap rejections. `encode_bytes`/`decode_bytes` (raw `&[u8]` / `Vec<u8>`) are the Story 2.2 file-path additions; `encode`/`decode` (`&str` / UTF-8-validated `String`) are thin wrappers for the text case.
  - `src-tauri/src/commands/base64.rs` (183 lines): the four async commands, plus `MAX_FILE_BYTES` (10 MB, deliberately smaller than core's 100 MB and scoped to this command because a Base64-encoded file's *output* size grows with its input and has to render in a `<textarea>`). `map_join_error` → `base64-internal` on a `spawn_blocking` panic.
  - `src/tools/base64/Base64View.vue` (261 lines) + `src/tools/base64/Base64View.spec.ts` (existing component tests — re-verify and extend, don't duplicate).
  - i18n: `tools.base64.*` in `src/locales/en.json` (block starts ~line 150) and `src/locales/fr.json`; guarded by `src/locales/locales.spec.ts`.
- **FR mapping.** **FR10** — text ↔ Base64 including URL-safe alphabet, auto-detected on decode. **FR11** — file → Base64 and Base64 → downloadable file (this tool was the project's first use of the drop service + save helper, Story 2.2). **FR12** — invalid input produces a clear inline error, never a crash or silent empty output. Epic 8's preamble makes the FR10–FR12 revision this story's own output — do not treat the current wording as fixed.
- **Architecture boundaries Task 1 must scope against and Task 2 must obey:**
  - **AD-1** — every new transformation is a pure function in `umbra-core::base64`; never computed in Vue or `src-tauri`. Core returns machine values, the view renders them.
  - **AD-3** — every command returns `Result<T, ToolError>`; the view renders from `ToolError`'s structure only (`code`, `position`), never by parsing `message`. Commands are `base64_<verb>`.
  - **AD-4** — anything that can exceed ~100 ms CPU runs on the blocking pool via `spawn_blocking` (all four current commands already do).
  - **AD-6** — tools are islands. Do not touch another tool's files. Cross-cutting state lives only in the `settings` / `registry` Pinia stores. (Base64 already declares a `clipboardMatch` on its registry entry for Story 7.8's clipboard-suggestion surface — changing that shape is a shell concern; if the decision record wants it, present it as options with trade-offs, don't decide it silently.)
  - **AD-9 / AD-13 honesty** — Base64 has no natural-language grammar, so no AD-13-style disclosed exception is needed and French rides the existing `vue-i18n` seam like every other tool. But if Task 1 adds any "guess what this blob is / what encoding this is" heuristic, `EXPERIENCE.md`'s honesty bar and AD-9's "never a confident-sounding wrong answer" principle apply — the same reasoning Story 8.1 applied to its Repair tab's preview-then-confirm contract.
  - **AD-14** — window drops dispatch through the shell's one generic dispatcher to the tool's registry-declared handler; the view supplies only tool-specific args (here, `url_safe`). Register no document-level listeners.
  - **AD-15** — files cross IPC as absolute paths; `fs_helper` owns all reads/writes; `umbra-core` never touches the filesystem; byte arrays above ~64 KB never ride the JSON IPC bridge (clipboard-pasted images are the only sanctioned exception).
  - **AD-16** — one latest-wins runner per *independent* piece of state. `Base64View.vue` today shares a single `createLatestWinsRunner()` across encode/decode/paste (they all write the one `output` ref, so that is correct as-is). If Task 2 splits the tool into genuinely independent views/state-groups, scope one runner per group — `CronView.vue`'s two-section split is the cautionary precedent (a tool-wide runner falsely supersedes an unrelated section); `HashView.vue`'s `registry.getLatestWinsRunner(toolId)` is the reference when drop + an in-view invoke both write the same surface.
- **Reusable pattern from Story 8.1 — follow it, don't reinvent:**
  - `src/components/AppTabs.vue` exists now (built in 8.1 as DESIGN.md's Tab component). Reuse it if Task 1's scope yields multiple named views; do not build a second tab component.
  - **Per-slice delivery with a developer design-review pass right after the first real render** caught reflexive-copy mistakes in 8.1: an `AppButton` `primary` (orange) variant applied by copying another tool's shape without checking DESIGN.md reserves that colour for one true signature action; a whole layout row spent on a pick-once setting; generic Paste/Copy buttons that had lost their referent. **Base64's current Encode/Decode/Paste/Copy button cluster and the standard-vs-url_safe radio `<fieldset>` are exactly the shapes to reconsider in Task 1/2, not port forward by default.**
  - **vue-i18n treats a literal `{` / `}` in a message string as interpolation syntax.** Any new locale string that shows Base64/data-URI/alphabet syntax with a literal brace needs the `{'{'}` / `{'}'}` escape. `locales.spec.ts` guards it, but reach for the escape proactively.
  - **Never rely on incidental template whitespace for visual spacing** between adjacent Vue elements — it collapses silently. Give a spaced element its own node with real CSS `margin`.
  - **When tightening a loose test threshold** (e.g. a latency ceiling on a new command), calibrate it against a real `cargo test --workspace` run under parallel contention, not an isolated single-test measurement — 8.1's first attempt at a 2 s ceiling proved flaky and settled at 10 s.
- **Performance.** No Base64-specific performance profile exists (unlike JSON's Story 1.9 10 MB baseline). `base64` crate encode/decode is linear and fast; the practical ceiling is rendering a large output textarea — which is why `base64_encode_file` caps input at 10 MB. Any Task 2 view over large input should be sanity-checked for render cost and must honour the existing 100 MB / 10 MB caps rather than exempting itself.
- **Styling status.** `Base64View.vue` is 100% pre-Epic-7 — Task 2 is the first tokenization pass this tool gets. `base.css` covers a bare `<textarea>`'s border and focus-visible ring; still hardcoded and Task 2's real work: `.drop-hint`'s `#666` / `#ccc`, `p[role="alert"]`'s `#b00020`, and every bare `<button>`.
- **No inter-story dependency.** Epic 8's preamble states 8.1–8.9 are mutually independent once Epic 7 is done; 8.2 runs now because the developer chose it, and it can be created and implemented without waiting on 8.3–8.9.

### Project Structure Notes

- **Likely touched (contingent on Task 1's decision — confirm during Task 2):** `src/tools/base64/Base64View.vue`, `src/tools/base64/Base64View.spec.ts`; `crates/umbra-core/src/base64.rs` and its tests only if Task 1 decides new core transformations are needed; `src-tauri/src/commands/base64.rs` only if the command surface changes; `src/locales/en.json` + `src/locales/fr.json` for any new strings; `src/components/AppTabs.vue` and `src/components/AppButton.vue` as *consumers* (imported, not modified).
- **New:** Task 1's decision-record artifact — `_bmad-output/implementation-artifacts/8-2-base64-decision-record.md`.
- **Out of scope regardless of Task 1's outcome:** any other tool's files (AD-6, tools are islands) and any shared `src/shell/` / `src/stores/` file beyond what Epic 7 already generalized (`tokens.css`, `icons.ts`, `AppTabs.vue`, `AppButton.vue`) — unless the decision record explicitly justifies a shared-infrastructure change, in which case this project's CLAUDE.md governance-check discipline applies: present it as options with trade-offs to the developer, and check it against the project's established governance patterns (branch protection, LICENSE, CI gates, the `type(scope): subject` commit convention) rather than assuming they travel with the change.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` — Epic 8 preamble + shared story shape (231–243, 1318–1337); Story 8.2 charter (1342–1344); Epic 2 Base64 stories 2.1 (504–527) and 2.2 (529–552) as reference-only prior acceptance criteria; FR10–FR12 (49–51, 158–161); Story 6.3, the originally-cited (still unexecuted) pattern precedent (~972–991)]
- [Source: `_bmad-output/planning-artifacts/prds/prd-Umbra-2026-07-19/prd.md` — F3 / FR10–FR11 (57–61), FR12; FR2's "b64" → Base64 alias example (45)]
- [Source: `_bmad-output/planning-artifacts/sprint-change-proposal-2026-08-16.md` — Epic 8's "chartered, not fully spec'd" charter and rationale (§3, §4.3); Base64 explicitly **not** structurally impacted by Epic 7, FR-level behaviour unchanged (§2)]
- [Source: `_bmad-output/implementation-artifacts/8-1-reimagine-the-json-formatter-viewer.md` — the now-proven template: Task 1 AC shape (17–26), Task 2a/2b gating and per-slice delivery (109–241), Review Findings (243–269), and the i18n / template-whitespace / test-threshold lessons in Dev Notes and completion notes]
- [Source: `_bmad-output/implementation-artifacts/8-1-json-decision-record.md` — the decision-record format to mirror: Kept / Added / Cut (backlog) / FR revision / AD-1 core-split / i18n-AD-13 finding / "open items Task 2 still owns"]
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-Umbra-2026-07-20/ARCHITECTURE-SPINE.md` — AD-1 (45–49), AD-3 (57–61), AD-4 (63–67), AD-5 (69–73), AD-6 (75–79), AD-9 (93–97), AD-13 + its 2026-08-23 amendment (117–150), AD-14 (152–156), AD-15 (158–162), AD-16 + the 2026-08-04 runner-scoping amendment (164–175); Consistency Conventions table (179–184)]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-umbra-2026-08-15/DESIGN.md` — token frontmatter (6–113); `{typography.code}` / Geist Mono rationale (~154); Do's-and-Don'ts (187–196); the Tab component spec `AppTabs.vue` implements]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-umbra-2026-08-15/EXPERIENCE.md` — Information Architecture three-views table (16–29); Voice and Tone error-message bar (~50); State Patterns Error row (~71); Accessibility Floor (86–94)]
- [Source: `_bmad-output/implementation-artifacts/7-1-design-tokens-and-icon-system-land-in-the-shell.md` — the token naming convention (`--color-*`, `--font-<role>-*`, `--radius-*`, `--shadow-*`) Task 2 must consume, not reinvent]
- Live-read this drafting session: `crates/umbra-core/src/base64.rs`, `src-tauri/src/commands/base64.rs`, `src/tools/base64/Base64View.vue`, `src/locales/en.json` (base64 block) — full contents, confirming the current feature set and existing CWE-400 guards before discovery reconsiders them.

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (Claude Code, bmad-dev-story workflow)

### Debug Log References

- 2026-08-28 — Task 0: `git rev-parse origin/main` → `b151314d016e6a694439fbeb7d52c5a86e81d8ed` (confirms `baseline_commit`). `git checkout -b feat/story-8-2-reimagine-base64-encode-decode` from `13c6942`. Re-read `crates/umbra-core/src/base64.rs` (251 lines), `src-tauri/src/commands/base64.rs` (183 lines), `src/tools/base64/Base64View.vue` (261 lines) — all match the story Dev Notes exactly; no drift since the drafting session.
- 2026-08-29 — Task 1: `bmad-party-mode` discovery session (installed roster, `session` mode, party memory resumed Story 8.1). Read `tokens.css` / `base.css` / `AppButton.vue` / `AppTabs.vue` / `App.vue` / `icons.ts` / `JsonView.vue` for the design canvas. Published a 3-container comparison canvas as an Artifact (`6582897f-…`). Developer confirmed: container = Option A; blob detection IN; line-wrap on encode IN; 3 cut ideas → GitHub issues. Decision record written. `TRANSLATABLE_CODES` checked — zero `base64-*` coverage (finding recorded).
- 2026-08-29 — Task 2b **Slice 3 revision — follow-up fixes** (second render review):
  - **Phantom horizontal scrollbar on the in/out `<textarea>`s.** A Base64 blob is one unbroken token with no whitespace for a `<textarea>` to soft-wrap on (slice-1 gap — the tokenization pass set only `--font-code-*`). First attempt `width: 100%; min-width: 0; max-width: 70em; overflow-wrap: anywhere` — `overflow-wrap` does not take on `<textarea>` in WebKit, and the `70em` cap made the textarea narrower than its full-bleed siblings (`.output-header`, `.switch-row`) so the Copy / URL-safe controls ended up alone at the far right. Reverted the width props; the scoped `textarea` rule now adds only `word-break: break-all` (the property WebKit honours on `<textarea>`). If it still scrolls in the app it stays as-is — cosmetic, the text is readable, developer OK'd leaving it.
  - **"Dropping a `.txt` in Decode gives Base64, not a preview."** The developer's own diagnosis was right: a file **drop always encodes** (`base64_encode_file`, FR11) regardless of the direction switch, so dropping `base64.txt` (a PNG's Base64, as text) in Decode encoded *that text* → `base64(base64(png))` in the output, in Decode mode. Fix: (1) `.drop-hint` is `v-if="direction === 'encode'"` — it says "…to Base64-encode it", which contradicts Decode; (2) on any drop while in Decode the view flips the switch to Encode so "Output" isn't showing Base64 next to a "Decode" control. The flip would otherwise trigger the `[input,direction,urlSafe]` watch and re-convert (encoding the empty text box, wiping the drop result) — a one-shot `suppressConvertOnce` ref swallows exactly that watcher run. Also kept from the prior pass: `output.value = ""` at the top of `convert()` (clears a stale result the instant a *user-initiated* switch starts a new conversion, rather than when it finishes). New spec cases: drop-in-Decode flips to Encode + keeps the Base64; drop hint hidden in Decode; result cleared on convert start.
  - **No feedback while a dropped file processes** — `DropZone.vue` / `registry` shell gap affecting every drop-capable tool; out of 8.2's scope (AC14 freezes the drop path). Filed as [#116](https://github.com/dipaneb/umbra/issues/116) with the root cause + a proposed `registry.dropPending` signal.
- 2026-08-29 — Task 2b **Slice 4** (data URI build + decode + preview — AC11, AC12, AC16). `crates/umbra-core/src/base64.rs`: `pub struct DataUri { mime, payload }` + `pub fn parse_data_uri(&str) -> Result<DataUri, ToolError>` — pure, splits `data:[<mediatype>][;base64],<payload>`, requires the `;base64` marker, empty mediatype → `text/plain` (RFC 2397), any of {no `data:`, no `,`, no `;base64`} → `Err(base64-data-uri-malformed)`. 6 core tests. `src-tauri/src/commands/base64.rs`: `base64_parse_data_uri` (`spawn_blocking`, `Result<DataUri, ToolError>`, AD-3/AD-4) + 2 tests; `mime_from_path` (extension→MIME, 15 types) and `Ingest::Encoded` gains `mime: Option<String>` for the builder pre-select + 1 test; `base64_ingest_file` encode arm fills it. Registered in `lib.rs`. `src/shell/toolError.ts`: `base64-data-uri-malformed` added to `TRANSLATABLE_CODES` (a fixed canned sentence — the exception this file's own comment allows). **View** (`Base64View.vue`): `DataUriResult` type (`image` | `sniffed`); `dataUri` ref (reset in `convert()` + drop watcher). `convert()` decode branch: `value.trim().startsWith("data:")` → `runLatestWins(async () => { const du = await invoke("base64_parse_data_uri"); return du.mime.startsWith("image/") ? {kind:"image",…} : {kind:"sniffed", sniff: await invoke("base64_sniff", {input: du.payload}), …} })` — parse-then-maybe-sniff as one runner task (AC7). `detectionText` / `detectionMode` / `imageDataUri` extended for `dataUri.kind === "image"` (preview from `data:${mime};base64,${payload}`; `@error` → note). `saveSource` computed (`dataUri?.payload ?? input`) + `mimeToExt` → `onSaveAsFile` writes the payload with a MIME-named default. Builder: `DATA_URI_MIME_OPTIONS` (12), `builderOpen` / `builderMime` refs, `dataUriString` computed (`data:${builderMime};base64,${output}`), `onCopyDataUri` (reuses `useCopyFeedback` key `"datauri"`); template disclosure after the output field, Encode-only; drop watcher's `encoded` arm pre-selects `builderMime` from `ingest.mime` (guarded to the option list). i18n: `dataUriImageLine` (`{mime}`) / `dataUriBuilder` / `dataUriMimeLabel` / `dataUriCopied` + `errors.base64-data-uri-malformed` (en + fr). Spec: `Ingest` type gains `mime`; 5 new cases — data:image → preview `src` + no output field, malformed → translated alert (not the raw Rust message), non-image data URI → parse+sniff chain + `decoded.pdf` save name + payload-not-whole-string written, builder live string + copy, builder MIME from a dropped `.png`. CSP: unchanged (already has `img-src … data:`). Verification: `pnpm lint`, `pnpm test` (610), `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` (95 core + 243 tauri + 1), `cargo clippy --workspace` — all green. `pnpm tauri dev` rebuilt the Rust + HMR'd the view.
- 2026-08-29 — Task 2b **Slice 5** (line-wrap on encode — AC13, AC16). `crates/umbra-core/src/base64.rs`: `pub enum LineWrap { Col64, Col76 }` (serde snake_case, `Deserialize`), `wrap_encoded(String, Option<LineWrap>) -> String` (chunk the ASCII Base64 at 64/76 bytes, `join("\n")`; `None` → return as-is), and a `wrap: Option<LineWrap>` param on `encode` / `encode_bytes`. Every caller threaded (`base64.rs` tests, `jwt.rs` test helpers, `commands/jwt.rs`). Commands: `base64_encode(input, url_safe, wrap)` and `base64_ingest_file(path, url_safe, decode, wrap)` — AC13 lists `base64_encode_file`, which slice 3 renamed to `base64_ingest_file`, so the param lands there. 5 core tests (single-line default; wrapped at 64 → `[64,16]` line lengths, at 76 → `[76,4]`; wrapped `encode` → `decode` round-trips; `"col64"`/`"col76"` deserialize) + 1 command test. `Base64View.vue`: `WrapMode` type + `wrapMode` ref (default `"none"`) + `wrapArg()`; encode `invoke("base64_encode", …)` and `dropArgsProvider` send `wrap`; `watch([input, direction, urlSafe, wrapMode], …)` (a width change is a same-input change → immediate re-convert, AC13); a `<select>` `None / 64 / 76` in `.output-header` (`.output-header` reworked from `space-between` to `label { margin-right: auto }` so the label stays left and wrap + copy group right), `v-if="direction === 'encode'"`. `dataUriString` now `.replace(/\s+/g, "")`s the output — a wrapped Base64 must not become a multi-line `data:` URI. i18n: `wrapLabel` / `wrapNone` (en + fr). Spec: the two `base64_encode`-arg assertions + the `dropArgsProviders` assertion gain `wrap`; 3 new cases (wrap width passed + re-encode on change; selector absent in Decode; wrapped output kept one-line in the builder). Verification: `pnpm lint`, `pnpm test` (614), `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` (96 core + 247 tauri + 1), `cargo clippy --workspace` — all green. `pnpm tauri dev` rebuilt the Rust + HMR'd the view.
- 2026-08-29 — Task 2b **Slice 6** (classified error codes + French — AC15, AC16). `crates/umbra-core/src/base64.rs`: `map_decode_error` split — `DecodeError::InvalidByte` / `InvalidLastSymbol` → `base64-invalid-char` (offset in `position`), `InvalidLength` → `base64-invalid-length`, `InvalidPadding` → `base64-invalid-padding`; each `message` a fixed canned English sentence with no runtime value. `decode`'s `base64-not-utf8` message likewise canned (offset stays in `position`, no raw `FromUtf8Error` string). `src/shell/toolError.ts`: the 5 `base64-*` classification codes added to `TRANSLATABLE_CODES` (`base64-data-uri-malformed` already present since slice 4); `base64-input-too-large` deliberately excluded (byte-count-in-prose, like `json-input-too-large`). `src/locales/{en,fr}.json`: `errors.base64-invalid-char` / `-invalid-length` / `-invalid-padding` / `-not-utf8` — plain sentences, no `{`/`}`, `locales.spec.ts` compile + parity green. Rust tests: `"base64-invalid"` → `"base64-invalid-char"` in every bad-character assertion (`base64.rs` + `commands/base64.rs`) + a new test (`InvalidLength` → its own code; a mid-string `=` is a char error, not length). FE spec: the 5 `base64-invalid` mocks retargeted to real codes; the AC3 + no-position cases now assert the *translated* `errors.*` text and that the raw Rust `message` is **not** rendered (proving the view keys off `code` + `position`, AC16). No view code change — `toolErrorMessage(error, t)` + the `errorLocation` computed already do the right thing. Verification: `pnpm lint`, `pnpm test` (614), `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` (96 core + 248 tauri + 1), `cargo clippy --workspace` — all green. **Every Task 2b slice (1–6) is implemented.**
- 2026-08-29 — Task 2b **Slice 5 — render-review polish** (output-header balance): `.wrap-select` label + its `<select>` value were `--font-caption-size` (12px) vs `--font-body-size` (14px) — both now `--font-label-*` so "Wrap None" reads as one control; the copy icon went 24/16 → 26/18 so it holds its own next to the `<select>`; `.output-header .copy-button` gains `margin-left: var(--spacing-2)` so the wrap config and the copy action don't cluster. CSS-only; lint / tests / build green.
- 2026-08-29 — Task 2b **Slice 4 — three render-review fixes.** (1) *Stale input after an encode-drop.* The `dropResult` `encoded` branch set `output` but never cleared `input`, so dropping a `.png` on top of a typed `data:garbage` left the two panels contradicting each other. Now the branch clears `input` (the file is the source), guarding `suppressConvertOnce` so the resulting `[input, direction, urlSafe]` watch run doesn't re-encode the empty box over the result. (2) *Disclosure looked like a link.* `class="offer-action"` (JsonView's underlined position-link style) → a real disclosure: a rotating `PhCaretRight` + a plain `--font-label` label, `.disclosure` (quiet button, hover to primary, focus ring), `aria-controls`. The two labels `dataUriBuilderShow`/`dataUriBuilderHide` merged to one `dataUriBuilder` (the caret carries open/closed). (3) *Re-opening the builder was slow every time.* `v-if` on the fields destroyed + re-created the `<textarea>` holding the multi-MB data-URI string on every open → `v-show` so the DOM (and its layout) is retained; toggling is now a `display` flip. 1 new FE test (encode-drop clears stale input). 611 FE tests, Rust unchanged, lint / `vue-tsc` / build / `cargo fmt` / clippy green.
- 2026-08-29 — Task 2b **Slice 3 revision — decode-a-dropped-file** (developer chose this over "hide the drop zone in Decode"). The developer's real confusion was that a file drop *always encoded*, regardless of the switch. Now the drop follows the direction. `src-tauri/src/commands/base64.rs`: `base64_encode_file` → **`base64_ingest_file(path, url_safe, decode) -> Result<Ingest, ToolError>`** (`Ingest` = serde-tagged `Encoded { value } | Text { value } | NotText`); `!decode` → encode the bytes; `decode` → `String::from_utf8` → `Text` (the file's Base64 text, for the input box) or `NotText` (a real binary file). 10 MB `check_file_size` and the metadata-before-read guard kept. 3 old `base64_encode_file` tests migrated + 2 new (`Text`, `NotText`). Registered in `lib.rs` (swapped in the `use` + `generate_handler!`); `registry.ts` drop handler string swapped; `dropZone.spec.ts` fixture + assertion updated (generic-dispatch test, mechanism unchanged). `Base64View.vue`: `dropArgsProvider` → `{ url_safe, decode: direction === "decode" }`; `dropResult` watcher branches on `mode` — `encoded` → Output + flip to Encode (via `landInEncode()` / `suppressConvertOnce`, unchanged); `text` → `input.value = value`, stay in Decode, live pipeline takes over; `not_text` → `showDropNotice()` (a self-contained `ref` + 8 s `setTimeout`, cleared in `convert()` and on unmount), stay in Decode, nothing encoded. Drop hint is direction-aware again (`dropHintEncode` / `dropHintDecode`) instead of hidden in Decode. New `.drop-notice` line (`role="status"`, signature-tint, not the destructive colour — it's a mistake, not a failure). i18n: `dropHint` → `dropHintEncode` + `dropHintDecode` + `fileNotBase64Text` (en + fr). Spec: `dropArgsProviders` now `{url_safe, decode}`; drop tests use the `Ingest` shape; new cases — Base64-text file in Decode lands in the input + decodes there, binary file in Decode shows the notice + does NOT encode, drop-hint text follows the direction. AC14 amended in the AC section (dispatch mechanism unchanged; handler + args + direction-awareness are the change). Verification: `pnpm lint`, `pnpm test` (605), `vue-tsc`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` (92 core + 237 tauri + 1), `cargo clippy --workspace` — all green.
- 2026-08-29 — Task 2b **Slice 3 revision** (post-render-review — the empty output textarea): `/design` canvas `562ce7ef` gained page 3 with 4 options (`OutputCollapse` / `OutputSummary` / `OutputCard` / `OutputPreview`); developer picked **Option D**. `Base64View.vue`: `showOutputPanel` computed (`!error && detectionMode === null`) gates the whole output `.field`; new `detectionMode` computed (`"jwt" | "image" | "note" | null`). JWT template inverted — split shows by default (`jwtCollapsed` ref, default `false`, reset in `convert()` + drop watcher), under a `.reveal-strip` ("Reading as JWT" + "Show raw"); collapsed state is the slice-3 `.detection` line with "Read as JWT". `detectionMode === "image"` (PNG + `imagePreviewOk`): `.image-preview` with a `.reveal-strip` (`detectionText` + "Save as file") and `<img :src="imageDataUri" @error="imagePreviewOk = false">`; `imageDataUri` computed builds `data:image/png;base64,` + `input.value.replace(/\s+/g,"").replace(/-/g,"+").replace(/_/g,"/")` — pure view-side (AC11 principle), CSP `img-src … data:` already present. `.image-frame` = checkerboard `repeating-conic-gradient` + `max-height:320px` contain. `detectionMode === "note"` (pdf/gzip/zip/unknown, or PNG whose `<img>` errored): the one `.detection` line + "Save as file". Removed `detectionAction` / `onDetectionAction` / `.jwt-reveal` CSS; added `.reveal-strip` / `.image-preview` / `.image-frame`. Locales: `readingAsJwt` / `showRaw` / `imagePreviewAlt` added (en + fr). `Base64View.spec.ts`: JWT test rewritten (split visible by default; "Show raw" collapses; "Read as JWT" re-expands; `#base64-output` absent); PNG test → asserts `.image-preview img` `src` is the `data:image/png;base64,…` URI + strip text; new `@error` → text-note fallback test. AC9 + AC10 got inline `*Amended …*` notes in the AC section; AC12's CSP discovery item marked resolved. No Rust change (`sniff` already returns header/payload/byte_len). Verification: `pnpm lint`, `pnpm test` (600), `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` (90 + 237 + 1) — all green. `pnpm tauri dev` HMR'd (no Rust rebuild needed).
- 2026-08-29 — Task 2b **Slice 3** (`sniff` + the one contextual slot): `crates/umbra-core/src/base64.rs` gains `pub enum Sniff` (`#[serde(tag = "kind", rename_all = "snake_case")]`; `Jwt { header, payload }` / `Png|Pdf|Gzip|Zip { byte_len }` / `Text { text }` / `Unknown { byte_len }`) and `pub fn sniff(input: &str) -> Result<Sniff, ToolError>`. Ordering (AC10): `sniff_jwt` on the raw string first (a token's `.`s make the whole string invalid Base64, so decode-first would only ever see the error) → `decode_bytes` (propagates the real `base64-invalid` / `base64-input-too-large` `ToolError` for the "not-Base64" case, which the view renders in the error slot) → `classify_bytes` (magic bytes → `String::from_utf8` → `Unknown`). `sniff_jwt` requires exactly 3 `.`-segments with non-empty header+payload, both decoding to JSON *objects*, and an `alg` member in the header (RFC 7515 §4.1.1); it pretty-prints the first two segments into the `Jwt` variant so "Read as JWT" is a pure reveal — **no third command** (AC16 enumerates only `sniff` / `parse_data_uri`). `decode_bytes`/`decode`/engines/UTF-8 layer/`map_decode_error`/`MAX_INPUT_BYTES` all unchanged. 8 core tests (incl. the AC10 regression: `sniff("dGVzdA==")` → `Sniff::Text{"test"}`, never binary; `alg`-required negative; `alg:none` empty-sig positive; magic-byte quartet; over-max). `src-tauri/src/commands/base64.rs`: `base64_sniff(input: String) -> Result<Sniff, ToolError>`, `spawn_blocking`, `map_join_error` (AD-3/AD-4), + 2 command tests; registered in `lib.rs` invoke_handler. `clippy` nits fixed (`?` over `if is_none`, dropped a fn-pointer test array). **View** (`Base64View.vue`): decode direction's `convert()` branch now calls `invoke<Sniff>("base64_sniff", …)` instead of `base64_decode` — one call returns the text *and* the identification, so the shared latest-wins runner never has two decode calls racing (AC7); `notText: Ref<boolean>` → `sniff: Ref<Sniff | null>` + `jwtRevealed: Ref<boolean>` (reset at the top of `convert()` and in the drop watcher). New computeds `detectionText` / `detectionAction` (`"jwt" | "save" | null`) / `detectionActionLabel` / `jwtSegments`; `formatBytes` helper (B/KB/MB). Template: the slice-2 `error` + `notText` pair → `<p v-if="error" role="alert">` then `<template v-else-if="detectionText || detectionActionLabel">` holding one caption `<p class="detection">` (flex, `gap`, `--font-caption-*`, never a card — AC9) + an optional `.offer-action` button + the `v-if="jwtRevealed && jwtSegments"` reveal (two readonly `#base64-jwt-header` / `#base64-jwt-payload` panels behind a hairline rule). `text` kind → output panel filled, **no** detection line (the panel is the identification). `onSaveAsFile`'s `defaultPath` upgraded from flat `"decoded.bin"` to `saveDefaultName()` (`decoded.png` / `.pdf` / `.gz` / `.zip`, else `.bin`) — the slice-2 render-review follow-up, now that the type is known. CSS: `.not-text-offer` → `.detection`; added `.jwt-reveal`; `.offer-action` lost its `margin-left` (the flex `gap` owns spacing now). i18n: added `looksLikeJwt` / `readAsJwt` / `looksLikeBinary` (`{type}` + `{size}`) / `decodedUnrecognized` (`{size}`) / `jwtHeader` / `jwtPayload` in en + fr; removed `decodedNotText`. `locales.spec.ts` placeholder-parity + compile pass (`{type}`/`{size}` are interpolations, no literal braces). Spec reworked: `reachSaveOffer` now resolves `{kind:"unknown"}`; decode tests assert `invoke("base64_sniff", …)`; new cases — text→no line, png→"PNG"+"2.0 KB"+empty output, JWT reveal only after confirm (`#base64-jwt-header` absent until "Read as JWT" clicked), error outranks detection, type-aware save name. No `toolError.ts` / `TRANSLATABLE_CODES` change (slice 6 owns classified codes). Verification: `pnpm lint`, `pnpm test` (599), `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` (90 core + 237 tauri + 1 integration), `cargo clippy` (core + tauri) — all green. `pnpm tauri dev` auto-rebuilt the Rust (new PID) and HMR'd the view — live for the render review.
- 2026-08-29 — Task 2b **Slice 2 revision** (post-render-review, design-driven): `/design` canvas `562ce7ef` published (8 artboards); developer picked sliding-thumb motion + inline URL-safe checkbox + contextual "Save as file". `Base64View.vue`: `.segments` (label-fill active state) → `.direction-switch` grey track + absolutely-positioned `.direction-thumb` white card, `transition: transform 140ms cubic-bezier(.32,.72,0,1)`, `.is-decode` → `translateX(100%)`, `@media (prefers-reduced-motion: reduce)` kills it. `<fieldset>`/`<legend>` → `role="radiogroup"` + `:aria-label="t('tools.base64.directionLegend')"`. Icons `PhArrowRight`/`PhArrowLeft` (`weight="bold"`). `alphabet: Ref<"standard"|"url_safe">` → `urlSafe: Ref<boolean>`; radio `<fieldset>` (3 keys) → one `<input type="checkbox" v-model="urlSafe">` in a new `.switch-row` flex container, `v-if="direction === 'encode'"`, `margin-left:auto`. `convert()` reads `urlSafe.value` directly; `watch` source `[input, direction, urlSafe]`. Decode-to-file: removed the gated `.actions` `AppButton` (and the `AppButton` import — none left in this file); added `notText` ref, set in `convert()`'s catch when `dir === "decode" && toolErr.code === "base64-not-utf8"` (else `error.value`), cleared at the top of `convert()` and in the drop watcher. Renamed `onDecodeToFile` → `onSaveAsFile` (same body: `save()` + `base64_decode_to_file`, `decodingToFile` guard); the render review flagged the dialog opening as "Untitled" (pre-existing — `save()` was always called with no options), so `save({ defaultPath: "decoded.bin" })` now pre-fills a name — type-aware naming is deferred to slice 3's `sniff` / slice 4's MIME (AC12). Template: `<p v-else-if="notText" class="not-text-offer">` after the `role="alert"` `<p>`, with an `.offer-action` plain-underline button (`color: inherit`, not signature accent — JsonView `.position-link` precedent; own node + real margin, no template-whitespace spacing). Removed CSS: `.segments*`, `.actions`, `.direction-switch legend`, `fieldset` rule. Locales: dropped `alphabetLegend`/`alphabetStandard`/`alphabetUrlSafe`/`decodeToFile`, added `urlSafeAlphabet`/`decodedNotText`/`saveAsFile` (en + fr; `decodedNotText` has an apostrophe, no braces — `locales.spec.ts` compile passes). `Base64View.spec.ts`: `alphabet` radio selector → `urlSafeCheckbox` helper; `reachSaveOffer()` helper (live decode rejecting `base64-not-utf8`); all decode-to-file tests rerouted through the offer's "Save as file" button; new cases — sliding thumb `.is-decode` class tracks direction, radiogroup role present + no Encode/Decode buttons, checkbox encode-only, `base64-not-utf8` → offer not alert, `base64-invalid` → alert not offer. The old "generic error without a position" case switched from `base64-not-utf8` to `base64-invalid` (the former is now the offer path). 596 FE tests / 229 + 88 Rust pass; lint, `vue-tsc`, build, `cargo fmt --check` clean. `pnpm tauri dev` (running since slice 1) HMR-updated. No Rust/command changes.
- 2026-08-29 — Task 2b **Slice 2** (live conversion + segmented direction switch): added `direction` ref (`encode`/`decode`, default `encode`) and a `convert()` that dispatches `base64_encode`/`base64_decode` through the existing single `runLatestWins`, with the empty-input short-circuit *inside* the runner task (so clearing the input mid-flight still supersedes the older call). `debounce(() => void convert(), 200)` from `src/shell/debounce.ts`; `watch([input, direction, alphabet], ([nextInput],[prevInput]) => …)` — text change → debounced, direction/alphabet change → `debouncedConvert.cancel()` + immediate `convert()`. `debouncedConvert.cancel()` added to `onUnmounted`. Removed `runTransform`/`onEncode`/`onDecode`. Segmented control built inline (no shared component — none exists, story didn't ask for one): `<fieldset class="direction-switch">` + `.segments` pill wrapping two `<label>`-wrapped native radios; radios `position:absolute;opacity:0;pointer-events:none` (NOT `display:none` — keeps keyboard arrow-key selection + SR semantics), `.active` label filled with `--color-accent-default` (same token AppTabs' active state uses), `:focus-within` ring. Alphabet `<fieldset>` gated `v-if="direction === 'encode'"`; the Decode-to-file `.actions` row gated `v-if="direction === 'decode'"`. Input label is now a `computed` off `direction`. Locales: dropped `tools.base64.inputLabel`, added `inputLabelEncode`/`inputLabelDecode`/`directionLegend` in en + fr (no literal braces, no escape needed). `Base64View.spec.ts`: added `vi.useFakeTimers()` in `beforeEach` + `vi.useRealTimers()` in `afterEach` (mirrors JsonView.spec.ts); `setDirection()` / `settleConversion()` helpers; every action-driven test reworked to type-then-settle; decode-to-file tests set Decode direction first and deliberately leave the live-decode debounce unadvanced so `base64_decode_to_file` is the only `invoke` in play; new AC7 cases (debounce delay, immediate re-run on switch, single latest-wins runner via a slow/fast `deferred` pair) and AC6 cases (segmented switch present / no Encode-Decode `.app-button`, alphabet encode-only, decode-to-file decode-only, direction-aware label). `flushPromises()` under fake timers works here for the same reason it does in JsonView.spec.ts. `useCopyFeedback` shared Vite chunk unchanged. Verification: `pnpm lint`, `pnpm test` 594 passed, `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` 229 + 88 passed. `pnpm tauri dev` (still running from slice 1) HMR-updated `Base64View.vue` and page-reloaded on the locale edits — live for the render review.
- 2026-08-29 — Task 2b **Slice 1** (tokenize + restructure to the single enriched view): re-read `Base64View.vue`, `Base64View.spec.ts`, `AppButton.vue`, `tokens.css`, `base.css`, `JsonTree.vue` + `JsonView.vue` copy-button pattern, `useCopyFeedback.ts`, `icons.ts`, `en.json`/`fr.json` base64 block, `vitest.setup.ts` (confirms global i18n in tests). `Base64View.vue` rewritten: no `AppTabs` (there never was one — confirmed, none added); `.drop-hint` `#666`/`#ccc` → `--color-text-secondary` / `--color-border-hairline` + `--font-caption-size` + `--radius-default` + `--spacing-*`; `p[role="alert"]` `#b00020` → `--color-accent-destructive` (matches `JsonView.vue`); in/out `<textarea>` → `--font-code-*` (base.css's bare-textarea border/bg/focus-ring left untouched); layout margins/gaps → `--spacing-*`. Bare `<button>`s → `AppButton` **default** variant (Encode, Decode, Decode-to-file) — `primary` deliberately NOT used (reserved for one signature action; deferred to a later slice's discussion). **Paste button + `onPaste` + `readClipboardText` import removed** (⌘V covers it). Copy → 24px text-less icon button in a new `.output-header` row on the output panel, `PhCopySimple` default / `PhCheck` on confirm, `:disabled` when `output === ''`, signature-accent `.copy-success` — reuses `../json/useCopyFeedback` (imported, not modified; cross-tool import called out in a code comment — a candidate to hoist to `src/shell/` if a reviewer prefers, not done speculatively). New i18n key `tools.base64.outputCopied` (en + fr) for the copied-state aria-label/title. `Base64View.spec.ts`: dropped the Paste test + `readText`/`readTextMock` mock; retargeted the two Copy tests to `.copy-button`; added structural cases (no Paste button, Copy is a text-less icon button with `aria-label`, no `role=tab`/`role=tablist`, exactly 3 `.actions .app-button`, signature-accent copied confirmation). Debug log — `useCopyFeedback` became its own shared Vite chunk (`useCopyFeedback-*.js`) once imported by a second tool; expected, harmless. Full verification pass green; `pnpm tauri dev` launched for the render review, no compile/runtime errors in its log.

### Completion Notes List

- Story context created via `bmad-create-story` (2026-08-28) — comprehensive developer guide for a Task-1-gated Epic 8 charter story. Only Task 1 (discovery) carries real, testable acceptance criteria; Task 2's ACs are deferred to a follow-up edit (Task 2a) once `8-2-base64-decision-record.md` exists, per epics.md's explicit instruction that Task 2 ACs would be fiction before the decision record is made. `baseline_commit` set to `b151314` (origin/main tip: Story 8.1 merged as PR #105, then releases 0.2.0 / 0.2.1). Current Base64 implementation (core, command wrapper, view, i18n) live-read in full so discovery starts from confirmed fact.
- 2026-08-28 — `bmad-dev-story` session started. Task 0 (branch setup) complete. Task 1 (open scope discovery via `bmad-party-mode`) is a facilitated roundtable requiring the developer's cut/keep/add judgement calls — begun interactively, not run autonomously (Story 8.1's decision record shows the same: "Developer confirmed all four open questions").
- 2026-08-29 — Task 1 discovery complete bar the AC3 filing step. Decision record `8-2-base64-decision-record.md` written and confirmed by the developer. **Scope decided:** KEPT — text↔Base64 both alphabets + auto-detect, whitespace-tolerant decode, file↔Base64 (drop + save), distinct `base64-not-utf8`/`base64-invalid` + byte offsets, 100 MB / 10 MB caps, all 20 core tests, drop dispatch / decode-to-file / latest-wins. CHANGED (interaction) — live as-you-type conversion (Encode/Decode buttons removed → segmented direction switch), Paste button cut (divergence from 8.1, recorded), Copy → output-panel icon, one priority-ordered contextual slot (error > detection > data-URI offer, never stacked, caption not card), full tokenization pass. ADDED — (1) data URI build + decode with inline image preview, (2) blob detection "looks like X" (JWT/PNG/PDF/gzip/zip/text/unknown), honest-by-construction per AD-9, one caption line, (3) line-wrap on encode (none/64/76). CONTAINER — single enriched view (Option A); tabs and two-pane rejected; `AppTabs` not used. CUT → 3 backlog GitHub issues (hex/radix view, decode-as-gzip, recursive decode) — drafted, **not yet filed**. AD-1 split — core `encode*`/`decode*`/engines/detection/UTF-8 layer/guards unchanged; new pure `parse_data_uri` + `sniff` + `wrap` param; commands change additively (2 new `base64_<verb>`, `wrap` arg on 2 existing). i18n finding — `TRANSLATABLE_CODES` has zero `base64-*` coverage; classify + translate in Task 2. |
- 2026-08-29 — Task 2b **Slice 3 revised after the render review.** The slice-3 contextual line under a still-present, empty ten-row output box read as broken. Ran `/design` (canvas `562ce7ef`, new page "Not-text output", 4 options); developer chose **Option D — auto-preview**. When a decode isn't text the output field is now *removed*, and the space is filled by a reading of the payload: a JWT shows its **header + payload split by default**, headed by "Reading as JWT · Show raw" (collapsible to a one-line "Looks like a JWT"); a PNG blob renders an **inline image preview** (a `data:` URI composed view-side — no command, and the CSP already allows it, which closes AC12's open CSP question); pdf/gzip/zip/unknown keep the single caption line + "Save as file"; a PNG that doesn't actually render falls back to that caption line rather than a broken image. AC9 and AC10 were amended in the AC section to record this (the honesty bar is kept — the JWT reading is framed present-tense as an interpretation, always dismissable, and `sniff`'s JWT test stays strict). No Rust change. Verification green (600 FE tests, Rust unchanged at 90 + 237, lint / `vue-tsc` / build / `cargo fmt`). Stopped for the developer's render review of the revision before slice 4. Not committed.
- 2026-08-29 — Task 2b **Slice 3 shipped** (`sniff` + the one contextual slot — AC9, AC10, AC16). New pure `umbra_core::base64::sniff(&str) -> Result<Sniff, ToolError>` identifies a decoded payload — first match wins in the fixed order **JWT → PNG / PDF / gzip / zip → valid UTF-8 text → unknown**; input that isn't Base64 at all is the `Err`, which the view renders in the error slot (error outranks detection — AC9). New `base64_sniff` `spawn_blocking` command (`Result<_, ToolError>`, AD-3/AD-4). The decode direction now runs `base64_sniff` in place of `base64_decode` — one call yields the text *and* the identification, so the shared latest-wins runner never has two decode calls racing. Under the output there is now exactly one contextual line, caption weight, never stacked: a conversion error, or a detection line, or nothing. A JWT shows "Looks like a JWT · Read as JWT" and the segment split appears **only on confirm** (`sniff` already decoded the header/payload, so the confirm is a pure reveal — no third command). Binary/unknown blobs show "Looks like a PNG file (N)" / "Decoded N — unrecognized" with a "Save as file" action reusing the unchanged decode-to-file flow, and the save dialog now pre-fills a **type-aware** name (`decoded.png` / `.pdf` / `.gz` / `.zip`, else `.bin`) — the slice-2 render-review "Untitled" follow-up, resolved now that the type is known. Plain text gets no line (the output panel is the answer). i18n: 6 new `tools.base64.*` keys (en + fr), `decodedNotText` removed. `crates/umbra-core/src/base64.rs` core transforms, both size caps, and `map_decode_error` are untouched (AD-1). Full verification green (599 FE, 90 core + 237 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, clippy). Stopped for the developer's render review before slice 4 (data-URI build + decode + preview). Not committed.
- 2026-08-29 — Task 2b **Slice 2 revised after the render review.** The developer ran `/design` (canvas `562ce7ef` — reference-matched switch, four auto-looping motion options, alphabet-footprint options, decode-to-file-home options, a composite) and chose: **sliding-thumb** motion; **inline URL-safe checkbox** on the switch row (Option A's placement with Option B's control — one checkbox, "Standard" is the unstated default); **contextual "Save as file"** replacing the stray Decode-to-file button. All three are now implemented. The switch is a grey track with a white raised card that slides under the active half (`transform 140ms`, `prefers-reduced-motion` respected), `→ Encode` / `← Decode` Phosphor arrows, `role="radiogroup"` (no visible legend). The alphabet radio `<fieldset>` (~90px) is gone — one encode-only checkbox on the switch row. "Decode to file" is no longer a standing button: when a live decode yields `base64-not-utf8` (bytes that decoded fine but aren't text), a caption-weight line offers **Save as file**, wired to the unchanged `save()` + `base64_decode_to_file` flow (AC14 preserved — only the trigger moved). Full "looks like PNG / JWT / …" detection and the raw-bytes decode path remain slice 3; this handles the one non-text case detectable with today's commands. There is no longer any `AppButton` in the tool. Verification green (596 FE / 229 + 88 Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`). Stopped for the developer's render review of the revision before slice 3. Not committed.
- 2026-08-29 — Task 2b **Slice 2 shipped** (live conversion + segmented direction switch — AC7, and AC6's segmented-switch element). Conversion is now live: a 200ms-debounced `watch` on `[input, direction, alphabet]` runs `base64_encode` / `base64_decode` with no button press; toggling the Encode ⇄ Decode segmented control re-runs against the current input *immediately* (the debounce is cancelled and `convert()` called directly). The single `createLatestWinsRunner()` is unchanged and still backs every conversion — AD-16, no new runner scope. The Encode/Decode `AppButton`s are gone, replaced by an inline two-radio segmented control (radios kept operable for keyboard/SR, visually collapsed). Per the developer's input-label question, the static "Text or Base64 input" label was replaced with a direction-aware one — "Text to encode" ⇄ "Base64 to decode" (en + fr) — matching the mainstream single-field-converter pattern (jam.dev et al.). **Two judgment calls flagged for the render review:** (1) the URL-safe alphabet selector is now shown only in the Encode direction — decode auto-detects the alphabet, and the legend already reads "used by Encode only"; (2) the Decode-to-file action is shown only in the Decode direction — it has no meaning while encoding. Both follow from making direction the primary axis; neither is spelled out in slice 2's one-line checklist entry. AC6's "exactly three controls with empty input" is closer but not literally satisfied (the encode-direction alphabet selector is a fourth control) — left as later-slice / explicit-design work rather than forced in here. No Rust, command, drop-path, decode-to-file, or size-cap changes (AC14). Verification green: `pnpm lint`, `pnpm test` (594 passed), `vue-tsc --noEmit`, `pnpm build`, `cargo fmt --check`, `cargo test --workspace` (229 + 88 passed). Stopped for the developer's render review before slice 3. Not committed.
- 2026-08-29 — Task 2b **Slice 1 shipped** (tokenize + restructure to the single enriched view — AC6, AC8, partial AC14/AC16). `Base64View.vue` is now fully token-driven: `--color-*` for the drop hint and error text, `--font-code-*` for the in/out panels, `--font-caption-size` / `--radius-*` / `--spacing-*` for layout. Every bare `<button>` is an `AppButton` **default** variant — the signature `primary` (orange) was deliberately not applied (reserved for one true action per screen; if any Base64 control ever earns it, that's a separate conversation, not a reflex). **Paste is gone** (⌘V into the input is the paste path); its handler and the now-unused `readClipboardText` import went with it. **Copy is a 24px text-less icon button** on the output panel (`PhCopySimple` → `PhCheck` on confirm, disabled while output is empty), reusing `useCopyFeedback` from the JSON tool (imported, not modified) and the same signature-accent "copied" feedback — no separate success colour. `AppTabs` is **not** imported (there was no tab bar to begin with; none was added). New i18n key `tools.base64.outputCopied` in `en` + `fr`. `Base64View.spec.ts` extended for the structural changes (Paste absent, Copy is an icon button, no `role="tab"`, three `.actions` `AppButton`s, the copied confirmation) and the stale Paste test + `readText` mock removed — 588 FE tests pass, 229 Rust tests pass, `vue-tsc` / `pnpm build` / `cargo fmt --check` all clean. No Rust, command, drop-path, decode-to-file, alphabet-detection, latest-wins-runner, or size-cap changes (AC14 preserved). Judgment call: the Encode⇄Decode segmented switch + debounced live conversion stay in slice 2 (AC7's scope) — slice 1 keeps Encode/Decode as click-to-convert `AppButton`s. **Stopped for the developer's render review before slice 2.** |

### Change Log

| Date | Change |
| --- | --- |
| 2026-08-30 | **Icon follow-up** (raised by the developer during the code review, outside the story's own AC scope). The Base64 tool's sidebar/grid icon was `PhBinary` — an unrecorded pick from Story 7.1's bulk text→Phosphor migration that reads as base-2, a different encoding. Replaced with a typographic `64` badge (`src/shell/Base64GlyphIcon.vue`, stroked geometry so it stays a true pictogram past the AC5 "not raw text" shell tests), resolved through `icons.ts` like the pictogram entries. Recorded in `DESIGN.md`'s Card note as a deliberate one-off exception to the Phosphor set (the rest of the set stays Phosphor). Touches `src/shell/icons.ts`, `src/shell/Base64GlyphIcon.vue` (new), `DESIGN.md`; 615 FE tests / lint / vue-tsc / build green. Uncommitted. |
| 2026-08-30 | **Code review** (`bmad-code-review`, 3 parallel adversarial layers vs. `b151314..HEAD`). 33 raw findings → 23 after dedup → **4 decision-needed, 8 patch, 6 deferred, 5 dismissed**; no layer failed. All 4 decision-needed resolved with the developer (AC6 / AC14 / AC16 amended in place; DN2 + DN3 became patches). All 10 patches applied: drop-vs-debounce race (`debouncedConvert.cancel()` in the drop watcher); `parse_data_uri` CWE-400 size guard; data-URI PNG preview uses the parsed payload not the raw input; `onSaveAsFile` TOCTOU snapshot; `classify_bytes` returns Text for valid-UTF-8 payloads before the magic-byte arms (AC10 amended, narrow fix); `DATA_URI_MIME_OPTIONS` aligned with `mime_from_path` + `octet-stream` → `bin`; case-insensitive `data:` / `;base64` / `image/`; `dropZone.spec.ts` fixture on the real `Ingest` shape; "Save as file" restored on the text output panel (`decoded.txt`); `sniff_jwt` rejects a non-Base64URL signature segment. Full verification green: `cargo test --workspace` (+4 new Rust tests), `pnpm test` 615 (+1), `cargo fmt` / `clippy --workspace` / `pnpm lint` / `vue-tsc` / `pnpm build` all clean. 6 deferred items appended to `deferred-work.md`. Story `review` → `done`. Review patches are **uncommitted working-tree changes** — not committed or pushed. |
| 2026-08-28 | Task 0 complete: branched `feat/story-8-2-reimagine-base64-encode-decode` from `13c6942`; `baseline_commit` `b151314` verified against `origin/main`. Story moved `ready-for-dev` → `in-progress` in sprint-status.yaml. Task 1 discovery started. |
| 2026-08-29 | Task 1 complete. `bmad-party-mode` discovery (installed roster); 3-container design canvas published as an Artifact; developer chose the single enriched view. Decision record `8-2-base64-decision-record.md` written (kept/changed/added/cut, container, FR10–FR12 revision, i18n finding, AD-1 split). 3 cut-idea GitHub issues filed: #113 (hex view), #114 (decode-as-gzip), #115 (recursive decode), `backlog-candidate` label. AC1–AC4 satisfied. |
| 2026-08-29 | Task 2a complete. Same `bmad-party-mode` room resolved the decision record's open items and wrote AC6–AC16 (container / live conversion / Paste removed + Copy icon / one contextual slot / honest blob detection / data-URI build + decode + preview / line-wrap on encode / i18n + classified codes / architecture compliance) into the story's "Acceptance Criteria — Task 2 (Redesign)" section. Task 2b re-planned into 6 vertical slices with a developer render-review after slice 1. Awaiting developer sign-off on the AC set before 2b. Nothing committed yet (developer wants one commit covering the record + story file). |
| 2026-08-29 | Task 2b **Slice 6** complete — classified error codes + French (AC15). `map_decode_error` split into `base64-invalid-char` / `-invalid-length` / `-invalid-padding` (canned messages, offsets ride `position`); `base64-not-utf8` message also canned. All 5 `base64-*` classification codes in `TRANSLATABLE_CODES`; `base64-input-too-large` stays out. `errors.base64-*` in en + fr. Rust + FE tests retargeted; the AC3 case now asserts the translated text, not the raw Rust message. 614 FE / 96 core + 248 tauri Rust, lint, `vue-tsc`, build, `cargo fmt`, `cargo clippy --workspace` — all green. **All 6 Task 2b slices done.** Not committed; awaiting final render review + `bmad-code-review`. |
| 2026-08-29 | Task 2b complete — all 6 slices implemented, developer render review passed. Story `ready-for-dev` → `review` (story file + `sprint-status.yaml`); implementation committed on `feat/story-8-2-reimagine-base64-encode-decode`. Next: `bmad-code-review` (fresh context, different LLM), then `review` → `done`. |
| 2026-08-29 | Task 2b **Slice 5** complete — line-wrap on encode (AC13). `LineWrap { Col64, Col76 }` enum + `wrap_encoded` helper + `wrap: Option<LineWrap>` on `encode`/`encode_bytes`; commands `base64_encode` / `base64_ingest_file` take it. Inline `None / 64 / 76` `<select>` in the output header, Encode-only, default `None`; a width change re-encodes live; decode stays whitespace-tolerant. The data-URI builder strips wrap so its `data:` string stays one line. i18n `wrapLabel` / `wrapNone` (en + fr). 6 new Rust tests, 3 new FE tests. 614 FE / 96 core + 247 tauri Rust, lint, `vue-tsc`, build, `cargo fmt`, `cargo clippy --workspace` — all green. Stopped for the render review before slice 6. Not committed. |
| 2026-08-29 | Task 2b **Slice 4** complete — data URI build + decode + preview (AC11, AC12). NEW pure `parse_data_uri` (core, 6 tests) + `base64_parse_data_uri` command (`spawn_blocking`, `Result<DataUri, ToolError>`, 2 tests). Encode direction: a collapsed "Build a data URI" disclosure — MIME `<select>` + a live, copyable `data:<mime>;base64,<output>` string (pure view-side); a dropped file pre-selects the MIME (`Ingest::Encoded` now carries `mime`, from `mime_from_path`). Decode direction: `data:`-prefixed input → parse the MIME/payload → inline `<img>` preview for `image/*`, else chain `base64_sniff(payload)`; "Save as file" writes the payload with a MIME-named default (`decoded.pdf`). `base64-data-uri-malformed` → translated (added to `TRANSLATABLE_CODES` + `errors.*`, en + fr). CSP unchanged (already allows `data:`). i18n: 5 `dataUri*` keys (en + fr). 610 FE / 95 core + 243 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, `cargo clippy --workspace` — all green. Stopped for the render review before slice 5. Not committed. |
| 2026-08-29 | Task 2b **Slice 3 revised again** — decode-a-dropped-file. A file drop now follows the direction: `base64_encode_file` → `base64_ingest_file(path, url_safe, decode)` returning `Ingest` (`Encoded` / `Text` / `NotText`). Encode direction → encode the bytes (unchanged). Decode direction → the file's text lands in the input and decodes there; a binary file shows a transient `.drop-notice` ("that file isn't Base64 text — a binary file can only be encoded") and is **not** silently encoded. Drop hint is direction-aware (`dropHintEncode` / `dropHintDecode`). AC14 amended (AD-14 dispatch unchanged; handler + args + direction-awareness are the change). `registry.ts` + `dropZone.spec.ts` follow the handler rename. 605 FE / 92 core + 237 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, `cargo clippy --workspace` — all green. Not committed. |
| 2026-08-29 | Task 2b **Slice 3 revised** (post-render-review). `/design` canvas `562ce7ef` page 3; developer chose Option D. When a decode isn't text the output `<textarea>` is removed and the space is filled: JWT split shown by default under a "Reading as JWT · Show raw" strip; PNG blob → inline `<img>` preview from a view-side `data:` URI (CSP already allows `data:`, closing AC12's open item); other binary → one caption line + Save; `<img>` `@error` → caption fallback. AC9 + AC10 amended in the AC section. No Rust change. i18n `readingAsJwt` / `showRaw` / `imagePreviewAlt` added (en + fr). 600 FE tests, Rust unchanged (90 + 237 + 1), lint / `vue-tsc` / build / `cargo fmt` green. Stopped for the render review before slice 4. Not committed. |
| 2026-08-29 | Task 2b **Slice 3** complete: pure `umbra_core::base64::sniff` (`Result<Sniff, ToolError>`, first-match-wins JWT → PNG/PDF/gzip/zip → UTF-8 text → unknown; 8 core tests incl. the AC10 plain-ASCII regression) + `base64_sniff` `spawn_blocking` command (2 tests, registered). View: decode direction calls `base64_sniff` instead of `base64_decode`; `notText` → `sniff` ref; one caption-weight contextual line under the output (error > detection, never stacked — AC9); "Read as JWT" reveals the pre-decoded header/payload on confirm; binary/unknown offer "Save as file" with a type-aware `defaultPath` (`decoded.png` etc.). 6 new FE tests. i18n: `looksLikeJwt` / `readAsJwt` / `looksLikeBinary` / `decodedUnrecognized` / `jwtHeader` / `jwtPayload` added (en + fr), `decodedNotText` removed. No `toolError.ts` change. 599 FE / 90 core + 237 tauri Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`, clippy — all green. Stopped for the render review before slice 4. Not committed. |
| 2026-08-29 | Task 2b **Slice 2 revised** (post-render-review). `/design` canvas `562ce7ef` published; developer chose sliding-thumb motion, an inline URL-safe checkbox, and a contextual "Save as file". Implemented: the segmented switch is now a grey track + sliding white card (`transform 140ms`, reduced-motion aware), `role="radiogroup"`, Phosphor `→`/`←` arrows; the alphabet radio `<fieldset>` → one encode-only checkbox on the switch row (`urlSafe` boolean ref); the Decode-to-file button → a contextual "Save as file" line shown only when a live decode returns `base64-not-utf8`, wired to the unchanged save flow (AC14). No `AppButton` remains in the tool. Locales: `alphabet*` + `decodeToFile` keys removed; `urlSafeAlphabet` / `decodedNotText` / `saveAsFile` added (en + fr). `Base64View.spec.ts` reworked (checkbox, `reachSaveOffer` helper, offer-vs-alert cases, sliding-thumb class). 596 FE / 229 + 88 Rust tests, lint, `vue-tsc`, build, `cargo fmt --check` all green. No Rust/command changes. Stopped for the render review of the revision. Not committed. |
| 2026-08-29 | Task 2b **Slice 2** complete: live as-you-type conversion (200ms debounced `watch` on input/direction/alphabet) replaces the Encode/Decode buttons; direction is now an inline Encode ⇄ Decode segmented control (native radios, visually collapsed), and toggling it re-runs the conversion immediately. The single `createLatestWinsRunner()` still backs every conversion (AC7 / AD-16). Input label made direction-aware ("Text to encode" ⇄ "Base64 to decode", en + fr) per the developer's research question; `tools.base64.inputLabel` replaced by `inputLabelEncode` / `inputLabelDecode` / `directionLegend`. Judgment calls flagged: URL-safe alphabet selector shown Encode-only, Decode-to-file shown Decode-only. `Base64View.spec.ts` moved to fake timers and reworked for live conversion + new AC6/AC7 cases. Full verification green (594 FE / 229 + 88 Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`). No Rust/command/drop/size-cap changes (AC14). Stopped for the developer's render review before slice 3. Not committed. |
| 2026-08-29 | Task 2b **Slice 1** complete: `Base64View.vue` restructured to the single enriched view and put through the Epic-7 tokenization pass — hardcoded `#666`/`#ccc`/`#b00020` replaced with `--color-*`/`--font-caption-size`; in/out panels adopt `--font-code-*`; layout on `--radius-*`/`--spacing-*`. Bare `<button>`s → `AppButton` (default variant). Paste button removed (⌘V covers it); Copy downsized to a 24px output-panel icon button reusing the JSON tool's `useCopyFeedback` + signature-accent confirm. No `AppTabs`. New key `tools.base64.outputCopied` (en + fr). `Base64View.spec.ts` extended for the structural changes; Paste test + `readText` mock removed. Full verification green (588 FE / 229 Rust tests, lint, `vue-tsc`, build, `cargo fmt --check`). AC14 behaviours (drop dispatch, decode-to-file, alphabet auto-detect, the shared latest-wins runner, 100 MB / 10 MB caps) untouched — no Rust/command changes. Stopped for the developer's render review before slice 2 (segmented switch + live conversion, AC7). Not committed. |

### File List

- `_bmad-output/implementation-artifacts/8-2-reimagine-base64-encode-decode.md` (this story — Task/Dev Agent Record updates)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (status `ready-for-dev` → `in-progress`)
- `_bmad-output/implementation-artifacts/8-2-base64-decision-record.md` (NEW — Task 1 deliverable)
- `src/tools/base64/Base64View.vue` (slices 1–2 — single enriched view, tokenization, live conversion, sliding-thumb switch, inline URL-safe checkbox; slice 3 — `base64_sniff` in the decode path, `sniff` ref, contextual detection; slice 3 revisions — output field removed for non-text decodes, JWT split by default with "Show raw", inline PNG `<img>` preview via a view-side `data:` URI, direction-aware file drop + `.drop-notice`)
- `src/tools/base64/Base64View.spec.ts` (slices 1–2 — structural + fake-timer live-conversion cases; slice 3 + revisions — `base64_sniff` mocks, JWT-split / "Show raw" / PNG-preview / `<img>`-error fallback / error-outranks-detection / type-aware-save / `Ingest`-shaped drop / decode-a-dropped-file / binary-drop-notice cases)
- `src/locales/en.json` (slice 3 — detection/JWT keys, `decodedNotText` removed; revisions — `readingAsJwt` / `showRaw` / `imagePreviewAlt`, `dropHint` → `dropHintEncode` + `dropHintDecode` + `fileNotBase64Text`; slice 4 — `dataUriImageLine` / `dataUriBuilder` / `dataUriMimeLabel` / `dataUriCopied` + `errors.base64-data-uri-malformed`; slice 5 — `wrapLabel` / `wrapNone`; slice 6 — `errors.base64-invalid-char` / `-invalid-length` / `-invalid-padding` / `-not-utf8`; earlier slices — `outputCopied`, `inputLabel*`, `directionLegend`, `urlSafeAlphabet`, `saveAsFile`)
- `src/locales/fr.json` (same key set as en.json)
- `crates/umbra-core/src/base64.rs` (slice 3 — NEW `Sniff` + `sniff`; slice 4 — NEW `DataUri` + `parse_data_uri`; slice 5 — NEW `LineWrap` + `wrap` param on `encode`/`encode_bytes`; slice 6 — `map_decode_error` split into classified `base64-*` codes, `decode`'s `base64-not-utf8` message canned)
- `crates/umbra-core/src/jwt.rs` (slice 5 — test helpers thread the new `encode_bytes` `wrap` arg)
- `src-tauri/src/commands/base64.rs` (slice 3 — NEW `base64_sniff`; slice 3 revision — `base64_ingest_file` + `Ingest`; slice 4 — NEW `base64_parse_data_uri` + `mime_from_path` + `Ingest::Encoded.mime`; slice 5 — `wrap` param on `base64_encode` / `base64_ingest_file`)
- `src-tauri/src/commands/jwt.rs` (slice 5 — test helper threads the `encode_bytes` `wrap` arg)
- `src-tauri/src/lib.rs` (slice 3 — `base64_sniff`; slice 3 revision — `base64_ingest_file`; slice 4 — `base64_parse_data_uri` — all in the `use` + `generate_handler!`)
- `src/shell/toolError.ts` (slice 4 — `base64-data-uri-malformed`; slice 6 — `base64-invalid-char` / `-invalid-length` / `-invalid-padding` / `-not-utf8` added to `TRANSLATABLE_CODES`)
- `src/stores/registry.ts` (slice 3 revision — base64 drop handler string `base64_encode_file` → `base64_ingest_file`)
- `src/shell/dropZone.spec.ts` (slice 3 revision — base64 fixture + assertion follow the renamed handler; generic dispatch unchanged)

### Review Findings

Code review 2026-08-29 (`bmad-code-review`, three parallel adversarial layers — Blind Hunter,
Edge Case Hunter, Acceptance Auditor — against `b151314..HEAD`). 33 raw findings → 23 after
dedup → 4 decision-needed, 8 patch, 6 deferred, 5 dismissed as noise. No review layer failed.

**Decision-needed** (resolved 2026-08-29 with the developer):

- [x] [Review][Decision] AC6 "exactly three controls visible with empty input" is not met in the Encode direction (URL-safe checkbox, wrap `<select>`, disabled Copy, "Build a data URI" disclosure are also visible). **Resolved: accept & amend AC6** — dated `*Amended*` note added to AC6 (Decode shows three; Encode adds the alphabet / wrap / copy / data-URI-builder affordances as accepted low-noise standing controls; the "and `AppButton`" clause relaxed for the icon/link `<button>`s). No code change.
- [x] [Review][Decision] "Base64 → file via the save dialog" is unreachable when the decoded output is text — `onSaveAsFile` (`Base64View.vue:458`) is wired only into `detectionMode === 'image'` / `'note'`. **Resolved: add "Save as file" to the text output panel** — see patch below (reuses the existing `base64_decode_to_file` flow).
- [x] [Review][Decision] JWT detection — `sniff_jwt` (`crates/umbra-core/src/base64.rs:270`) never validates the 3rd (signature) segment, so `b64(header).b64(payload).<garbage>` reads as a JWT. **Resolved: tighten the signature check** — see patch below (require a non-empty base64url signature, still allow the empty sig for `alg:none`). The near-miss error message is left as-is.
- [x] [Review][Decision] AC14 / AC16 clauses violated by mechanically-forced changes. **Resolved: amend the AC text** — dated `*Amended*` notes added to AC14 (rewritten test assertions, the `wrap` drop-arg, and the DN2 save-as-file follow-up) and AC16 (`registry.ts`, `dropZone.spec.ts`, `jwt.rs`×2 test-helper edits). No code change.

**Patch**:

- [x] [Review][Patch] (from DN2) Add a "Save as file" action to the text output panel — when a decode yields `Sniff::Text` the output panel currently offers only Copy; wire a "Save as file" action (plain text-link style, matching `.offer-action`) that reuses `onSaveAsFile` / `base64_decode_to_file` with a `decoded.txt` default name, restoring the AC14 "Base64 → file" preserved behaviour for text results. [src/tools/base64/Base64View.vue:573]
- [x] [Review][Patch] (from DN3) Require a non-empty base64url signature segment in `sniff_jwt` — after the 3-segment / `alg` checks, reject the token unless `segments[2]` is empty (the `alg:none` case) or is itself valid base64url; keeps `b64(header).b64(payload).<garbage>` from being read as a JWT. Add a regression test. [crates/umbra-core/src/base64.rs:270]

- [x] [Review][Patch] A file dropped within 200 ms of a keystroke has its result wiped ~200 ms later — the `registry.dropResult` watcher never calls `debouncedConvert.cancel()`, so a debounce scheduled by the last keystroke fires after the drop and re-runs `convert()` against the now-empty input, clearing `output` / `error` / `dropNotice`; `suppressConvertOnce` only neutralises the synchronous watcher run, not the pending timer. Fix: `debouncedConvert.cancel()` at the top of the drop watcher. [src/tools/base64/Base64View.vue:406]
- [x] [Review][Patch] `parse_data_uri` has no size guard — a `data:`-prefixed paste bypasses `MAX_INPUT_BYTES` (100 MB, CWE-400); `sniff` → `decode_bytes` calls `check_size` but the data-URI branch (`Base64View.vue:316` → `base64_parse_data_uri` → `parse_data_uri`) does full-size `String` copies and builds a `data:` `<img src>` unbounded. Fix: `check_size(input.len())?` at the top of `parse_data_uri`. [crates/umbra-core/src/base64.rs:185]
- [x] [Review][Patch] `imageDataUri` builds a malformed `<img src>` for a PNG delivered through a non-image data URI (`data:application/octet-stream;base64,<png>`) — `detectionMode` returns `"image"` off the bare `sniff.kind === "png"` branch, but `imageDataUri` then prefixes the whole `data:…` input string (mangling `-` → `+`), producing `data:image/png;base64,data:application/octet+stream;…`; the `<img>` errors and the UI degrades to the note. Fix: when `dataUri.value` is set use `dataUri.value.payload`, or only return `"image"` from the bare-`sniff` branch when `dataUri.value === null`. [src/tools/base64/Base64View.vue:232]
- [x] [Review][Patch] `onSaveAsFile` reads `saveSource.value` after `await save(...)` — if a pending debounce fires while the native save dialog is open, `base64_decode_to_file` is invoked with the new input while the filename still reflects the old detection. Fix: snapshot `saveSource.value` (and the name) into locals before the `await`. [src/tools/base64/Base64View.vue:458]
- [x] [Review][Patch] (was P5) `classify_bytes` reported *valid text* beginning `%PDF` (or `PK\x03\x04`) as `Sniff::Pdf` / `Sniff::Zip`, hiding the text behind the "looks like a … — Save as file" note. **Resolved 2026-08-30 with the developer: narrow fix (option c).** `classify_bytes` now returns `Sniff::Text` immediately when the payload is valid UTF-8; the PNG / PDF / gzip / zip magic-byte arms run only on the non-UTF-8 branch, so real files (non-text past the header) still classify correctly while `%PDF`-prefixed plain text lands on Text — AC10's own incidental-collision principle. AC10 amended in place; `sniff_identifies_png_pdf_gzip_and_zip_by_magic_bytes` fixtures updated to carry a non-UTF-8 byte (as every real file of those types does); new test `sniff_reads_text_that_merely_starts_with_a_magic_signature_as_text`. [crates/umbra-core/src/base64.rs:319]
- [x] [Review][Patch] MIME tables are out of sync — `mime_from_path` (`src-tauri/src/commands/base64.rs:77`) can return `application/zip` / `application/gzip` / `application/wasm` / `font/woff2`, none of which are in `DATA_URI_MIME_OPTIONS`, so dropping a `.zip`/`.gz`/`.wasm`/`.woff2` file never pre-selects the builder MIME (partial AC11 miss); and `mimeToExt("application/octet-stream")` returns `"octet-stream"` → `decoded.octet-stream`. Fix: add the missing types to `DATA_URI_MIME_OPTIONS` and special-case `application/octet-stream` → `bin`. [src/tools/base64/Base64View.vue:42]
- [x] [Review][Patch] `data:` scheme and `image/` MIME are matched case-sensitively — `parse_data_uri`'s `strip_prefix("data:")` (`base64.rs:194`), the view's `value.trim().startsWith("data:")` (`Base64View.vue:316`) and `du.mime.startsWith("image/")` (`:325`); RFC 2397 / 2045 make both case-insensitive, so `DATA:` / `IMAGE/PNG` fall through to a confusing `base64-invalid-char` error. Fix: compare case-insensitively. [crates/umbra-core/src/base64.rs:194]
- [x] [Review][Patch] `dropZone.spec.ts:234` asserts an outdated `base64_ingest_file` contract — `toHaveBeenCalledWith("base64_ingest_file", { path, url_safe: true })` (no `decode` / `wrap`) and `dropResult` `value: "//4AAQ=="` (a bare string, not the `Ingest::Encoded { value, mime }` shape). Passes only because the mock is hand-fed. Fix: update the fixture to the real arg + `Ingest` shape. [src/shell/dropZone.spec.ts:234]

**Deferred** (also recorded in `deferred-work.md`):

- [x] [Review][Defer] Decode-error byte offsets are relative to the whitespace-stripped string, not the user's input [crates/umbra-core/src/base64.rs:103] — deferred, pre-existing (both the strip and the `ByteOffset` render predate this story; live rendering makes it more visible).
- [x] [Review][Defer] `base64_sniff` runs a full decode (up to three, plus a JSON pretty-print, for dotted input) on every debounced keystroke, up to `MAX_INPUT_BYTES` (100 MB); `createLatestWinsRunner` discards stale results, not the work [src/tools/base64/Base64View.vue:342] — deferred, new with live conversion (AC7) but academic at realistic paste sizes; `spawn_blocking` keeps the UI thread free.
- [x] [Review][Defer] `base64_decode` command + `umbra_core::base64::decode` are orphaned (nothing in `src/` invokes `base64_decode`; the live path is `base64_sniff`), so `base64-not-utf8` is never produced by the UI despite slice 6 adding its translation + `TRANSLATABLE_CODES` entry [src-tauri/src/commands/base64.rs:49] — deferred, dead-but-harmless surface; remove or record why it is kept.
- [x] [Review][Defer] `suppressConvertOnce` is a single one-shot boolean whose correctness depends on Vue coalescing the drop handler's `input` + `direction` writes into one watcher tick [src/tools/base64/Base64View.vue:123] — deferred, no current defect; a generation/request-id counter would be sturdier.
- [x] [Review][Defer] `convert()` resets `jwtCollapsed = false` on every run, so a "Show raw" collapse is lost on every subsequent keystroke while editing a JWT [src/tools/base64/Base64View.vue:294] — deferred, minor UX; consider preserving it while the sniff result stays a JWT.
- [x] [Review][Defer] `useCopyFeedback` is imported cross-tool from `../json/` — a base64 → json source coupling [src/tools/base64/Base64View.vue:17] — deferred, the Dev Agent Record already flags it as "a candidate to hoist to `src/shell/`"; hoist if a third tool needs it.

**Dismissed as noise** (5): French `looksLikeBinary` `{type}` interpolates a hardcoded English literal — by design, format names (PNG/PDF/gzip/ZIP) are not localized. · Unpadded Base64 in the PNG `<img>` `data:` URI relies on WKWebView leniency — the `@error` → note fallback handles the failure. · Whitespace-only decode input → empty output panel with no message — consistent with the tool's documented whitespace-tolerance; pre-existing. · AC6 "every bare `<button>` → `AppButton`" unmet — the remaining `<button>`s are icon / text-link affordances (`.copy-button`, `.disclosure`, `.offer-action`) that `AppButton` would be wrong for; `JsonTree.vue`'s bare copy buttons are the precedent. · `.drop-notice` renders as a tinted card — it sits above the input, outside AC9's "one contextual slot under the output" scope; a transient status is deliberately more prominent.
