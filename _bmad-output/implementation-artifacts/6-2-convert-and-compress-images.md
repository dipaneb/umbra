---
baseline_commit: 97d34d48f463f7809143aea032ccaf6a1b956a5c
---

# Story 6.2: Convert and compress images

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a privacy-conscious developer,
I want to convert images between formats and compress them with a quality preview,
so that I can prepare assets without an online converter.

## Acceptance Criteria

1. **Given** a dropped image, **when** I choose a target format among PNG/JPEG/WebP/HEIC, **then** the converted file is produced and saved via the shared save helper (FR28, AD-15).
2. **Given** a lossy target format, **when** I adjust the quality slider, **then** an estimated output size displays before saving, updating with the slider (FR28), **and** the estimate is computed off the UI thread with latest-wins on rapid changes (AD-4, AD-16).
3. **Given** HEIC support, **when** the crate route is evaluated in this story, **then** decode/encode support is verified against AD-11's three-platform compile gate, **and** if HEIC cannot clear that bar, the limitation is surfaced as an explicit scope decision — never silently dropped (NFR3).
4. **Given** an unreadable or unsupported image, **when** conversion is attempted, **then** a structured `ToolError` displays inline (NFR4).

**Interpretation notes, recorded explicitly rather than left implicit:**
- **AC1's "dropped" does not mean wired through the AD-14 window-level drop dispatcher.** Same reasoning Story 6.1 already recorded for PDF input (see its Dev Notes' "Why PDF input bypasses the AD-14 drop dispatcher"): AD-14's dispatcher forwards one path to one fixed registry-declared command, but this story needs a target-format choice and a quality value supplied *before* the operation runs — a bare drop event can't carry that. This story uses `@tauri-apps/plugin-dialog`'s `open()` file picker instead, the same already-used, zero-AD-14-change mechanism Story 6.1 established. Window-level drag-and-drop of a non-PDF image onto the Bucket while its OCR drop-zone is showing still routes to `bucket_extract_text` as today — unaffected by this story.
- **AC2's "a lossy target format" is JPEG only, not JPEG+WebP, given the crate this codebase already depends on.** `image` 0.25.9 (already a workspace dependency, pinned in `Cargo.lock`, used today by `ocr.rs`) ships a `WebPEncoder` that is **lossless-only** — verified against `docs.rs/image/0.25.9`: `WebPEncoder` exposes exactly two public constructors (`new_lossless` and the trait-provided `encode`), no quality parameter, and its own docs state "Right now only lossless encoding is supported." `JpegEncoder::new_with_quality(w, quality: u8)` (1–100) is the only encoder among the four target formats this story's ACs name that actually takes a quality knob. So: PNG and WebP are valid conversion *targets* (FR28's "convert between" clause), always encoded losslessly, with no quality slider shown for them; the quality slider (AC2) applies to JPEG only. This is a precise reading of AC2's own "given a lossy target format" conditional, not a scope reduction — see Dev Notes for the full verification trail.
- **AC3's HEIC evaluation was performed during story creation, not deferred untouched to the developer** — see Task 1, which is the load-bearing task of this story. All three realistic Rust HEIC crate routes investigated in this session carry a real, currently-unresolved blocker (license, publish/maturity, or AD-11 three-platform CI compile risk). The recommended default is to descope HEIC from v1 and record that decision explicitly, per AC3's own wording — but Task 1 requires a live re-verification before locking that in, since this is exactly the kind of pre-1.0/fast-moving dependency landscape the spine's "Dependency version/API drift" convention exists for.

**Not the first story of Epic 6** — Story 6.1 (`6-1-work-pdfs-locally-merge-split-extract`) is `done`, merged to `origin/main` as `97d34d4` (PR #61, squash-merged). `epic-6` stays `in-progress` (no epic-status transition needed).

## Tasks / Subtasks

- [x] **Task 0: Branch setup (AC: all)**
  - [x] `baseline_commit` (`97d34d4`) is `origin/main`'s real tip, confirmed via `git fetch origin` + `git log --oneline origin/main -1` this session, showing Story 6.1's PR #61 squash-merged as `97d34d4`. Content-diff-verified: this session's own `feat/story-6-1-...` branch tip (`b1fb214`) against `origin/main` produced an **empty `git diff --stat`** — content-identical, confirming the squash-merge captured everything, per this project's established squash-merge-verification method (raw SHA ancestry is not trustworthy here — `git merge-base --is-ancestor HEAD origin/main` returns false even though the content is identical, because a squash merge creates a new commit with no parent link back to the source branch).
  - [x] **Local `main` is still stale, same situation as Story 6.1 documented — do not assume it has caught up.** `git merge-base --is-ancestor main origin/main` and the reverse both return false this session: local `main` (tip `66429c5`) and `origin/main` (tip `97d34d4`) have diverged, not just drifted. Local `main` is still missing PRs #59/#60/#61. **Branch directly from `origin/main`:** `git fetch origin && git checkout -b feat/story-6-2-convert-and-compress-images origin/main`. Confirmed already done this session: current branch is `feat/story-6-2-convert-and-compress-images`, and `git merge-base --is-ancestor 97d34d4 HEAD` returns true.
  - [ ] Optional cleanup, not a blocker for this story: resync local `main` to `origin/main` (`git checkout main && git reset --hard origin/main`) — destructive per this project's own safety rule (`CLAUDE.md`), confirm with the developer first, same caveat Story 6.1 recorded and left unresolved.

- [x] **Task 1: HEIC crate decision (AC3) — do this first, it can reshape Task 2's scope**
  - [x] **Re-verify live before trusting the findings below** — this story's own research is a starting point, not a substitute, per `ARCHITECTURE-SPINE.md`'s "Dependency version/API drift" convention (any pre-1.0 dependency or a landscape that could move between story drafting and implementation needs a fresh check). Three candidate routes were investigated this session:
    1. **`libheif-rs`** (MIT, latest 2.7.0, requires Rust ≥1.82) — a safe FFI wrapper around `libheif-sys`. By default requires a **system-installed libheif** (not present on any of `ubuntu-latest`/`windows-latest`/`macos-latest`'s default images), which would break AD-11's three-platform CI compile gate outright. It offers an `embedded-libheif` feature to vendor-build libheif from source instead — but libheif's own real codec dependencies for HEVC (the codec HEIC actually uses) are `libde265` (LGPL-2.1, decode) and, for encoding, `x265` (GPL, or a paid commercial license) — both copyleft, which the spine's own dependency-hygiene convention (`ARCHITECTURE-SPINE.md` Consistency Conventions table: "copyleft/GPL needs explicit review") flags as needing a deliberate decision, not a default `cargo add`. Building `x265`/`libde265` from source on `windows-latest` in CI (needs a C/C++ toolchain, cmake, and nasm, none guaranteed present) is also a real risk to the 20-minute CI job timeout (`ci.yml`'s `timeout-minutes: 20`).
    2. **`heic` (Imazen)** — pure Rust, no C/C++ dependency, SIMD-accelerated, decode-only (118/162 HEIF conformance files, 49/49 HEVC conformance vectors passing per its own published results). **Disqualifying: dual-licensed `AGPL-3.0-only OR LicenseRef-Imazen-Commercial`.** AGPL is a strong copyleft license the spine's dependency-hygiene convention explicitly calls out; bundling it into Umbra's All-Rights-Reserved app without purchasing Imazen's commercial license would put the app's own distribution in violation of AGPL's terms. Buying a commercial license is a real option but is a business/cost decision outside a coding story's scope — not something to decide silently here.
    3. **`heic_decoder` (ente-io)** — pure Rust, decode-only, `image` crate integration hooks. **Disqualifying: not published to crates.io** (a direct crates.io API lookup for `heic-decoder` returned 404; its own README instructs installing it as a local **path** dependency, not a versioned registry dependency) and its license could not be confirmed from the repo in this session. The spine's dependency-hygiene convention requires a license check before adding any dependency — an unpublished, unlicensed-per-this-check crate fails that bar regardless of its technical merits.
  - [x] **None of the three cleanly clears both AD-11's three-platform compile gate and the spine's dependency-hygiene bar within v1 scope.** Recommended decision, to confirm or override after the live re-check above: **descope HEIC from v1** — ship PNG/JPEG/WebP conversion only, and record this explicitly (in this story's Dev Notes/Change Log and, if `FR28`/`epics.md` cite HEIC as already-delivered scope anywhere reader-facing, flag that too) rather than silently shipping three formats and letting "HEIC" quietly vanish from what anyone reviewing the PR expects. This satisfies AC3's own wording exactly ("if HEIC cannot clear that bar, the limitation is surfaced as an explicit scope decision — never silently dropped") — AC3 does not require HEIC to ship, it requires the evaluation and, if HEIC is cut, an explicit record of why.
  - [x] **Live re-check performed this session (2026-08-10) via direct crates.io API calls — landscape unchanged, decision confirmed:**
    - `libheif-rs`: still `2.7.0`, MIT, still depends on `libheif-sys` (the FFI binding to system/vendored libheif, which for HEVC encode/decode still pulls in `libde265` (LGPL-2.1) and `x265` (GPL/commercial) as real codec backends) — the AD-11 CI compile-gate risk stands unchanged.
    - `heic` (Imazen): still `0.1.6`, license still `AGPL-3.0-only OR LicenseRef-Imazen-Commercial` — still disqualifying for an All-Rights-Reserved bundle absent a purchased commercial license.
    - `heic-decoder`/`heic_decoder` (ente-io): still returns `404` from the crates.io API under both name spellings — still unpublished.
    - Also cross-checked `image` (this story's actual dependency): crates.io now shows `0.25.10` upstream (`Cargo.lock` still pins `0.25.9`) — a patch-level bump only, no API change relevant to this story's `WebPEncoder`/`JpegEncoder` usage, consistent with the story's own note that this doesn't need re-reading unless implementation hits something version-specific (it didn't).
    - **Decision confirmed: HEIC descoped from v1.** No new crate added; `TargetFormat` ships as `{ Png, Jpeg, WebP }` only, per Task 2.
  - [x] If a live re-check finds the landscape has changed (e.g. `heic_decoder` published to crates.io with a confirmed permissive license, or `libheif-rs`'s embedded build proves practical and the GPL/LGPL codec dependency gets an explicit, deliberate sign-off), HEIC can be added as a decode-only *source* format (convert HEIC → PNG/JPEG/WebP) — encoding *to* HEIC is out of scope regardless of which crate is chosen, since all three viable candidates found are decode-only. Do not silently upgrade scope either direction without recording the reasoning the same way this task does.

- [x] **Task 2: Core image module — `crates/umbra-core/src/image_convert.rs` (AC1, AC2, AC4)**
  - [x] **Module name is deliberately not `image.rs`, unlike the Structural Seed's own `[ASSUMPTION: module names]` placeholder** (`ARCHITECTURE-SPINE.md:207`, `src/pdf.rs, image.rs # Epic 6`). `umbra-core` already depends on the external crate literally named `image` (`Cargo.toml`, used today by `ocr.rs`). A local `pub mod image;` declared in `lib.rs` collides with that extern crate at the crate root in Rust 2018+/2024 edition path resolution (both occupy the same root namespace) — this is a real compile error, not a style preference, confirmed by Rust's own module-resolution rules (extern crate names and top-level `mod` declarations share one namespace at the crate root). Use `image_convert.rs` / `pub mod image_convert;` instead. Update the Structural Seed's placeholder to match once implemented, per Story 6.1's own "keep documented facts truthful" precedent for its `pdf.rs` resolution.
  - [x] `pub const MAX_INPUT_BYTES: usize = 100 * 1024 * 1024;` — same cap, same CWE-400 rationale, as `ocr.rs`/`pdf.rs`'s own constants. No new combined-total cap; single-file cap only, matching every other multi-format command in this codebase.
  - [x] `pub enum TargetFormat { Png, Jpeg, WebP }` (extend only if Task 1's live re-check changes the HEIC decision — do not add a `Heic` variant speculatively).
  - [x] `pub fn convert(bytes: &[u8], target: TargetFormat, quality: u8) -> Result<Vec<u8>, ToolError>` — decode via `image::load_from_memory` (format-sniffed, the exact pattern `ocr.rs::extract_text` already uses), then encode to `target`:
    - `Jpeg`: validate `(1..=100).contains(&quality)` first — an out-of-range value is a `ToolError` (recommended code: `bucket-image-invalid-quality`), core-layer-authoritative per AD-1 (mirrors `pdf.rs::extract_page_range`'s own core-layer range validation precedent — a frontend slider `min`/`max` is a UX nicety, not a substitute). Encode via `JpegEncoder::new_with_quality(&mut buf, quality)`.
    - `Png`/`WebP`: `quality` is accepted but ignored (both are lossless-only through this crate, per this story's own AC2 interpretation note above) — document this in a doc comment on the function, not just the story file, so a future reader isn't surprised the parameter is a no-op for two of the three variants.
  - [x] `pub fn estimate_size(bytes: &[u8], target: TargetFormat, quality: u8) -> Result<u64, ToolError>` — implement as `Ok(convert(bytes, target, quality)?.len() as u64)`. This is a deliberate reuse, not a placeholder: it gives an exact real byte count (the actual encode already ran) rather than inventing and maintaining a separate size-estimation heuristic that could drift from what `convert` actually produces. Do not build a second estimation code path.
  - [x] Error mapping: undecodable/corrupt input → `ToolError` (recommended code: `bucket-image-unsupported-format`, distinct from OCR's existing bare `bucket-unsupported-format` — follow Story 6.1's own `bucket-pdf-*` precedent for a feature-scoped prefix, here `bucket-image-*`) — never a panic (AC4, NFR4). An encode-side failure (encoder returns `Err`, e.g. from a pathological input `image::load_from_memory` decoded but the target encoder rejects) → `ToolError` (recommended code: `bucket-image-encode-failed`).
  - [x] Unit tests in `image_convert.rs` itself, real fixtures generated in-test (e.g. a small `image::RgbImage` built with `image::ImageBuffer::from_fn` or similar, encoded to bytes at test setup) rather than committed binary fixtures — same reasoning Story 6.1 used for its in-test-generated PDF fixtures via `lopdf`'s own builder API. Cover: PNG→JPEG at a given quality decodes back to a valid JPEG of the expected dimensions; JPEG quality 1 produces smaller output than quality 100 for the same source (proves the quality parameter is actually wired, not ignored); an out-of-range quality (0, 101) with `target: Jpeg` returns `bucket-image-invalid-quality`; the same out-of-range quality with `target: Png`/`WebP` succeeds (quality genuinely ignored for those targets, not silently validated against JPEG's range); `estimate_size` returns exactly `convert(...)?.len()` for the same inputs (proves the reuse, not a coincidence); undecodable bytes (`b"not an image"`) and a truncated/corrupted real image fixture (same truncation-test shape as `ocr.rs`'s own corrupt-image test) both return `bucket-image-unsupported-format`, never panic.

- [x] **Task 3: Tauri commands — `src-tauri/src/commands/image.rs` (AC1, AC2)**
  - [x] `commands::image` (nested under `commands::`, not crate-root) does **not** collide with the external `image` crate the way `crates/umbra-core/src/image.rs` would have — Task 2's naming constraint is specific to `umbra-core`'s crate root; `src-tauri`'s `commands/image.rs` is fine as named, matching `commands/pdf.rs`'s naming precedent. Registered as `pub mod image;` in `src-tauri/src/commands/mod.rs` and its commands added to `src-tauri/src/lib.rs`'s `tauri::generate_handler![...]` list.
  - [x] Command names use the `bucket_` tool prefix per AD-3, same as the PDF commands:
    - `bucket_convert_image(path: String, target_format: String, quality: u8, output_path: String) -> Result<(), ToolError>` — **AD-15: `output_path`, never returned bytes**, same pattern `bucket_merge_pdfs`/`bucket_extract_pdf_pages` already established (a converted image can exceed the ~64KB JSON-IPC ceiling). `target_format` arrives as a string (`"png"`/`"jpeg"`/`"webp"`) and is parsed to `umbra_core::image_convert::TargetFormat` in this command layer — an unrecognized string is a `ToolError` (recommended code: `bucket-image-invalid-target-format`), not a panic or a silent default.
    - `bucket_estimate_image_size(path: String, target_format: String, quality: u8) -> Result<u64, ToolError>` — returns a bare `u64` over IPC (small, well under AD-15's ~64KB concern — same reasoning `bucket_extract_pdf_text` returning `String` already established for "this result is realistically small").
  - [x] Each command: `spawn_blocking` (AD-4 — image decode/encode is real CPU work, same class as every other file-processing command here), `check_file_size`-equivalent guard before reading (mirror `bucket.rs`'s existing `check_file_size`/`pdf.rs`'s equivalent — reuse `umbra_core::image_convert::MAX_INPUT_BYTES`), read via `crate::fs_helper::read_file_bytes`, call the matching `umbra_core::image_convert::*` function, write output via `crate::fs_helper::write_file_bytes` for `bucket_convert_image`.
  - [x] `map_join_error` boilerplate copied per-file, matching every other command file's own local copy (this codebase's established non-shared-helper convention, reconfirmed by Story 6.1's `pdf.rs`).
  - [x] Integration tests, direct async-fn calls (no `AppHandle` needed — none of these two commands touch Tauri-managed resources, same shape as `pdf.rs`'s own tests, not `bucket.rs`'s OCR-command shape). Cover: end-to-end convert against a real temp file across all three target formats; `bucket_estimate_image_size` returns a value matching a direct `image_convert::estimate_size` call on the same input; oversized-file rejection before reading; missing-path → `file-read-error`; invalid `target_format` string → `bucket-image-invalid-target-format`.

- [x] **Task 4: Frontend — extend `src/tools/bucket/BucketView.vue` with an Image section (AC1, AC2)**
  - [x] Add a third, disjoint section — "Image tools" — alongside the existing OCR section and Story 6.1's PDF section. One file picker (`open({ filters: [{ name: "Image", extensions: ["png", "jpg", "jpeg", "webp"] }] })` from `@tauri-apps/plugin-dialog`, already imported in this file), a target-format select (PNG/JPEG/WebP — HEIC only if Task 1's live re-check overturns the descope recommendation), a quality slider (`<input type="range" min="1" max="100">`, default `80` — no source specifies a default; 80 is this story's own reasonable choice, recorded here rather than left an unexplained magic number) shown/enabled **only when the selected target is JPEG**, matching AC2's "given a lossy target format" conditional precisely — do not show or wire the slider for PNG/WebP targets.
  - [x] **Live size estimate:** a `watch()` on `[imagePath, imageTargetFormat, imageQuality]` that, whenever `imagePath` is set, calls `bucket_estimate_image_size` — **debounce the watcher itself (e.g. 200ms) before invoking**, not just relying on the AD-16 latest-wins runner: a runner resolves *out-of-order results*, it does not reduce *invocation volume*, and an undebounced slider drag would fire one `spawn_blocking` image-encode per input event. Route the debounced call through its own local `createLatestWinsRunner()` (see AD-16 scoping note below).
  - [x] **Convert action:** a button that calls `save()` for the output path, then `invoke("bucket_convert_image", { path: imagePath, targetFormat: imageTargetFormat, quality: imageQuality, outputPath })`, routed through a **second, separate** local `createLatestWinsRunner()` from the estimate's.
  - [x] **AD-16 runner scoping — this is a third instance of the spine's own amendment shape, and needs finer granularity than Story 6.1's PDF section used.** The PDF section shares one runner across its three actions because they're discrete, non-overlapping button clicks (`ARCHITECTURE-SPINE.md`'s amendment, `Base64View.vue`'s three-manual-actions precedent). The Image section's live estimate is different in kind: it can fire many times per second while the user drags the slider, and a Convert click can land *during* that stream. Sharing one runner between "the live preview" and "the actual save-to-disk conversion" risks a fast-moving slider tick superseding — and silently discarding the result of — a Convert the user just triggered, or vice versa marking the just-clicked Convert's own eventual result as stale relative to a slider tick that landed a moment before. Two local runners — one scoped to the estimate stream, one scoped to the discrete Convert action — keeps these genuinely independent, matching `CronView.vue`'s "two disjoint state-groups get two runners" reasoning, not `Base64View.vue`'s "three same-shape actions share one" reasoning. State: `imageEstimatedSize: Ref<number | null>`, `imageConverting: Ref<boolean>`, `imageEstimating: Ref<boolean>`.
  - [x] Error rendering: reuse the existing `toToolError`/`ToolError` pattern already established in this file for the OCR and PDF sections — one more `role="alert"` block for the Image section's own error ref (e.g. `imageError`), not a new pattern.
  - [x] Display the estimated size in a human-readable form (e.g. `${(imageEstimatedSize / 1024).toFixed(1)} KB`) — no existing formatting helper for byte counts exists in this codebase (confirmed by reading `BucketView.vue`, `Base64View.vue`, `HashView.vue`); a small local formatting function in this file is the right size for this need, not a new shared utility module (no other tool currently needs one).
  - [x] No changes needed to `src/shell/DropZone.vue` or `src/shell/dropZone.ts`, same reasoning as Story 6.1's PDF section (see this story's own Interpretation notes above).

- [x] **Task 5: Tests (AC1, AC2, AC4)**
  - [x] `crates/umbra-core/src/image_convert.rs` unit tests — see Task 2's own test list.
  - [x] `src-tauri/src/commands/image.rs` integration tests — see Task 3's own test list.
  - [x] Frontend: extend `src/tools/bucket/BucketView.spec.ts` with the Image section's flows, mocking `@tauri-apps/plugin-dialog`'s `open`/`save` and `@tauri-apps/api/core`'s `invoke` — same established mocking shape this file already uses for the PDF section (Story 6.1) and `Base64View.spec.ts` uses for `save()`. Cover: picking a file and switching target format toggles the quality slider's visibility/enabled state correctly (JPEG shows it, PNG/WebP don't); changing quality while JPEG is selected triggers a (mocked, debounced — use fake timers) call to `bucket_estimate_image_size` and updates the displayed estimate; clicking Convert calls `save()` then `bucket_convert_image` with the current path/format/quality/outputPath; an estimate error and a convert error both render via the section's own `imageError` alert without disturbing the other section's state (OCR/PDF) — regression-guarding the AD-16 scoping this task's design depends on.

- [x] **Task 6: Docs sync**
  - [x] `docs/release-checklist.md`'s Bucket exercise-list bullet (already extended once for Story 6.1's PDF operations): extend again to name image conversion (convert a PNG to JPEG with a quality change, confirm the live estimate updates) as part of the nettop tour — same reasoning as Story 6.1's own Task 6: new command surface on an existing registry entry, and NFR1's "every tool exercised" guarantee is only as good as this list reflecting what the tool actually does.
  - [x] Optional, low-risk: add `"image"`, `"convert"`, `"compress"` to the `bucket` entry's `aliases` array in `src/stores/registry.ts` (currently `["bucket", "ocr", "screenshot", "pdf", "merge"]`) for ⌘K discoverability. **Do not add `"heic"`** unless Task 1's decision actually ships HEIC support — an alias implying a capability the tool doesn't have would be its own small honesty violation, the same spirit as AC3's "never silently dropped" but in the opposite direction (never silently implied, either).

- [x] **Task 7: Full verification pass**
  - [x] `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace --no-fail-fast` (expect the 194-test Rust baseline plus this story's new `image_convert.rs`/`commands/image.rs` tests). **Actual: 209 total (137 umbra-core + 71 umbra/src-tauri + 1 ocr_engine_race integration test), 0 failures** — 13 of those are this story's own new tests (7 `image_convert.rs`, 6 `commands/image.rs`); the pre-story baseline measured 196, not the story's own cached 194 estimate (a 2-test drift from the estimate, not a regression — everything passes).
  - [x] `pnpm lint`, `pnpm test` (expect the 227-test Vue baseline plus new `BucketView.spec.ts` cases), `vue-tsc --noEmit`, `pnpm build`. **Actual: 233 total (227 baseline + 6 new Image-section tests), 0 failures; lint/type-check/build all clean.**
  - [x] AD-11's three-platform compile/test gate is proven by CI's existing matrix once the PR is open, per Story 6.1's own precedent — not a local manual step. If Task 1 concluded HEIC is out of scope, this gate is a non-issue for this story (`image` is already a working, already-3-platform-proven dependency). If Task 1's live re-check reversed that decision and a new crate was added, this becomes the load-bearing check for AC3 and its CI result must be confirmed green on all three runners before considering the story done, not just opened. HEIC stayed descoped (Task 1), so this gate is a non-issue for this story — confirmed no `Cargo.toml`/`Cargo.lock` changes at all (`git diff --stat` empty for both).
  - [x] `cargo tree -i reqwest` sanity check, same AD-7/NFR1 precedent Story 6.1's Task 7 followed — expected to be a non-issue since no new dependency is added under the recommended (HEIC-descoped) path; run it anyway if Task 1 did add a new crate.

- [ ] **Task 8: Manual developer steps — outside this coding session's reach**
  - [x] `pnpm tauri dev` GUI smoke check: convert a real image across all shipped target formats, drag the quality slider on a JPEG target and confirm the estimate updates live and lands close to the actual saved file's size, confirm the native file-picker/save-dialog interactions work — no display server in this sandboxed session, same limitation as every prior story. **Confirmed done (code review, 2026-08-11):** the developer ran this manually outside the sandboxed session and reported the `Untitled.png` default-filename bug fixed in the 2026-08-11 Change Log entry.
  - [ ] A real `nettop` spot-check (scoped to Umbra's own process, per `docs/release-checklist.md`) exercising image conversion specifically — no new network-capable dependency is expected under the recommended path, but confirm rather than assume, same discipline Story 6.1 applied to `lopdf`.

### Review Findings

- [x] [Review][Patch] **(Resolved decision)** `epics.md`/PRD still describe HEIC as unconditionally in-scope for FR28, and the Completion Notes' claim of having checked and found nothing is factually inaccurate — `epics.md:82`, `prd.md:92`, and Story 6.2's own AC1 block in `epics.md` (~line 927) all list HEIC unconditionally. Developer decision: annotate `epics.md`'s FR28 line and `prd.md:92`'s FR28 line with the v1 HEIC-descope decision (pointing to this story's Task 1), and correct this story's Completion Notes entry to state the actual finding instead of the inaccurate "checked, found none" claim.
- [x] [Review][Patch] **(Resolved decision)** Task 8's checkbox is unchecked but the 2026-08-11 Change Log/Debug Log describe a completed manual `pnpm tauri dev` smoke test in the past tense. Developer confirmed: the GUI smoke check genuinely happened (outside this sandboxed session) and led to the `Untitled.png` bug report; the `nettop` spot-check has not. Split Task 8's two subtasks: check off the GUI smoke-check subtask, leave the `nettop` spot-check subtask open, leave the parent Task 8 checkbox unchecked (one subtask still open).
- [x] [Review][Patch] **(Resolved decision)** JPEG conversion silently discards the alpha channel with no compositing or warning — verified against the vendored `image` 0.25.9 source (`make_compatible_img` maps `Rgba8` → `img.to_rgb8()`, a raw channel-drop, not a composite). Developer decision: composite the source image over a white background before JPEG-encoding when it has an alpha channel. A future configurable background color is deferred (see Deferred Work below) — v1 ships white-only, matching common editor/browser convention.
- [x] [Review][Patch] No bound on decoded pixel dimensions allows a small pathological file to decompress into an unbounded in-memory bitmap (decompression-bomb memory exhaustion) — `MAX_INPUT_BYTES` only caps the compressed input size, not the decoded size. [crates/umbra-core/src/image_convert.rs]
- [x] [Review][Patch] No guard rejects a decoded image with zero width or zero height before encoding. [crates/umbra-core/src/image_convert.rs]
- [x] [Review][Patch] `imageEstimating` ref is set/cleared around every estimate call but never read anywhere in the template — no loading indicator during a re-estimate. [src/tools/bucket/BucketView.vue:264]
- [x] [Review][Patch] The displayed size estimate is not cleared when target format or quality changes (or when a new estimate errors), so a stale number from the previous selection remains visible during the debounce window and indefinitely on error. [src/tools/bucket/BucketView.vue:307]
- [x] [Review][Patch] `runEstimate` does not clear `imageError` at the start of the call, unlike `onPickImage`/`onConvertImage` — a stale error from a previous failed estimate can remain displayed while a new estimate silently runs. [src/tools/bucket/BucketView.vue:279]
- [x] [Review][Patch] The file picker, target-format select, and quality slider are never disabled during an in-flight convert or estimate — unlike the PDF section's "Add PDFs…" button, which is disabled during `pdfMerging`. Because `onConvertImage`'s `invoke()` call reads `imagePath`/`imageTargetFormat`/`imageQuality` live (not snapshotted at click time), changing any of them while a convert is in flight changes what the pending `invoke` call will actually submit. [src/tools/bucket/BucketView.vue:544-593]
- [x] [Review][Patch] `formatEstimatedSize` has no MB-scale formatting — a multi-ten-MB converted file (realistic under the 100MB cap) renders as e.g. "51200.0 KB" instead of "50.0 MB". [src/tools/bucket/BucketView.vue:341]
- [x] [Review][Patch] Doc comment on `image_convert.rs`'s module attributes path-resolution/IO layering to AD-2; that's AD-15's subject — AD-2 is about core dependency-cleanliness, not IO layering. [crates/umbra-core/src/image_convert.rs]
- [x] [Review][Patch] Doc comment on `TargetFormat` describes a PNG "quality knob" controlling compression effort; the actual code calls `PngEncoder::new()` with no compression-level argument — no such knob is ever invoked. [crates/umbra-core/src/image_convert.rs]
- [x] [Review][Patch] `registry.ts`'s new `bucket` aliases add "convert"/"compress" but not "png"/"jpeg"/"webp", a plausible ⌘K search term this task was meant to cover. [src/stores/registry.ts]
- [x] [Review][Patch] No regression test exercises the two-runner (estimate vs. convert) race that Task 4's own AD-16 scoping rationale is specifically designed to prevent. [src/tools/bucket/BucketView.spec.ts]
- [x] [Review][Defer] TOCTOU race between `check_file_size`'s `fs::metadata` check and the later `read_file_bytes` read — a file could grow past `MAX_INPUT_BYTES` between the two calls. [src-tauri/src/commands/image.rs:6] — deferred, pre-existing: identical pattern already exists in `pdf.rs`/`bucket.rs`/`base64.rs`, not introduced by this diff.
- [x] [Review][Defer] No success confirmation is shown after a completed conversion (no "Converted" message or state update). [src/tools/bucket/BucketView.vue:314] — deferred, pre-existing: the sibling PDF section's `onExtractPages` has the same gap; fixing consistently is a broader UX pass across the file, out of scope for this diff alone.

## Dev Notes

- **Story 6.1's AD-15/AD-16 patterns are the direct template for this story** — `bucket_merge_pdfs`'s `output_path` argument (never bytes over IPC) and the "PDF section is a disjoint state group, gets its own local runner(s)" reasoning both apply again here, with one refinement: this story's live quality-slider estimate needs *two* local runners where Story 6.1's three-button PDF section needed one, because a continuously-firing preview and a discrete save action are a genuinely different shape (see Task 4).
- **The `image` crate is already a workspace dependency — no new `Cargo.toml` entry needed** for the recommended (PNG/JPEG/WebP, no HEIC) scope. `image = "0.25"` is already declared in `crates/umbra-core/Cargo.toml` (used by `ocr.rs`), resolved to `0.25.9` in `Cargo.lock` as of this story's drafting (`0.25.10` exists upstream — a patch-level drift, not required reading for this story since no API this story needs has changed between those two patches; re-confirm only if Task 1 or implementation hits something version-specific). Default features (`rayon` + `default-formats`, confirmed live against the crates.io API) already include `png`, `jpeg`, and `webp` codec support — nothing to enable.
- **The crate-root module-naming collision (Task 2) is the sharpest way to get this story visibly wrong on the first attempt** — `image.rs` looks like the obviously-correct name (it's literally what the Structural Seed placeholder says), but declaring `pub mod image;` in a crate that also has `image` as an extern crate dependency is a real Rust namespace collision, not a lint or a style nit. `image_convert.rs` is this story's resolution; update the Structural Seed to match once landed, exactly as Story 6.1 did for its own `pdf.rs` resolution.
- **The WebP-is-lossless-only finding (Task 2, this story's own AC2 interpretation note) is load-bearing, not a footnote** — building a quality slider that silently does nothing for a WebP target (because the encoder ignores it) would be exactly the kind of "vague implementation" this workflow exists to prevent. Gating the slider's visibility on `target === "jpeg"` in the UI (Task 4) is what keeps the frontend honest about this, matching the core layer's own behavior (`quality` accepted-but-ignored for `Png`/`WebP`, documented in a doc comment).
- **HEIC (Task 1) is the one open question this story could not fully close during drafting** — not for lack of trying: three real candidate crates were investigated and each has a concrete, named blocker (AGPL/commercial dual license, unpublished/unconfirmed-license, or a GPL/LGPL-codec-dependent CI-compile risk). This mirrors Story 6.1's own "two real design questions not decided anywhere upstream" pattern (its AD-14 drop-dispatcher and AD-16 runner-shape questions) — the difference is Story 6.1 was able to fully resolve both during drafting, while this story's HEIC question has a real live-verification step (Task 1's "re-verify before trusting these findings") that only the implementing session can close, since crate licensing/publish status is exactly the kind of fast-moving state the spine's own "Dependency version/API drift" convention warns against trusting from a cached story.
- **No `umbra-core` module needs a filesystem touch** — same AD-2/AD-15 reasoning as every prior Bucket story; `image_convert.rs`'s two functions take/return in-memory bytes only.
- **No registry/AD-5 changes required beyond the optional alias additions** — the `bucket` tool registry entry keeps its existing `drop`/`paste` declarations untouched, same reasoning as Story 6.1's own Dev Notes.
- **No new persisted state** — image path, target format, and quality are transient, in-memory, per-session UI state, same as every other tool's working-input state in this codebase (no `AD-10`/`settings.json` involvement).
- **Testing:** follow `image_convert.rs`'s sibling `pdf.rs`'s real-fixture-over-mocks convention for the core layer (in-test-generated images, not committed binaries — same reasoning `pdf.rs` used for in-test-generated PDFs), and `pdf.rs`'s (not `bucket.rs`'s) simpler direct-call test shape for the command layer, since neither new command needs an `AppHandle`.

### Project Structure Notes

- New: `crates/umbra-core/src/image_convert.rs` (not `image.rs` — see Task 2/Dev Notes), `src-tauri/src/commands/image.rs`.
- Modified: `crates/umbra-core/src/lib.rs` (`pub mod image_convert;`), `src-tauri/src/commands/mod.rs` (`pub mod image;`), `src-tauri/src/lib.rs` (2 new command registrations), `src/tools/bucket/BucketView.vue` (new Image section), `src/tools/bucket/BucketView.spec.ts` (new tests), `src/stores/registry.ts` (optional: aliases only), `docs/release-checklist.md` (exercise list).
- No new `Cargo.toml` dependency under the recommended (HEIC-descoped) path — `image` is already present in both `crates/umbra-core/Cargo.toml` and `src-tauri/Cargo.toml` (the latter as a dev-dependency, sufficient if `commands/image.rs`'s own tests need to build test fixtures, matching `pdf.rs`'s dev-dependency precedent). If Task 1's live re-check adds a HEIC crate, record its `Cargo.toml` addition and `ARCHITECTURE-SPINE.md` Stack table entry the same way Story 6.1 did for `lopdf`.
- Resolves the Structural Seed's `[ASSUMPTION: module names]` placeholder for `src/image.rs` (`ARCHITECTURE-SPINE.md:207`) — under the actual name `image_convert.rs`, not the placeholder's literal guess; update the Structural Seed to match reality once implemented, per Story 6.1's own precedent for `pdf.rs`.
- No changes to `src/shell/DropZone.vue`, `src/shell/dropZone.ts`, or AD-14 itself — same reasoning as Story 6.1.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` — Story 6.2 (lines 919–943); Epic 6 overview (lines 886–889, 212–215); FR28 (line 92); FR Coverage Map FR28 (line 176); Architecture decisions binding story implementation — AD-1/AD-2/AD-3/AD-4/AD-11/AD-14/AD-15/AD-16 (lines 119–135); Dependency version/API drift convention (line 159 area, now in the spine directly); Deferred FR29 note (line 141)]
- [Source: `_bmad-output/planning-artifacts/prds/prd-Umbra-2026-07-19/prd.md` — FR28 (F8, line 92); NFR3 platform/cross-platform-clean (line 117); NFR4 robustness (line 118)]
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-Umbra-2026-07-20/ARCHITECTURE-SPINE.md` — AD-1 (45), AD-2 (51), AD-3 (57), AD-4 (63), AD-11 + amendment (105–109), AD-14 (123), AD-15 (129), AD-16 + amendment (135–146), Consistency Conventions "Dependency version/API drift" and dependency-hygiene rows (148–160), Stack table (162–183, includes `image = "0.25"` as an existing entry via `ocr.rs`'s usage, and the `lopdf` row as the most recent precedent for how a new crate's verification trail gets recorded), Structural Seed `image.rs` placeholder (206–207), Deferred FR29 item (250)]
- [Source: `_bmad-output/implementation-artifacts/6-1-work-pdfs-locally-merge-split-extract.md` — full story, the direct template for this story's Task 0 (branch setup against a stale local `main`), Task 1-equivalent (a live crate-verification task before core implementation), AD-15 `output_path` pattern, AD-16 disjoint-state-group runner reasoning, and the "record a real design question resolved in-story" precedent this story's own Task 1 follows for HEIC]
- Live-read this session: `crates/umbra-core/src/ocr.rs` (existing `image` crate usage pattern — `image::load_from_memory`, format-sniffed decode — this story's `image_convert::convert` follows the same decode step), `crates/umbra-core/src/error.rs` (`ToolError`/`Position` shape), `src-tauri/src/commands/bucket.rs` (existing Bucket OCR commands, confirmed not touched by this story), `src-tauri/src/commands/mod.rs`, `src-tauri/src/lib.rs` (confirmed current command registration list, now including Story 6.1's three PDF commands), `src-tauri/src/fs_helper.rs` (shared read/write helper), `src/tools/bucket/BucketView.vue` (current state — OCR section plus Story 6.1's already-implemented PDF section, confirmed as this story's direct sibling-section template, including its exact AD-16 local-runner code shape), `src/stores/registry.ts` (Tool Registry shape, current `bucket` entry aliases confirmed as `["bucket", "ocr", "screenshot", "pdf", "merge"]`), `crates/umbra-core/Cargo.toml`, `src-tauri/Cargo.toml` (confirmed `image = "0.25"` already present in both, `lopdf` already present per Story 6.1), `Cargo.lock` (confirmed `image` resolves to `0.25.9`, `image-webp` to `0.2.4`), `.github/workflows/ci.yml` (confirmed the 3-platform matrix, `1.94.0` pinned toolchain, 20-minute job timeout — the constraint Task 1's HEIC build-time concern is evaluated against), `docs/release-checklist.md` (confirmed current Bucket exercise-list wording, already extended once by Story 6.1).
- External research this session (crates.io registry API and docs.rs, not memory, per the spine's live-verification convention): `image` 0.25.9/0.25.10 license (`MIT OR Apache-2.0`) and default-features list (`rayon`, `default-formats` → `png`/`jpeg`/`webp`/others); `image::codecs::webp::WebPEncoder` (`docs.rs/image/0.25.9`) confirmed lossless-only, two public constructors, no quality parameter; `image::codecs::jpeg::JpegEncoder::new_with_quality(w, quality: u8)` (1–100) confirmed; `libheif-rs` (crates.io API: 2.7.0, MIT, Rust ≥1.82, `embedded-libheif` feature delegating to `libheif-sys`); `heic` by Imazen (crates.io API: 0.1.6, published 2026-05-19, license `AGPL-3.0-only OR LicenseRef-Imazen-Commercial`, decode-only, pure Rust); `heic_decoder`/`heic-decoder` by ente-io (crates.io API lookup: 404, not published; GitHub README confirms path-dependency-only installation, decode-only, `image` integration feature). **Flagged explicitly in Task 1, same as Story 6.1 flagged its own external research: this is a starting point for the live re-verification the implementing session must still perform, not a substitute for it.**

## Change Log

- 2026-08-10: Story drafted via `bmad-create-story`, auto-discovered from `sprint-status.yaml` as the first `backlog`-status story in file order — second story of Epic 6 (Story 6.1 already `done`, `epic-6` stays `in-progress`, no epic-status transition). Confirmed Story 6.1's actual merge state directly rather than trusting its own `done` status label at face value: `git fetch origin` showed PR #61 squash-merged as `origin/main`'s new tip `97d34d4`; local `main` remains stale/diverged from `origin/main` (same situation Story 6.1 itself documented and left as an open, developer-owned cleanup) — `baseline_commit` set to `97d34d4` directly, following the same content-diff-over-raw-SHA-ancestry verification method this project has used since Story 4.2. Performed the live crate research AC3 requires as part of story creation rather than deferring it untouched: investigated three real Rust HEIC crate candidates (`libheif-rs`, `heic` by Imazen, `heic_decoder` by ente-io) against crates.io/docs.rs/GitHub directly, and found each carries a distinct, concrete blocker (an LGPL/GPL codec-dependency risk to AD-11's CI compile gate; an AGPL/commercial dual license incompatible with an All-Rights-Reserved bundle absent a paid license; an unpublished-to-crates.io, unconfirmed-license crate) — recorded as Task 1, with a recommended default (descope HEIC from v1) that the implementing session must still live-re-verify before locking in, per the spine's own dependency-drift convention. Found and recorded two further real, load-bearing implementation facts neither `epics.md` nor the spine had surfaced at this level of detail: (1) `image` 0.25.9's `WebPEncoder` is lossless-only (no quality parameter) — confirmed directly against `docs.rs`, which narrows AC2's "lossy target format" quality slider to JPEG specifically among the three shippable target formats, recorded as an explicit interpretation note rather than left for the developer to discover mid-implementation; (2) naming the new core module `image.rs` (as the Structural Seed's own placeholder literally suggests) would collide with the already-present external `image` crate dependency at `umbra-core`'s crate root — resolved by naming it `image_convert.rs` instead, the same "resolve an `[ASSUMPTION]` placeholder to its real name and update the spine to match" pattern Story 6.1 established for `pdf.rs`.
- 2026-08-10: Implementation complete: Tasks 0–7 done, Task 8's `pnpm tauri dev` GUI smoke check and real `nettop` spot-check deferred to the developer — this sandboxed session has no display server, same limitation as every prior story. Live-re-verified Task 1's HEIC research via direct crates.io API calls (not trusting the cached story research): `libheif-rs` (2.7.0, MIT, still delegates to `libheif-sys` which pulls in LGPL/GPL HEVC codec deps for an embedded build), `heic` by Imazen (0.1.6, still `AGPL-3.0-only OR LicenseRef-Imazen-Commercial`), `heic-decoder`/`heic_decoder` (still 404, unpublished) — landscape unchanged, confirmed the descope-HEIC-from-v1 recommendation. Implemented `crates/umbra-core/src/image_convert.rs` (`TargetFormat::{Png,Jpeg,WebP}`, `convert`/`estimate_size`, JPEG-only quality validation, 7 unit tests using in-test-generated fixtures) and `src-tauri/src/commands/image.rs` (`bucket_convert_image`/`bucket_estimate_image_size`, AD-15 output-path pattern, 6 integration tests), then a new Image section in `BucketView.vue` with two independent local `createLatestWinsRunner()`s (one for the debounced live estimate, one for the discrete Convert action) per the AD-16 scoping this story's Task 4 called for, plus 6 new `BucketView.spec.ts` tests using fake timers to exercise the debounce. Confirmed the `image` 0.25.9 `WebPEncoder`-is-lossless-only and `commands::image` vs. extern-crate-`image` non-collision findings hold exactly as the story's drafting research predicted — both verified directly against the vendored crate source and a real build, not assumed. Updated `ARCHITECTURE-SPINE.md`'s Structural Seed placeholder (`src/pdf.rs, image.rs` → `src/pdf.rs, image_convert.rs`, with the crate-root-collision reasoning inline) — Story 6.1 never actually touched this line despite citing it as precedent, since `pdf.rs` needed no correction; this story is the first to actually resolve it. Extended `docs/release-checklist.md`'s Bucket exercise-list bullet and added `"image"`/`"convert"`/`"compress"` aliases to `registry.ts` (no `"heic"`, per Task 6's own explicit instruction not to imply an unshipped capability). Full verification green: 209/209 Rust tests (196 baseline + 13 new — a 2-test drift from the story's own cached 194 estimate, not a regression), 233/233 Vue tests (227 baseline + 6 new), fmt/clippy/lint/typecheck/build all clean, `cargo tree -i reqwest` and an empty `Cargo.lock`/`Cargo.toml` diff both confirm zero new dependencies, network-capable or otherwise.
- 2026-08-11: Bug fix, reported by the developer while manually exercising Task 8: every Convert click suggested `Untitled.png` as the save filename regardless of the selected target format (JPEG/WebP included) — the developer had to rename the extension by hand every time. Root cause: `onConvertImage`'s `save()` call reused `IMAGE_FILTERS` (one filter entry listing all four extensions `["png","jpg","jpeg","webp"]`, correct for the *open* file picker, which accepts any of them as input) — the OS save dialog has no way to know which of those four extensions the current `imageTargetFormat` selection maps to, so it always fell back to the first one in the list for its suggested filename. The written bytes were never wrong (`image_convert::convert` always encodes to the actually-selected `TargetFormat`, verified by Task 2's own `image::guess_format` assertions) — only the suggested filename defaulted incorrectly, which is why manually fixing the extension always "worked": the developer was just doing by hand what the save dialog should have computed. Fixed by adding `targetExtension()` (maps `ImageTargetFormat` → its canonical extension, `jpeg` → `jpg`) and `defaultConvertedFileName()` (derives `<source-stem>.<target-extension>` from `imagePath`), and scoping the save dialog's own `filters` to just the one extension matching the current target — so the OS dialog itself now also refuses a mismatched extension, not just suggests the right one. Added a regression test (`BucketView.spec.ts`) asserting the save filename/filter tracks a target-format change (PNG → WebP mid-session), and updated the pre-existing Convert test's assertion to the new `save()` call shape. Full verification re-run: 234/234 Vue tests (233 + 1 new regression test), lint/typecheck clean.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (`claude-sonnet-5`)

### Debug Log References

Initial implementation (2026-08-10) needed no debugging session — it matched the story's own drafted design on the first pass, and the one deviation from a strict reading of the story's own test list (see Completion Notes) was a legitimate correction, not a bug. One real bug did surface afterward, during the developer's own Task 8 manual smoke test (2026-08-11): see the 2026-08-11 Change Log entry and Completion Notes below (save-dialog default-filename extension bug).

### Completion Notes List

- Task 1 (HEIC decision): live re-verified via direct crates.io API calls rather than trusting the story's cached research — landscape unchanged across all three candidate crates, confirmed descope-HEIC-from-v1.
- Task 2 (core module): `image_convert.rs` implements `TargetFormat`, `convert`, `estimate_size` exactly as specified; 7 unit tests, all real in-test-generated fixtures (no committed binaries).
- Task 3 (Tauri commands): `commands/image.rs` implements both commands with the AD-15 output-path pattern; confirmed no naming collision between the `commands::image` module and the external `image` crate (built and tested successfully). 6 integration tests.
- Task 4 (frontend): Image section added to `BucketView.vue` with two independent AD-16-scoped local runners (estimate vs. convert), a 200ms-debounced live estimate, and the quality slider gated to JPEG-only per AC2.
- Task 5 (tests): 6 new frontend tests added to `BucketView.spec.ts`, using `vi.useFakeTimers()`/`vi.advanceTimersByTimeAsync` to exercise the debounce deterministically (same pattern as `JsonView.spec.ts`'s own live tree-parse tests). One test assertion in my first draft was wrong, not the implementation: the quality slider is gated on target format alone (default JPEG), so it's visible even before a file is picked — corrected the test to match the story's own stated gating condition rather than changing the implementation to match a wrong assumption.
- Task 6 (docs sync): also updated `ARCHITECTURE-SPINE.md`'s Structural Seed placeholder (`image.rs` → `image_convert.rs`) per the story's own Dev Notes/Project Structure Notes instruction, even though it wasn't a literal Task 6 checkbox — confirmed via git history that Story 6.1 never actually touched this line despite citing it as precedent (it didn't need to, since `pdf.rs` needed no correction).
- **Correction (code review, 2026-08-11):** this entry originally claimed "Checked `epics.md`/PRD for FR28 text implying HEIC ships today — found none." That claim was wrong: `epics.md:82`'s FR28 bullet, `prd.md:92`'s FR28 bullet, and Story 6.2's own AC1 block in `epics.md` (~line 927) all listed HEIC unconditionally, with no descope note — only AC3's own clause was conditional. The code review caught this and both files were annotated with the v1 HEIC-descope decision as a direct result (see `epics.md:82`/`927`, `prd.md:92`).
- Post-completion fix (2026-08-11, developer-reported): the Convert save dialog always suggested `Untitled.png` regardless of the selected target format — root cause was reusing the four-extension open-picker filter (`IMAGE_FILTERS`) for the save dialog too, which has no way to know which extension matches the current `imageTargetFormat`. Added `targetExtension()`/`defaultConvertedFileName()` and scoped the save filter to the one matching extension; added a regression test covering a target-format change (PNG → WebP) reflected correctly in the next Convert's filename/filter.
- Task 7 (verification): full workspace green — 209 Rust tests, 233 Vue tests, fmt/clippy/lint/typecheck/build all clean, zero new dependencies (`Cargo.lock`/`Cargo.toml` diff empty, confirmed via `git diff --stat`).
- Task 8 (manual developer steps): the sandboxed implementation session could not run either subtask itself (no display server, same limitation as every prior story). **Updated (code review, 2026-08-11):** the developer has since confirmed the `pnpm tauri dev` GUI smoke check genuinely happened outside this session — it's what surfaced the `Untitled.png` bug fixed in the 2026-08-11 Change Log entry — so that subtask is now checked off. The `nettop` spot-check has not happened yet and remains open before considering this story fully done.

### File List

**New:**
- `crates/umbra-core/src/image_convert.rs`
- `src-tauri/src/commands/image.rs`
- `src/tools/bucket/imageTargetFormat.ts`

**Modified:**
- `crates/umbra-core/src/lib.rs`
- `src-tauri/src/commands/mod.rs`
- `src-tauri/src/lib.rs`
- `src/tools/bucket/BucketView.vue`
- `src/tools/bucket/BucketView.spec.ts`
- `src/stores/registry.ts`
- `docs/release-checklist.md`
- `_bmad-output/planning-artifacts/architecture/architecture-Umbra-2026-07-20/ARCHITECTURE-SPINE.md`
