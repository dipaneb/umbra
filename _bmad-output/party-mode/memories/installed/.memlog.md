---
topic: Umbra tool redesigns (Epic 8), starting with the JSON Formatter/Viewer (Story 8.1)
goal: produce Task 1 decision records for each tool via open scope discovery
updated: 2026-09-02T02:02
---

- (moment) Story 8.1 opener: room split fast on JSON tool scope -- Sally/Amelia want the editor made real (textarea has zero editor affordances); Winston pushed back hard on scope creep (diff = new json.rs surface, wants it costed); Mary flagged EXPERIENCE.md already parked diff as a MAYBE; Paige+Mary landed a near-free win (rewrite serde's generic error text to match the JWT-inspector voice bar); Sally's tree-search idea won fast unanimous nods, prompting a running bit -- Winston needling that everything that wins fast is the stuff that skips his crate.
- (outcome) Story 8.1 scope firmed up: repair ships preview-then-confirm (never silent auto-apply, per AD-9/EXPERIENCE.md honesty bar); transform ships TypeScript first, other languages deferred as their own call; JSONL deferred to the public backlog, not this story; tabbed IA (Explorer/Validate/Query/Diff/Transform) adopted as the redesign's container, composing with DESIGN.md's already-locked-but-unused Tab component. Mid-session: origin/main moved (PR #104) and tokenized JsonTree.vue's focus ring plus added a global src/styles/base.css layer that already styles bare textareas -- baseline_commit bumped to b357aff, story's Dev Notes corrected so Task 2 doesn't redo that work.
- (dynamic) Correction: the previous outcome entry overstated it -- repair's preview-then-confirm shape, TS-first transform, JSONL-deferred, and the tabbed IA are the room's live proposal, not yet confirmed by the developer. Still awaiting their answer on all four.
- (outcome) Confirmed by the developer 2026-08-24: repair ships preview-then-confirm; transform ships TypeScript first (Go/Python/YAML/CSV deferred as their own decision); JSONL deferred to the FR35 backlog; tabbed IA (Explorer/Validate/Repair/Query/Diff/Transform) adopted. Developer also caught a real gap the room hadn't named: French/i18n -- resolved as no AD-13 exception needed (no NL grammar involved, unlike cron), but surfaced a genuine pre-existing bug: toolError.ts's TRANSLATABLE_CODES has zero json-* coverage, folded into the Validate tab's scope. Decision record written to 8-1-json-decision-record.md; Story 8-1's Task 1 marked complete. Task 2 (real ACs + redesign) is next, not started.
- (outcome) Story 8.1 Task 2 ACs written 2026-08-24: room resolved both open items from Task 1 -- Repair is its own tab (cross-linked from Validate, not merged: preview-then-confirm needs its own real estate) and Query speaks JSONPath/RFC 9535 via serde_json_path (standardized, drops onto existing serde_json::Value, matches Explorer's own copy-JSONPath output -- JMESPath rejected for breaking that consistency). Winston's running bit continued (everything routes through his crate). AC6-14 written into the story file; Task 2 split into 2a (ACs, done) / 2b (implementation, not started).
- (moment) Story 8.2 (Base64) opener: room fast-agreed on live conversion + killing the Encode/Decode/Paste button cluster (Sally: Base64 is a lookup, not a pipeline like JSON -- Copy survives only as an output-panel icon). Developer wanted data-URI support but said they lack the knowledge to build it; asked what 'blob detection' even is -- both explained. Winston's running bit held: he objected to 'what is this?' detection on a security-adjacent tool, then stood down once Amelia showed parse_data_uri + sniff are small pure umbra-core functions ('That's a first.'). Container shape (tabs vs single view vs two-pane) still open -- built a /design canvas of all three for the developer to pick.
- (outcome) Story 8.2 (Base64) Task 1 decided 2026-08-29. Developer picked the single enriched view (Option A) off a 3-container design canvas -- tabs rejected as ceremony for a 2-job tool, two-pane rejected (halves vertical space, blobs are tall). Scope: KEPT the converter + both alphabets + caps + error model; live as-you-type conversion replaces Encode/Decode buttons; Paste button cut (Sally: Base64 is a lookup not a pipeline -- deliberate divergence from 8.1); Copy -> icon. ADDED: data URI build+decode with image preview, blob detection ('looks like a JWT/PNG/...') honest-by-construction per AD-9, line-wrap on encode. Anti-clutter rule the developer asked for: one priority-ordered contextual slot (error > detection > data-URI), caption not card, never stacked -- Amelia turned the vibe into a testable rule. CUT to backlog issues: hex view, decode-as-gzip, recursive decode. Winston's crate-grudge finally inverted -- this redesign is mostly his crate (parse_data_uri + sniff + wrap param). Decision record: 8-2-base64-decision-record.md. Next: file the 3 GitHub issues, then Task 2a (real ACs, same room).
- (outcome) Story 8.2 Task 2a done 2026-08-29, same room. Open items resolved: data-URI build is sync view-side string compose (NOT a new state group, no new latest-wins runner); detection = fixed first-match-wins order (not-Base64 > JWT > PNG/PDF/gzip/zip magic > UTF-8 text > unknown), no scoring; line-wrap = inline Encode-only selector, default none (Sally: changing emitted-output default for existing users is a trap). CSP-for-data:-preview and the final base64-* code list punted to 2b as implementation-discovery. Winston got his vertical-slice + developer-render-review-after-slice-1 discipline written into 2b (the 8.1 reflexive-copy safeguard). ACs AC6-16 in the story file. 3 cut-idea issues filed: #113/#114/#115. Awaiting developer sign-off before 2b implementation; single commit planned for record + story file.
- (moment) Story 8.3 (UUID) opener: room framed two users — generate-a-UUID vs inspect-one-I-have. Sally pitched a v7-timestamp inspector as 'the missing point of the tool'; Winston flagged it as a second tool (real umbra-core parser, version/variant decode, timestamp extract) not a reskin. Paige's near-free win: v4/v7 radios and bare 'UUID' heading explain nothing — needs 'random'/'timestamp-ordered' labels. Winston's crate-grudge running bit continued, now self-aware ('bracing for it to invert again'). Thrown to developer: do you generate or inspect in real life?
- (moment) 8.3: developer caught the room saying Story 2.3 shipped '2024' — actually 2026-07-31, one month ago; Mary owned it (her treasure-hunter instinct romanticizes any un-touched file into an artifact), John owned repeating it unc checked. Developer: doesn't use UUID tools IRL, doesn't know v4 vs v7 — room turned that into the evidence FOR a teach-the-versions feature (the author doesn't know, so no user does). Developer proposed a ? icon -> tooltip per version or per selector. Room converged: inline radio taglines (v4 — random / v7 — timestamp-ordered) as the cheap-correct base, ? for the deeper 'when do I pick which'. Amelia: NO tooltip component exists, only native title=; a real Popover.vue would be the app's FIRST floating surface -> new shared infra, AD-6, governance flag, exercises untested DESIGN.md floating-surface tokens. Winston declined to rubber-stamp this one despite it not touching his crate — wants it as its own line/story. Two forks thrown to developer: (1) inline taglines y/n, (2) native-title ? now vs spec real Popover as separate scope.
- (outcome) Story 8.3 (UUID) Task 1 decided 2026-08-30. Developer confirmed: ENRICHED single view (not tabs — one job, AppTabs stays unused; 8.1/8.2 discipline held). KEPT v4/v7 only + 1000 cap as-is (room reasoned it: DOM row-render is the binding limit, not compute; virtualisation deliberately skipped). ADDED full format-toggle set (lowercase/UPPERCASE/{braces}/no-hyphens — all view-side string ops, AD-1) + bulk download to file. Developer killed the per-result timestamp-decode inspector with one line — '100 UUIDs = 200 lines?' — Sally conceded instantly. CUT to backlog: v1/v6, v3/v5, inspector, ULID/nanoid/CUID. Paige's ?-tooltip idea became: inline taglines now (v4 · random / v7 · time-ordered), and a SEPARATE story for a shared Popover.vue — the app's first floating surface, governance-flagged, multi-tool consumer; Winston refused to rubber-stamp it despite it not touching his crate. Winston's crate-grudge ran its full arc and lost again: cleanest AD-1 split in Epic 8, umbra-core entirely untouched ('for the third time running, the redesign skips my crate. I'd say I'm surprised.'). Also: developer caught the room dating Story 2.3 to '2024' — it shipped 2026-07-31, one month ago; Mary owned the treasure-hunter reflex. Decision record: 8-3-uuid-decision-record.md. Next: file 5 backlog issues, publish enriched-view design canvas, then Task 2a (real ACs, same room).
- (outcome) 8.3 amendment (same session, right after the decision record): developer overruled the room's 'Popover = separate story' call and folded it INTO Story 8.3 — as a general reusable src/components/Popover.vue, the app's first floating surface. Winston's 'first floating surface deserves its own story' position lost to the developer's 'I want it in.' Now in-scope: taglines + a working ? on the version selector. Component API to be presented as options in Task 2a per CLAUDE.md. Backlog issues to file: 5 -> 4. umbra-core still untouched.
- (moment) Story 8.4 (Hash) opener: Sally pitched Verify/compare (sha256sum -c / 'verify your download') as the missing half of the tool; developer: 'in, of course'. Winston's crate-grudge running bit finally paid off — SHA-3 add = real umbra-core work (new sha3 dep, cross-platform CI, HashDigests struct reshaped to an Algorithm-keyed collection) after three redesigns skipped his crate ('Three stories I waited for this'). Mary needled his patience.
- (outcome) Story 8.4 (Hash) Task 1 decisions locked with developer (pre-canvas): KEPT the 4 digests + drop path + shared latest-wins runner + view-side case toggle. ADDED — (1) Verify/compare mode (paste expected hash -> match/no-match; mismatch is a role=status result, NOT a role=alert error, per EXPERIENCE.md instrument voice; no new error codes); (2) SHA3-256 + SHA3-512 via the sha3 crate (Digest-trait, clean add); (3) user-selects-algorithms via checkboxes, persisted as a hash.* setting (8.3 uuid.* pattern), default SHA-256/512 on — so umbra-core moves from HashDigests{4 named fields} to an Algorithm-enum-keyed collection, command takes an algo list, hash_compute_file gains a dropArgsProvider (Base64 pattern); (4) output encoding hex (default) + Base64 — both view-side transforms, hex->bytes->Base64 in JS, no core field; (5) live as-you-type hashing (debounced + length ceiling) replaces the explicit Compute button. Paige: MD5/SHA-1 checkbox labels become 'not collision-resistant', not 'legacy'. CUT (developer explicitly directed NOTHING filed to GitHub/backlog — deviation logged, 8.3 precedent): BLAKE2b, BLAKE3, byte-grouped hex, 0x prefix, HMAC mode, a signature-verify tool. Password hashing stays FR14-excluded as its own future tool. Container shape unresolved -> 4-artboard design canvas next (enriched single view / Compute|Verify tabs / Compute<->Verify segmented switch / polymorphic input a la Wildcard). Open for the canvas: Verify input-source model (compare currently-loaded input vs Verify's own file+hash pair) — developer wants an interactive example to decide.
- (outcome) Story 8.4 (Hash) Task 1 DECIDED 2026-08-31. Published a 4-option container canvas (claude.ai/code/artifact/c928d06d...); developer liked Options 1 (enriched single view) + 4 (polymorphic paste-offer), asked for the resting/empty state of 1 and the post-'Compare' state of 4 -> added 2 more artboards, 3 pages. Room's synthesis (Winston): they're not competing — Option 4 is a front door to Option 1's persistent Verify field. Developer chose: Option 1 as the container + smart paste-detection that MOVES a bare-hash input into the Verify field, with a VISIBLE acknowledgement the input moved (their explicit requirement; no silent swap, AD-9). UI quality bar = the UUID 8.3 redesign, NOT the wireframes. Winston's crate-grudge arc finally broke his way: HashDigests{4 named fields} -> an Algorithm enum + Vec<DigestEntry>, sha3='0.11' added to Cargo.toml, hash_compute/_file signatures take an algorithm list, hash_compute_file gains a dropArgsProvider. 'Three stories I waited for this.' Decision record: 8-4-hash-decision-record.md. AC3 deviation logged (developer: nothing filed to GitHub/backlog — 8.3 precedent). No drift found vs the story Dev Notes (rare — stated explicitly in the record). Next: Task 2a — real ACs, same room, scoped to the record + a polished canvas.
- (outcome) Story 8.4 (Hash) Task 2a done 2026-09-01, same room. Re-seeded the design canvas to delivery fidelity (4 artboards: Default / Verify active / Smart-detection offer / After 'Move it to Verify'). The acknowledgement moment the developer insisted on = two-ended: a note where the value LEFT + a briefly-tinted Verify panel where it LANDED + Undo — not a single missable toast. 15 ACs (AC6-AC20) written into the story file. Open items resolved: Verify checks against EVERY selected algorithm (developer's call — so the smart detector never has to disambiguate SHA-256 vs SHA3-256 at 64 hex chars); case+encoding = two composing persisted segmented controls; digest export CONFIRMED OUT (John's JTBD + Amelia's cost read: <=6 digests != a bulk problem, SHA256SUMS is a pipeline job — Winston: 'leave it cut'). Developer signed off. Next: Task 2b (per-slice implementation, render-review each slice) — its own working effort.
- (moment) Story 8.5 (JWT Inspector) opener: room convened on the signature question — segments[2] is discarded entirely today. Three forks framed: surface raw sig (zero crypto) / offline verification (crypto crate + AppTabs Decode|Verify + hard AD-7 audit) / stay decode-only. First Epic 8 tool where tabs and Winston's crate could BOTH genuinely be in play. JWT already carries clipboardMatch (EXPERIENCE.md Flow 4's worked example) — preserving that is a constraint. Thrown to developer (John's JTBD framing): do you verify JWT signatures in real life, and do you want it in this tool?
- (outcome) 8.5 fork 1 answered: developer does NOT manipulate JWTs IRL (uses them as web-auth access tokens, never hand-edits/verifies). NO signature verification this story — direction (b) is out. alg prominence + alg:none warning ships regardless (agreed). Open: (a) surface raw signature bytes display-only vs leave discarded — developer didn't follow (a) vs (b) distinction, room to explain plainly and re-ask.
- (outcome) 8.5 forks settled: signature = direction (c) stays discarded, no raw display, no verify; 'not verified' notice REWRITTEN as a calm clear statement not a grey footnote. FR18/Story-2.6-AC4 revised (verification P2->explicitly-out-of-scope, stays P3 backlog). Container = single enriched view, NO tabs (8.1-8.4 discipline; 5th redesign, Winston's crate untouched again - 'it is my personality'). Decode goes LIVE as-you-type (debounced), Decode button dropped. Header/payload STAY as <pre> for now - JsonTree.vue NOT reused, stays a JSON-tool island (AD-6 question closed clean). Per-block Copy added (header/payload/claim) in useCopyFeedback icon-button style - tool has none today.
- (outcome) 8.5 batch 2: PASTE BUTTON CUT (developer: 'hell no' to keeping it) - live decode + native Cmd+V + clipboardMatch front door make it redundant ceremony, 8.2 precedent. Tool is now ONE textarea + output, ZERO action buttons, no primary-orange anywhere (deliberate). onDecode/onPaste collapse to one debounced watch(token), one local latest-wins runner. Size cap: 1MB frontend+core defense-in-depth (Hash 8.4 AC19 pattern) - developer wants the over-cap STATE mocked not debated -> /design canvas. Live clock re-eval for expired + nbf 'not valid until' sibling status line: IN. Extended claims table iss/aud/sub/jti: BACKLOG candidate (redundant w/ kept <pre>). Both deferred-work.md items folded: Invalid-Date bounds check + wrong-type-claim factual role=status note. Next: build /design canvas of the redesigned single enriched view states, developer picks feed Task 2a.
- (outcome) 8.5: published design canvas 'JWT Inspector Redesign' (claude.ai/code/artifact/9ab4c58a-8f60-4cdd-9e71-8bdf32139529) - 4 artboards: Resting / Decoded-healthy / Decoded-flags-firing / Edge-states, light theme (dark parity owed). Shows: rewritten calm notice, live-decode textarea, alg line + alg:none unsigned warning (destructive text/glyph only, no filled box), header/payload <pre> w/ ghost Copy icons, claims w/ relative time, wrong-type-claim factual note (muted not red), Expired/Not-valid-yet status lines (muted+glyph, role=status), malformed role=alert, over-1MB role=status. AD-1 split came out ultra-clean: JwtDecoded struct + decode + decode_segment + numeric_claim + 11 tests + jwt_decode + jwtDecoded.ts mirror ALL UNCHANGED; only core change = MAX_INPUT_BYTES const + guard + ~3 tests + new jwt-input-too-large code (embeds byte count -> stays OUT of TRANSLATABLE_CODES like hash/json). Everything else view-side (AD-1 presentation): live debounced decode, alg extraction, live-clock expired/nbf computeds, relative time, formatClaim bounds check, wrong-type detection reads raw payload Value, per-block Copy via useCopyFeedback. NO verify primitive / NO crypto crate / NO Cargo.toml / NO command-surface / NO registry / NO clipboardMatch change. Winston: 5th redesign, gets exactly one const.
- (outcome) 8.5 canvas v2 + final scope calls: developer rejected the dashed-box notice ('why use the dotted box used for dropping a file?') and the always-on Algorithm line. RESOLVED: (1) 'not verified' becomes a PLAIN description caption under h1 (no box, like every other tool's .tool-desc) + a ? AppPopover (WeakHashPopover pattern) for the longer 3-parts explanation; (2) NO permanent Algorithm line - alg is line 2 of the header <pre> already; a CONDITIONAL boxless warning appears ONLY when alg is none/absent/non-string ('This token is unsigned (alg: none) - anyone can create or alter it', destructive text+triangle glyph, no box). Canvas re-published same URL. (1) NO persisted jwt.* data. (2) isJwtShaped tightening (eyJ-prefix cheap / decode+parse thorough) = AD-6 shell concern -> backlog issue, not this story. (3) cut ideas -> GitHub backlog-candidate issues (like #113-116), NOT personal backlog this time. (4) FR16/FR17/FR18 revisions agreed. Next: write 8-5-jwt-decision-record.md, file issues, Task 2a ACs, developer sign-off.
- (outcome) Story 8.5 (JWT) Task 1 + Task 2a DONE 2026-09-02, same room. Decision record 8-5-jwt-decision-record.md written (Kept/Changed/Added/Cut + container=single-enriched-view-no-tabs + FR16/17 expanded + FR18/2.6-AC4 downgraded + AD-1 split + i18n/TRANSLATABLE_CODES finding + JsonTree.vue=not-reused). AC3: 6 GitHub backlog-candidate issues filed #120-125 + comment on pre-existing #55 (verification) - developer confirmed GitHub route this story (not 8.3/8.4 personal backlog). Canvas 'JWT Inspector Redesign' published+revised once (9ab4c58a). Developer's two sharp catches this session: killed the dashed-box notice ('why use the dotted box used for dropping a file??' -> plain caption + ? AppPopover) and killed the always-on Algorithm line ('do we really need to say algorithm: ... when the first line of the header IS the algorithm?' -> conditional unsigned-alg warning only). Winston's crate-grudge arc: 5th redesign, umbra-core gets exactly ONE const (MAX_INPUT_BYTES) - 'I'm not even going to make the joke.' Mary said nothing, visibly. Task 2a: AC6-AC22 written into the story file, developer signed off. Task 2b (per-slice impl, render-review each slice, ask before pushing) NOT started - its own working effort.
