---
topic: Umbra tool redesigns (Epic 8), starting with the JSON Formatter/Viewer (Story 8.1)
goal: produce Task 1 decision records for each tool via open scope discovery
updated: 2026-09-07T03:01
---

- (moment) Story 8.1 opener: room split fast on JSON tool scope -- Sally/Amelia want the editor made real (textarea has zero editor affordances); Winston pushed back hard on scope creep (diff = new json.rs surface, wants it costed); Mary flagged EXPERIENCE.md already parked diff as a MAYBE; Paige+Mary landed a near-free win (rewrite serde's generic error text to match the JWT-inspector voice bar); Sally's tree-search idea won fast unanimous nods, prompting a running bit -- Winston needling that everything that wins fast is the stuff that skips his crate.
- (outcome) Story 8.1 scope firmed up: repair ships preview-then-confirm (never silent auto-apply, per AD-9/EXPERIENCE.md honesty bar); transform ships TypeScript first, other languages deferred as their own call; JSONL deferred to the public backlog, not this story; tabbed IA (Explorer/Validate/Query/Diff/Transform) adopted as the redesign's container, composing with DESIGN.md's already-locked-but-unused Tab component. Mid-session: origin/main moved (PR #104) and tokenized JsonTree.vue's focus ring plus added a global src/styles/base.css layer that already styles bare textareas -- baseline_commit bumped to b357aff, story's Dev Notes corrected so Task 2 doesn't redo that work.
- (dynamic) Correction: the previous outcome entry overstated it -- repair's preview-then-confirm shape, TS-first transform, JSONL-deferred, and the tabbed IA are the room's live proposal, not yet confirmed by the developer. Still awaiting their answer on all four.
- (outcome) Confirmed by the developer 2026-08-24: repair ships preview-then-confirm; transform ships TypeScript first (Go/Python/YAML/CSV deferred as their own decision); JSONL deferred to the FR35 backlog; tabbed IA (Explorer/Validate/Repair/Query/Diff/Transform) adopted. Developer also caught a real gap the room hadn't named: French/i18n -- resolved as no AD-13 exception needed (no NL grammar involved, unlike cron), but surfaced a genuine pre-existing bug: toolError.ts's TRANSLATABLE_CODES has zero json-* coverage, folded into the Validate tab's scope. Decision record written to 8-1-json-decision-record.md; Story 8-1's Task 1 marked complete. Task 2 (real ACs + redesign) is next, not started.
- (outcome) Story 8.1 Task 2 ACs written 2026-08-24: room resolved both open items from Task 1 -- Repair is its own tab (cross-linked from Validate, not merged: preview-then-confirm needs its own real estate) and Query speaks JSONPath/RFC 9535 via serde_json_path (standardized, drops onto existing serde_json::Value, matches Explorer's own copy-JSONPath output -- JMESPath rejected for breaking that consistency). Winston's running bit continued (everything routes through his crate). AC6-14 written into the story file; Task 2 split into 2a (ACs, done) / 2b (implementation, not started).
- (moment) Story 8.2 (Base64) opener: room fast-agreed on live conversion + killing the Encode/Decode/Paste button cluster (Sally: Base64 is a lookup, not a pipeline like JSON -- Copy survives only as an output-panel icon). Developer wanted data-URI support but said they lack the knowledge to build it; asked what 'blob detection' even is -- both explained. Winston's running bit held: he objected to 'what is this?' detection on a security-adjacent tool, then stood down once Amelia showed parse_data_uri + sniff are small pure umbra-core functions ('That's a first.'). Container shape (tabs vs single view vs two-pane) still open -- built a /design canvas of all three for the developer to pick.
- (outcome) Story 8.2 (Base64) Task 1 decided 2026-08-29. Developer picked the single enriched view (Option A) off a 3-container design canvas -- tabs rejected as ceremony for a 2-job tool, two-pane rejected (halves vertical space, blobs are tall). Scope: KEPT the converter + both alphabets + caps + error model; live as-you-type conversion replaces Encode/Decode buttons; Paste button cut (Sally: Base64 is a lookup not a pipeline -- deliberate divergence from 8.1); Copy -> icon. ADDED: data URI build+decode with image preview, blob detection ('looks like a JWT/PNG/...') honest-by-construction per AD-9, line-wrap on encode. Anti-clutter rule the developer asked for: one priority-ordered contextual slot (error > detection > data-URI), caption not card, never stacked -- Amelia turned the vibe into a testable rule. CUT to backlog issues: hex view, decode-as-gzip, recursive decode. Winston's crate-grudge finally inverted -- this redesign is mostly his crate (parse_data_uri + sniff + wrap param). Decision record: 8-2-base64-decision-record.md. Next: file the 3 GitHub issues, then Task 2a (real ACs, same room).
- (outcome) Story 8.2 Task 2a done 2026-08-29, same room. Open items resolved: data-URI build is sync view-side string compose (NOT a new state group, no new latest-wins runner); detection = fixed first-match-wins order (not-Base64 > JWT > PNG/PDF/gzip/zip magic > UTF-8 text > unknown), no scoring; line-wrap = inline Encode-only selector, default none (Sally: changing emitted-output default for existing users is a trap). CSP-for-data:-preview and the final base64-* code list punted to 2b as implementation-discovery. Winston got his vertical-slice + developer-render-review-after-slice-1 discipline written into 2b (the 8.1 reflexive-copy safeguard). ACs AC6-16 in the story file. 3 cut-idea issues filed: #113/#114/#115. Awaiting developer sign-off before 2b implementation; single commit planned for record + story file.
- (moment) Story 8.3 (UUID) opener: room framed two users — generate-a-UUID vs inspect-one-I-have. Sally pitched a v7-timestamp inspector as 'the missing point of the tool'; Winston flagged it as a second tool (real umbra-core parser, version/variant decode, timestamp extract) not a reskin. Paige's near-free win: v4/v7 radios and bare 'UUID' heading explain nothing — needs 'random'/'timestamp-ordered' labels. Winston's crate-grudge running bit continued, now self-aware ('bracing for it to invert again'). Thrown to developer: do you generate or inspect in real life?
- (moment) 8.3: developer caught the room saying Story 2.3 shipped '2024' — actually 2026-07-31, one month ago; Mary owned it (her treasure-hunter instinct romanticizes any un-touched file into an artifact), John owned repeating it unc checked. Developer: doesn't use UUID tools IRL, doesn't know v4 vs v7 — room turned that into the evidence FOR a teach-the-versions feature (the author doesn't know, so no user does). Developer proposed a ? icon -> tooltip per version or per selector. Room converged: inline radio taglines (v4 — random / v7 — timestamp-ordered) as the cheap-correct base, ? for the deeper 'when do I pick which'. Amelia: NO tooltip component exists, only native title=; a real Popover.vue would be the app's FIRST floating surface -> new shared infra, AD-6, governance flag, exercises untested DESIGN.md floating-surface tokens. Winston declined to rubber-stamp this one despite it not touching his crate — wants it as its own line/story. Two forks thrown to developer: (1) inline taglines y/n, (2) native-title ? now vs spec real Popover as separate scope.
- (outcome) Story 8.3 (UUID) Task 1 decided 2026-08-30. Developer confirmed: ENRICHED single view (not tabs — one job, AppTabs stays unused; 8.1/8.2 discipline held). KEPT v4/v7 only + 1000 cap as-is (room reasoned it: DOM row-render is the binding limit, not compute; virtualisation deliberately skipped). ADDED full format-toggle set (lowercase/UPPERCASE/{braces}/no-hyphens — all view-side string ops, AD-1) + bulk download to file. Developer killed the per-result timestamp-decode inspector with one line — '100 UUIDs = 200 lines?' — Sally conceded instantly. CUT to backlog: v1/v6, v3/v5, inspector, ULID/nanoid/CUID. Paige's ?-tooltip idea became: inline taglines now (v4 · random / v7 · time-ordered), and a SEPARATE story for a shared Popover.vue — the app's first floating surface, governance-flagged, multi-tool consumer; Winston refused to rubber-stamp it despite it not touching his crate. Winston's crate-grudge ran its full arc and lost again: cleanest AD-1 split in Epic 8, umbra-core entirely untouched ('for the third time running, the redesign skips my crate. I'd say I'm surprised.'). Also: developer caught the room dating Story 2.3 to '2024' — it shipped 2026-07-31, one month ago; Mary owned the treasure-hunter reflex. Decision record: 8-3-uuid-decision-record.md. Next: file 5 backlog issues, publish enriched-view design canvas, then Task 2a (real ACs, same room).
- (outcome) 8.3 amendment (same session, right after the decision record): developer overruled the room's 'Popover = separate story' call and folded it INTO Story 8.3 — as a general reusable src/components/Popover.vue, the app's first floating surface. Winston's 'first floating surface deserves its own story' position lost to the developer's 'I want it in.' Now in-scope: taglines + a working ? on the version selector. Component API to be presented as options in Task 2a per CLAUDE.md. Backlog issues to file: 5 -> 4. umbra-core still untouched.
- (moment) Story 8.4 (Hash) opener: Sally pitched Verify/compare (sha256sum -c / 'verify your download') as the missing half of the tool; developer: 'in, of course'. Winston's crate-grudge running bit finally paid off — SHA-3 add = real umbra-core work (new sha3 dep, cross-platform CI, HashDigests struct reshaped to an Algorithm-keyed collection) after three redesigns skipped his crate ('Three stories I waited for this'). Mary needled his patience.
- (outcome) Story 8.4 (Hash) Task 1 decisions locked with developer (pre-canvas): KEPT the 4 digests + drop path + shared latest-wins runner + view-side case toggle. ADDED — (1) Verify/compare mode (paste expected hash -> match/no-match; mismatch is a role=status result, NOT a role=alert error, per EXPERIENCE.md instrument voice; no new error codes); (2) SHA3-256 + SHA3-512 via the sha3 crate (Digest-trait, clean add); (3) user-selects-algorithms via checkboxes, persisted as a hash.* setting (8.3 uuid.* pattern), default SHA-256/512 on — so umbra-core moves from HashDigests{4 named fields} to an Algorithm-enum-keyed collection, command takes an algo list, hash_compute_file gains a dropArgsProvider (Base64 pattern); (4) output encoding hex (default) + Base64 — both view-side transforms, hex->bytes->Base64 in JS, no core field; (5) live as-you-type hashing (debounced + length ceiling) replaces the explicit Compute button. Paige: MD5/SHA-1 checkbox labels become 'not collision-resistant', not 'legacy'. CUT (developer explicitly directed NOTHING filed to GitHub/backlog — deviation logged, 8.3 precedent): BLAKE2b, BLAKE3, byte-grouped hex, 0x prefix, HMAC mode, a signature-verify tool. Password hashing stays FR14-excluded as its own future tool. Container shape unresolved -> 4-artboard design canvas next (enriched single view / Compute|Verify tabs / Compute<->Verify segmented switch / polymorphic input a la Wildcard). Open for the canvas: Verify input-source model (compare currently-loaded input vs Verify's own file+hash pair) — developer wants an interactive example to decide.
- (outcome) Story 8.4 (Hash) Task 1 DECIDED 2026-08-31. Published a 4-option container canvas (claude.ai/code/artifact/c928d06d...); developer liked Options 1 (enriched single view) + 4 (polymorphic paste-offer), asked for the resting/empty state of 1 and the post-'Compare' state of 4 -> added 2 more artboards, 3 pages. Room's synthesis (Winston): they're not competing — Option 4 is a front door to Option 1's persistent Verify field. Developer chose: Option 1 as the container + smart paste-detection that MOVES a bare-hash input into the Verify field, with a VISIBLE acknowledgement the input moved (their explicit requirement; no silent swap, AD-9). UI quality bar = the UUID 8.3 redesign, NOT the wireframes. Winston's crate-grudge arc finally broke his way: HashDigests{4 named fields} -> an Algorithm enum + Vec<DigestEntry>, sha3='0.11' added to Cargo.toml, hash_compute/_file signatures take an algorithm list, hash_compute_file gains a dropArgsProvider. 'Three stories I waited for this.' Decision record: 8-4-hash-decision-record.md. AC3 deviation logged (developer: nothing filed to GitHub/backlog — 8.3 precedent). No drift found vs the story Dev Notes (rare — stated explicitly in the record). Next: Task 2a — real ACs, same room, scoped to the record + a polished canvas.
- (outcome) Story 8.4 (Hash) Task 2a done 2026-09-01, same room. Re-seeded the design canvas to delivery fidelity (4 artboards: Default / Verify active / Smart-detection offer / After 'Move it to Verify'). The acknowledgement moment the developer insisted on = two-ended: a note where the value LEFT + a briefly-tinted Verify panel where it LANDED + Undo — not a single missable toast. 15 ACs (AC6-AC20) written into the story file. Open items resolved: Verify checks against EVERY selected algorithm (developer's call — so the smart detector never has to disambiguate SHA-256 vs SHA3-256 at 64 hex chars); case+encoding = two composing persisted segmented controls; digest export CONFIRMED OUT (John's JTBD + Amelia's cost read: <=6 digests != a bulk problem, SHA256SUMS is a pipeline job — Winston: 'leave it cut'). Developer signed off. Next: Task 2b (per-slice implementation, render-review each slice) — its own working effort.
- (moment) Story 8.5 (JWT Inspector) opener: room convened on the signature question — segments[2] is discarded entirely today. Three forks framed: surface raw sig (zero crypto) / offline verification (crypto crate + AppTabs Decode|Verify + hard AD-7 audit) / stay decode-only. First Epic 8 tool where tabs and Winston's crate could BOTH genuinely be in play. JWT already carries clipboardMatch (EXPERIENCE.md Flow 4's worked example) — preserving that is a constraint. Thrown to developer (John's JTBD framing): do you verify JWT signatures in real life, and do you want it in this tool?
- (outcome) 8.5 fork 1 answered: developer does NOT manipulate JWTs IRL (uses them as web-auth access tokens, never hand-edits/verifies). NO signature verification this story — direction (b) is out. alg prominence + alg:none warning ships regardless (agreed). Open: (a) surface raw signature bytes display-only vs leave discarded — developer didn't follow (a) vs (b) distinction, room to explain plainly and re-ask.
- (outcome) 8.5 forks settled: signature = direction (c) stays discarded, no raw display, no verify; 'not verified' notice REWRITTEN as a calm clear statement not a grey footnote. FR18/Story-2.6-AC4 revised (verification P2->explicitly-out-of-scope, stays P3 backlog). Container = single enriched view, NO tabs (8.1-8.4 discipline; 5th redesign, Winston's crate untouched again - 'it is my personality'). Decode goes LIVE as-you-type (debounced), Decode button dropped. Header/payload STAY as <pre> for now - JsonTree.vue NOT reused, stays a JSON-tool island (AD-6 question closed clean). Per-block Copy added (header/payload/claim) in useCopyFeedback icon-button style - tool has none today.
- (outcome) 8.5 batch 2: PASTE BUTTON CUT (developer: 'hell no' to keeping it) - live decode + native Cmd+V + clipboardMatch front door make it redundant ceremony, 8.2 precedent. Tool is now ONE textarea + output, ZERO action buttons, no primary-orange anywhere (deliberate). onDecode/onPaste collapse to one debounced watch(token), one local latest-wins runner. Size cap: 1MB frontend+core defense-in-depth (Hash 8.4 AC19 pattern) - developer wants the over-cap STATE mocked not debated -> /design canvas. Live clock re-eval for expired + nbf 'not valid until' sibling status line: IN. Extended claims table iss/aud/sub/jti: BACKLOG candidate (redundant w/ kept <pre>). Both deferred-work.md items folded: Invalid-Date bounds check + wrong-type-claim factual role=status note. Next: build /design canvas of the redesigned single enriched view states, developer picks feed Task 2a.
- (outcome) 8.5: published design canvas 'JWT Inspector Redesign' (claude.ai/code/artifact/9ab4c58a-8f60-4cdd-9e71-8bdf32139529) - 4 artboards: Resting / Decoded-healthy / Decoded-flags-firing / Edge-states, light theme (dark parity owed). Shows: rewritten calm notice, live-decode textarea, alg line + alg:none unsigned warning (destructive text/glyph only, no filled box), header/payload <pre> w/ ghost Copy icons, claims w/ relative time, wrong-type-claim factual note (muted not red), Expired/Not-valid-yet status lines (muted+glyph, role=status), malformed role=alert, over-1MB role=status. AD-1 split came out ultra-clean: JwtDecoded struct + decode + decode_segment + numeric_claim + 11 tests + jwt_decode + jwtDecoded.ts mirror ALL UNCHANGED; only core change = MAX_INPUT_BYTES const + guard + ~3 tests + new jwt-input-too-large code (embeds byte count -> stays OUT of TRANSLATABLE_CODES like hash/json). Everything else view-side (AD-1 presentation): live debounced decode, alg extraction, live-clock expired/nbf computeds, relative time, formatClaim bounds check, wrong-type detection reads raw payload Value, per-block Copy via useCopyFeedback. NO verify primitive / NO crypto crate / NO Cargo.toml / NO command-surface / NO registry / NO clipboardMatch change. Winston: 5th redesign, gets exactly one const.
- (outcome) 8.5 canvas v2 + final scope calls: developer rejected the dashed-box notice ('why use the dotted box used for dropping a file?') and the always-on Algorithm line. RESOLVED: (1) 'not verified' becomes a PLAIN description caption under h1 (no box, like every other tool's .tool-desc) + a ? AppPopover (WeakHashPopover pattern) for the longer 3-parts explanation; (2) NO permanent Algorithm line - alg is line 2 of the header <pre> already; a CONDITIONAL boxless warning appears ONLY when alg is none/absent/non-string ('This token is unsigned (alg: none) - anyone can create or alter it', destructive text+triangle glyph, no box). Canvas re-published same URL. (1) NO persisted jwt.* data. (2) isJwtShaped tightening (eyJ-prefix cheap / decode+parse thorough) = AD-6 shell concern -> backlog issue, not this story. (3) cut ideas -> GitHub backlog-candidate issues (like #113-116), NOT personal backlog this time. (4) FR16/FR17/FR18 revisions agreed. Next: write 8-5-jwt-decision-record.md, file issues, Task 2a ACs, developer sign-off.
- (outcome) Story 8.5 (JWT) Task 1 + Task 2a DONE 2026-09-02, same room. Decision record 8-5-jwt-decision-record.md written (Kept/Changed/Added/Cut + container=single-enriched-view-no-tabs + FR16/17 expanded + FR18/2.6-AC4 downgraded + AD-1 split + i18n/TRANSLATABLE_CODES finding + JsonTree.vue=not-reused). AC3: 6 GitHub backlog-candidate issues filed #120-125 + comment on pre-existing #55 (verification) - developer confirmed GitHub route this story (not 8.3/8.4 personal backlog). Canvas 'JWT Inspector Redesign' published+revised once (9ab4c58a). Developer's two sharp catches this session: killed the dashed-box notice ('why use the dotted box used for dropping a file??' -> plain caption + ? AppPopover) and killed the always-on Algorithm line ('do we really need to say algorithm: ... when the first line of the header IS the algorithm?' -> conditional unsigned-alg warning only). Winston's crate-grudge arc: 5th redesign, umbra-core gets exactly ONE const (MAX_INPUT_BYTES) - 'I'm not even going to make the joke.' Mary said nothing, visibly. Task 2a: AC6-AC22 written into the story file, developer signed off. Task 2b (per-slice impl, render-review each slice, ask before pushing) NOT started - its own working effort.
- (moment) Story 8.6 (NL<->Cron) opener: first Epic 8 tool where AppTabs is genuinely live -- two real jobs, disjoint state groups, AD-16 amendment's own reference file. John pushed tabs; Sally countered (Base64 was bidirectional too, stayed single-view). Developer answered JTBD: BOTH directions matter, separate occasions, knows which each time -> points at two-door/tabs. Winston braced for another crate-skip then caught himself -- 'Cron IS my crate', this is the one redesign that can't route around umbra-core (describe() templater + NL parser + 48-row corpus all live there).
- (outcome) 8.6 MAJOR scope shift decided 2026-09-03: developer REJECTED the whole free-text NL->cron parser. Reframed the JTBD -- 'I forget field order' is a structure problem, not a language problem. Decision: DELETE parse_schedule + its entire grammar + the 48-row phrase corpus from cron.rs (git history preserves). REPLACE with a language-neutral GUIDED STRUCTURED BUILDER ([every][weekday v][at][time] controls) that works in EVERY shipped locale -- because a builder has no grammar to localise, only control-label i18n keys. This DELETES the AD-13 English-only problem entirely (nothing to disclose -> dashed notice gone). describe()+FieldSpec STAY (cron->English tab + builder live preview). Recorded as an AD-9 amendment: round-trip mandate no longer has a target here; applies to any FUTURE free-text/model NL->cron, now backlog. Developer challenged the room citing 'AD-9' reflexively -- fair; it IS in the current story as non-negotiable + is architecture-spine, but the builder direction changes what it governs. Custom small per-language NL->cron model = legit v2 (FR19 already says 'model upgrade is v2'), filed as FR35 backlog issue; Wildcard's point: the builder IS the model's future training set + fallback UI, so builder-first is step one of the model not an alternative. Winston: 6th redesign, cron is finally 'his crate', and the kept direction needs ZERO umbra-core -- Mary's treasure-map-to-empty-room bit again.
- (outcome) 8.6 container DECIDED 2026-09-03: ONE BIDIRECTIONAL GRID, no AppTabs (6th Epic 8 redesign to reject tabs -- but for a NEW reason: not 'one job' but 'both jobs are the same surface'). Shape: expression line (type/paste) + FLAT EXPERT GRID of 5 always-visible per-field editors (developer picked flat over progressive disclosure) each doing the FULL cron field grammar (every/value/list/range/step + L + #nth + W) + live panel (describe() prose one-liner + ALWAYS-ON per-field breakdown + next 3 runs). Bidirectional: edit expression->fields populate, edit field->expression recomposes. Seeded with a sane default (0 9 * * *), not empty/every-minute. Developer wants COMPLETE cron parity -- any schedule a cron can express, the grid can build (Q1 = month range 1-3, confirmed). 6-field seconds: REJECT with honest 'standard 5-field only'. Winston's crate FINALLY does real work: new full per-field parser+explainer -> Vec<FieldExplanation>, CronExplanation grows a fields[] member + TS mirror. DELETED: parse_schedule + whole NL grammar + 48-row corpus + 3 gate tests + ScheduleParseResult + cron_parse_schedule command + its lib.rs line (~700 lines). Mary's treasure-map bit inverted -- 'six digs and THIS is the one with gold in it.' Two things needing explicit developer sign-off before the record: FR21 retirement (no free-text = no honest-failure corpus) and EXPERIENCE.md Flow 3's scripted demo step ('type every Monday at 9am -> 0 9 * * 1') which NO LONGER EXISTS.
- (outcome) Story 8.6 (NL<->Cron) Task 1 DONE 2026-09-03, decision record 8-6-cron-decision-record.md written, awaiting developer sign-off. THE scope shift of Epic 8. Kept: explain/explain_at + describe()+FieldSpec templater + map_cron_error + cron-no-upcoming-runs guard + cron_explain command. Changed: two <hr>-split sections -> one bidirectional grid; Explain/Convert/Paste buttons all gone (live debounced recompute); Copy->icon (useCopyFeedback, 5th consumer, hoist STILL not done); Epic-7 tokenisation; 6-field rejected honestly. Added: the guided builder (flat 5-field-editor grid, plain grammar v1) + always-on per-field breakdown (new umbra-core Vec<FieldExplanation>, CronExplanation grows a member) + MAX_INPUT_BYTES. DELETED ~700 lines: parse_schedule + whole NL grammar + 48-row corpus + 3 gate tests + ScheduleParseResult + cron_parse_schedule command + its lib.rs line. FR21+FR22 RETIRED, FR19 rewritten, FR20 expanded. AD-9 amendment + AD-13 resolution = spine touches for developer ack. AC3 mixed: model-idea GitHub issue (unfiled), rest personal. Winston's 6-redesign crate-grudge arc RESOLVED at last -- cron IS his crate, the new per-field parser/explainer is real core work, ~700 lines deleted too. Mary: 'six digs, and THIS is the one with gold.' Developer twice pulled the room back from mechanics to premise ('are we even sure English->cron should exist?') -- that redirect produced the whole outcome. Next: sign-off -> Task 2a (real AC6+, same room).
- (outcome) 8.6 Task 1 SIGNED OFF 2026-09-03 (same session). Developer approved the full scope + the AD-9 amendment + AD-13 resolution (both -> ARCHITECTURE-SPINE.md at build). Cut #1 (small per-language NL->cron model) filed as GitHub dipaneb/umbra#130; cuts #2-8 personal backlog. Task 1 done. Next: Task 2a -- resolve the record's 11 open items + write real AC6+ in the same room. bmad-dev-story remains halted for Task 2a (its own facilitated session).
- (outcome) 8.6 Task 2a 2026-09-03: 11 open items resolved, AC6-AC26 written, awaiting sign-off. Field editor = HYBRID (Every/Specific segment + name helper on Month/DoW + auto-expand-only Advanced raw grammar) -- room had leaned 'expert raw input + rails', developer split the difference; Grumbal grudgingly OK once model was 'one string per field, controls are windows onto it' (no mode desync bug class). Developer calls: 6-field code cron-six-field-unsupported JOINS TRANSLATABLE_CODES (Paige: 'it's our sentence, not croner's'); generic describe() output SUPPRESSED via new description_generic bool (Sally: fallback prose is noise above a breakdown that says it better); MAX_INPUT_BYTES=1024. Winston's crate finally does real work: new Vec<FieldExplanation> fn + CronExplanation grows 2 members + ~700 lines NL grammar/corpus deleted. States/anatomy canvas 'Cron NL Redesign' published (f36c1906), 4 artboards light-only. Next: sign-off -> Task 2b per-slice.
- (outcome) Story 8.7 (Bucket/OCR) opener 2026-09-06: developer dropped the session's premise in one line -- the 'Bucket' grouping was never a product decision, it was an AI scaffolding mistake left unfixed for velocity. AC4a resolved instantly: THREE SEPARATE REGISTRY TOOLS, binds 8.8+8.9 (which are therefore no longer the stories epics.md describes -- correction owed). Winston's split-caution withdrawn; his affirmative argument: drop/paste/clipboardMatch are ALL OCR-only today, so the registry currently lies, and splitting makes declarations match reality rather than adding behaviour. Paige: typing 'merge' in Cmd-K returns a result called 'Bucket' -- the FR2 promise broken. Full split chosen over staged (PDF+Images move VERBATIM, no redesign -- Amelia's gate: if a moved PDF test needs rewriting, it stopped being a move). Name: 'Image to Text'. THEN the developer redirected the room again -- answered Winston's per-region-confidence question with 'can we make it like Apple Photos, select the text directly from the image'. Live Text. Amelia verified vendored oar-ocr-core-0.6.3: TextRegion has EIGHT fields (bounding_box polygon, dt_poly, rec_poly, orientation_angle, word_boxes, label) and run_ocr reads two and drops the struct -- the coordinates were there since Story 4.1. Mary: 'seven digs and the gold was sitting on the table.' Winston's blocker: the view has NO image bytes (AD-15, path-only, one sanctioned byte exception = clipboard RGBA, 'do not add a second'). Three ways out thrown to developer as an architecture fork: enable Tauri assetProtocol+scope (CSP already allows asset:/data: from 8.2; new webview FS edge = AD-14) / second byte-payload command (breaks AD-15's letter) / paste-path-only (room unanimously rejected). Spine amendment owed either way.
- (outcome) 8.7 batch 2, all forks answered 2026-09-06: (1) assetProtocol scoped ENABLED -- webview gets its first filesystem read edge, spine amendment owed (AD-14/AD-15 discussion; CSP already permitted asset:/data: since 8.2). (2) Editable textarea REPLACED by 'Copy all text' -- image-with-live-text IS the tool, no second copy of the text on screen (the 8.3 '100 UUIDs = 200 lines' redundancy rule again); FR24 revised, must propagate to prd.md+epics.md THIS story. (3) Low-confidence regions MARKED ON THE IMAGE, no number shown -- 'Bucket never bluffs' made visual, resolves the dead confidence field by making it useful. Amelia's verification round found the Bucket mistake runs deeper than the view: ALL NINE commands are bucket_* (incl. bucket_merge_pdfs) and ALL SIXTEEN error codes are bucket-* -- but umbra-core is CLEAN (ocr.rs/pdf.rs/image_convert.rs correctly named for capabilities). Rename scoped to OCR's own only, because bucket-internal + bucket-input-too-large are SHARED by pdf.rs and image.rs -- renaming them would break Amelia's verbatim-move gate; OCR gets NEW ocr-internal/ocr-input-too-large instead (duplication, not migration). PAIGE'S BIG FINDING, gap #9 reframed: TRANSLATABLE_CODES is the wrong fix -- nearly every bucket-* message is a wrapped third-party string (image crate, oar-ocr, tokio join) or embeds byte counts, so there is nothing OF OURS to translate. The real bug is a VOICE gap: the most-hit error in the app's flagship feature (drop a non-image) speaks in the image crate's English. Fix = write our own sentence, THEN it qualifies. Direct callback to Paige+Mary's Story 8.1 serde-error-voice win, seven stories apart -- 'I've been consistent.' AC4d: file picker becomes a THIRD write-trigger, so the view calls registry.getLatestWinsRunner directly for the first time (NOT a local runner) -- one runner, three doors, HashView shape; BucketView's three long 'why no local runner' comments become wrong and need rewriting (8.6's rationale-died-decision-survived case). Winston's tally: trait change + 3-way split + spine amendment + 7 code reshapes. Mary: 'seven digs.'
- (moment) 8.7 sign-off REFUSED by the developer 2026-09-06, and they were right: 'we didn't have time to discuss other things... especially with our first AI feature we didn't discuss anything about feature.' The whole session had gone to structural debt (the 3-way split, renames, runner scoping, the asset-protocol amendment) and the record held exactly ONE design idea -- Live Text -- which the DEVELOPER supplied, not the room. John owned it ('that's debt, not design'), Sally owned freezing on Live Text because it was obviously right, Winston owned steering every round to the split because it binds 8.8/8.9 ('defensible triage and still triage'). Reopened generative. Biggest miss found immediately: the developer had NAMED 'PDF pages with unselectable text' as a real use, and NEITHER tool serves it -- pdf extract-text reads only the embedded text layer (a scan returns 'no text in this PDF', true and useless), and OCR won't open a PDF. Today's honest answer is 'screenshot your own PDF' inside an app that owns both halves; the 3-way split turns a hidden seam into a VISIBLE gap between two sidebar entries. Winston costed it: lopdf parses, doesn't render -- rasterizing needs pdfium/mupdf, big native dep + cross-platform build + fresh AD-7 audit. Own story, possibly own epic -> becomes the record's highest-value CUT, not an omission. Amelia found a latent shipping BUG: run_ocr does lines.join('\n') in model DETECTION order, not reading order -- two-column screenshots interleave and no test catches it (every fixture asserts contains('UMBRA'); Paige: 8.6's lesson 7 again). Fix is FREE once geometry flows -- sort regions by bounding box. Winston accepted it on the spot. Sally: Cmd-F highlighting matched regions ON the image (Preview/Photos pattern, cheap, Winston had NO objection -- Mary: 'mark the date'), and nobody had discussed the RESTING state, the most-seen state in the app, for the tool we just called the demo feature. Also surfaced and undiscussed: QR codes (both Shottr and CleanShot do it in the same gesture -- same JTBD, different tech, new dep + AD-7 audit), TextRegion.label ('text'/'formula'/'seal', also discarded), and handwriting as an FR26 honesty risk that the low-confidence marking may or may not cover. AC3 route decided: SPLIT -- file only the substantial cuts to GitHub, rest personal.
- (outcome) 8.7 generative round, post-refusal 2026-09-06 -- the round the room owed. PDF-scan job resolved as THREE TIERS, developer agreed: T1 (in 8.7) a PDF dropped on Image to Text stops failing with the image crate's raw error and instead says PDFs open in the PDF tool -- magic-byte check + a sentence, no dependency; T2 (handed to 8.8 IN WRITING) the PDF tool's 'no text in this PDF' becomes 'this is a scan, its pages are images' -- lopdf already in tree; T3 (GitHub issue w/ full cost sheet) actual rasterize+OCR. Winston's T3 cost sheet: pdfium-render binds at RUNTIME so you ship libpdfium x4 platforms; roughly DOUBLES the 6.2MB bundle; SECOND native C++ dep and this project has a scar (Story 4.1 was BLOCKED by clipper2c-sys vs a broken Xcode CLT C++ stdlib); DPI/memory + the parked batch-concurrency question; licence CLEARED, PDFium is BSD-3-Clause, closed-source commercial redistribution fine w/ attribution (MuPDF would have been AGPL-blocking). Strongest downside, Winston to Sally 'you'll hate this': macOS Preview ALREADY does Live Text on scanned PDFs on the developer's own platform -- Mary countered that 'the OS already does it' is exactly the argument AD-8 rejected when choosing ONNX over Vision for NFR3. Mary refused to invent a prevalence statistic ('there is no reliable public number, I'm not making one up') and reframed: scanned PDFs are defined by ORIGIN not volume, so it's rare-but-blocking, costed by what happens when you hit one. Paige's unlock: 'serving the attempt is not the same as serving the extraction.' PAIGE ALSO FOUND THE SESSION'S SHARPEST ACCESSIBILITY CATCH: Live Text replaces a labelled textarea (unambiguously accessible) with transparent positioned spans read in DOM ORDER -- which is currently model DETECTION order -- so Amelia's reading-order bug is ALSO an NFR5/WCAG bug, and one geometric sort now serves three things (Copy fidelity, screen-reader order, Cmd-F match order). Sally unfroze and delivered: the RESTING state (most-seen state in the app, never designed -- a sentence where an affordance should be; becomes a real bordered drop target with the picker INSIDE it, dashed-border language legitimately used here where 8.5 banned it elsewhere; needs DropZone to expose drag-enter, a named shell change); and the in-flight fix is NOT a spinner -- SHOW THE IMAGE IMMEDIATELY from the path, text resolves onto it a beat later, so the ONNX wait (worst on first run, the OnceLock load) happens against something recognisable rather than a blank pane. Winston: free, nothing serialises them but us not thinking of it. Also decided: Cmd-A selects all image text (keyboard 'copy and leave'), Copy-all lives ON the image not under it (a second zone would compete with the thing we just made the tool), second drop REPLACES (no session, no history -- John: the job ends when you paste; Sally: privacy-correct). Handwriting stated as the REASON low-confidence marking exists (PP-OCRv6 tiny is printed-text-trained; confident nonsense is the FR26 risk), not a happy side effect. QR codes OUT to a real GitHub issue (developer's call).
- (outcome) 8.7 round 4, 2026-09-06 -- the developer refused sign-off a SECOND time, and again correctly: 'we need to think about the onnx runtime, the model used, if it's multilanguage, the whole pipeline (do we apply filters or resize before the model)... AI feature means some new thinking.' The record had a front door and a back door and NOTHING IN BETWEEN. Amelia audited the vendored oar-ocr source: OarOcrEngine::new calls OAROCRBuilder::new(det,rec,dict).build() and NOTHING ELSE -- no det config, no rec config, no text_type, no orientation classifier, no return_word_box, no ort_session. SIX inherited defaults nobody ever chose, in the app's only AI feature: (1) limit_side_len=960 limit_type=max -- EVERY image is downscaled so its longest side is <=960px before detection, so a 3024x1964 Retina screenshot is recognised at ~960x623, 13pt UI text at ~4px. Highest-leverage parameter in the pipeline, never examined, and it silently contradicts Live Text (overlay on a 3024px image whose recognition happened 3x smaller). Trades directly against FR24's ~3s bar -> a MEASUREMENT, not an argument. (2) max_text_length is AMBIGUOUS: TextRecognitionConfig::default()=25, the predictor builder=100, OAROCRBuilder's own test=128 -- cannot be resolved by static reading; if 25 bounds CTC decoding it TRUNCATES long lines silently and no test catches it (all fixtures assert contains('UMBRA') on a short fixture). Needs an empirical long-line test. (3) orientation_angle is ALWAYS None -- needs .with_text_line_orientation_classification() + a THIRD bundled model; Amelia self-corrected a claim she'd put in the record an hour earlier. (4) return_word_box=false, and the doc says word boxes need rec-model support PP-OCRv6 tiny may not have -- confirms the word-level cut for the right reason. (5) GOOD NEWS: recognition score_threshold defaults to 0.0, NO filtering -- so text:None means recognition genuinely failed, nothing is silently dropped, and the low-confidence marking is honest by construction. (6) no execution provider configured -> ort runs CPU; OrtExecutionProvider::CoreML exists and is unused on the exact platform FR24's 3s bar is written against. Plus Winston's risk, not a knob: ort is pinned to 2.0.0-rc.12, a RELEASE CANDIDATE, held by exact pin because oar-ocr-core 0.6.3 declares rc.12 but doesn't compile against rc.13 (Story 4.1's discovery) -- the whole inference layer sits on a pre-release we can't move off while the rustc floor blocks oar-ocr>0.6.3. Mary: 'six decisions nobody in this project has ever made -- inherited from whatever .build() does when you give it three paths.' John: 'you weren't crazy, an AI feature has a MIDDLE.'
- (outcome) 8.7 round 5, 2026-09-06: developer supplied the missing NUMBER by just running it -- '~3 seconds, but three seconds feels slow when you put an image and have no visual feedback.' Reframed the whole performance thread: it was never a speed problem, it's a FEEDBACK problem, and it's exactly gap #2 + Sally's image-renders-immediately decision. Developer's scoping call: build the simple feedback now, file NPU/execution-provider optimisation as a GitHub issue. Amelia's correction on the word 'progress bar': ONNX inference reports NO progress -- no callback, no fraction -- so a determinate bar would be a fake animation timed to nothing, and Paige killed it on honesty grounds ('a bar that fills at a rate we invented is a bluff about our own internals' -- FR26 / the-Bucket-never-bluffs applied to our OWN internals, not just to results). So: indeterminate indicator over an image that is already visible. THEN Winston's idea, the best of the session and Paige called it 'the most on-brand idea anyone has had': it's TWO models, detection then recognition, two sequential ONNX passes -- so the detected BOXES could appear on the image first and the recognised strings fill into them as rec completes. Not a progress indicator: the actual work made visible, every frame corresponding to something that really happened. Amelia costed it: OAROCR::predict() is ONE call, staging means driving oar-ocr-core's det/rec predictors separately (a layer deeper into a pre-1.0 crate, past what Story 4.1 verified) and the OcrEngine trait growing a two-phase/streaming shape -- much bigger than the agreed trait change. Cut with a name and a reason. Winston's earlier EP analysis, redone after the developer correctly pushed back on 'CoreML' as too narrow: six compile-time Cargo features on oar-ocr (coreml/cuda/directml/openvino/tensorrt/webgpu), Umbra enables NONE. Sorted by 'does the END USER have to install a vendor runtime' -- CoreML (macOS), DirectML (Win10+, any DX12 GPU), WebGPU (the only genuinely cross-platform one, and the only realistic Linux answer) are viable; CUDA/TensorRT/OpenVINO are OUT on distribution grounds, not performance ('nobody downloads a 15MB utility then installs the CUDA toolkit'). They're COMPILE-TIME features, so it's a build-matrix decision = 3 binaries with 3 inference stacks + a fresh AD-7 audit, and Amelia's constraint: we can only measure on the machine we have, so a CoreML number is real and a DirectML number would be a guess.
- (moment) 8.7 round 6, 2026-09-06 -- developer's third catch, and the sharpest: 'the only thing that comes to my mind is the choice of the model, which I found scandalous that you didn't even mention it, even if it was to say the choice is already done.' Mary noted it WAS in the record; John: 'as a FOOTNOTE, at the end of a table about resize limits, for the app's only AI feature where the model IS the product.' Winston found the worse problem and owned it: the footnote deferred tier re-evaluation 'until finding #1 is measured' -- and finding #1 was then decided WITHOUT measurement, so the gate could never fire. A dangling dependency of his own making. Room answered with facts instead of prose. THREE tiers exist, not two, all with official PaddlePaddle _onnx repos: tiny (shipped) 6.0MB, small 29.6MB (~5x, NEVER PREVIOUSLY MENTIONED BY ANYONE), medium 132.2MB (~22x) -- and ARCHITECTURE-SPINE.md records MEDIUM as 'the documented fallback if tiny proves inadequate', a note written in Story 4.1 without pulling file sizes. Paige: 'the escape hatch we wrote down is a 132MB download for a utility whose pitch is small, offline, yours -- that isn't a fallback, it's a different product.' Spine correction owed: name small, not medium. DECISION: tiny stays, but now FOR A REASON -- direction of cost is known (bigger rec model = slower per region, on top of limit_side_len already raised to 1600, against a MEASURED 3s baseline; plausibly 3s -> 10s, past the threshold where users disengage regardless of indicator) while the benefit is unquantified, and Winston explicitly DECLINED to guess ('I'd be guessing'), matching Mary's earlier refusal to invent a PDF-scan prevalence number. Cheaper lever pulled first: 1600 gives the EXISTING model ~4x the pixels at a fraction of small's cost. New concrete revisit gate replacing the broken one: if quality is still poor at 1600 with reading-order fixed, during Task 2b render reviews, evaluate small. PAIGE'S BEST FIND OF THE ROUND: PP-OCRv5 ships LANGUAGE-SPECIFIC rec models (latin_/en_/korean_/arabic_/cyrillic_/devanagari_...). Our 6904-entry dict is mostly CHINESE, serving a script this app never claimed to support. A Latin-script recogniser is 'not more model, but BETTER-TARGETED model' -- the more interesting bet for an EN+FR tool. Cut #17, personal backlog, v5-not-v6 so it needs Story 4.1's whole verification repeated.
- (outcome) 8.7 round 7 (final audit), 2026-09-06 -- developer said 'you tell me if something is missing', John refused to answer 'nothing' and audited properly. THREE findings, all verified against vendored image-0.25.10, all landing on the 'photos of documents' use: (1) LIVE BUG -- EXIF orientation is NEVER applied. load_from_memory is ImageReader::with_guessed_format().decode() and decode() does not apply orientation; the crate requires calling orientation()/apply_orientation() explicitly. A phone photo held sideways reaches the detector rotated 90deg, detection largely fails, user gets 'no text found' on an image visibly full of text -- confidently wrong, exactly what FR26 exists to prevent. FOLDED IN. (2) The format claim has been UNDER-stating the code since Story 4.1: FR23/registry/dropHint all say 'PNG, JPEG, WebP' but image's default-formats decodes FIFTEEN (tiff and bmp included -- and scanners produce TIFF). Paige caught that the error sentence she'd drafted an hour earlier ('PNG, JPEG and WebP are supported') WOULD HAVE BEEN A LIE. Winston: 'third time this session that writing down what the code actually does contradicted what the docs claim.' Developer chose VAGUE AND TRUE: 'Drop an image' / 'That file isn't an image this tool can read' -- no enumeration to drift with the image crate's features, still value-free+ours so still TRANSLATABLE_CODES-eligible. (3) HEIC unsupported (iPhone default) -> personal backlog, libheif = same native-dep class as the PDF rasterizer. ALSO IN SCOPE now: a QUALITY REGRESSION CORPUS (developer's call) -- the story changes limit_side_len, adds reading-order sort and applies EXIF, and the only quality assertion in the whole codebase is contains('UMBRA') on one short fixture; 8.6's lesson 7 again. Detection-threshold tuning (box_threshold 0.6, unclip_ratio 2.0, text_type 'table' preset) cut to backlog DELIBERATELY BEHIND the corpus -- 'tuning thresholds without a way to measure regression is guessing with extra steps'. Open item added: concurrency under latest-wins (one shared OAROCR behind &self, superseded requests still run to completion, so two ONNX jobs genuinely overlap -- Sync is compiler-enforced so it's safe, contention is unmeasured). Session pattern worth remembering: the developer refused sign-off THREE times (missing design round / missing AI pipeline / model choice as a footnote) and was right every time; each refusal produced the best material in the record.
